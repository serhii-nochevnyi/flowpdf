{
  "name": "flowpdf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.9.0",
  "scripts": {
    "build:wasm": "RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo build -p flow-wasm --target wasm32-unknown-unknown --release --locked && RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH wasm-bindgen target/wasm32-unknown-unknown/release/flow_wasm.wasm --target web --out-dir web/generated --out-name flow_wasm",
    "build:web": "npm run build:wasm && tsc -p tsconfig.web.json && node scripts/build-web.mjs",
    "check": "node scripts/check-phase1.mjs",
    "inspector": "npm run build:web && node scripts/serve-inspector.mjs",
    "test": "vitest run --passWithNoTests",
    "test:browser": "PLAYWRIGHT_BROWSERS_PATH=./work/playwright vitest run --project browser --passWithNoTests",
    "test:unit": "PLAYWRIGHT_BROWSERS_PATH=./work/playwright vitest run --project unit --project inspector-unit --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "4.1.6",
    "fake-indexeddb": "6.2.4",
    "playwright": "1.57.0",
    "typescript": "5.9.3",
    "vitest": "4.1.6"
  }
}
