---
phase: FLOWPDF-04-owned-pdf-preview-and-export
plan: "03"
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - crates/flow-core/src/pdf/assets.rs
  - crates/flow-core/src/pdf/metadata.rs
  - crates/flow-core/src/pdf/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/pdf_resources.rs
autonomous: true
requirements: [PDFX-05, PDFX-08, QUAL-07]
estimate: { tokens: 20000, raw_tokens: 20000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Supported PNG/JPEG assets are emitted through bounded validated image streams at the same derived rectangles as the display list."
    - "Export metadata and optional outline/link records are deterministic and unsupported source constructs are explicit diagnostics."
    - "Owned output contains no JavaScript, launch action, external resource action, or unvalidated raw asset bytes."
  artifacts:
    - "Bounded PDF image/resource adapter reusing the existing asset validation limits."
    - "Typed export metadata/outline/link support matrix and structural resource tests."
  key_links:
    - "Redeemed asset record -> validated PDF image stream -> display-list rect/resource identity."
---

<objective>Complete the supported PDF resource and document-structure subset without widening the hostile-input surface.</objective>

<context>
@.planning/phases/FLOWPDF-04-owned-pdf-preview-and-export/04-CONTEXT.md
@crates/flow-core/src/asset/mod.rs
@crates/flow-core/src/store/mod.rs
@crates/flow-core/src/model/mod.rs
@crates/flow-core/src/pdf/mod.rs
</context>

<tasks>
<task id="04-03-01" type="auto" tdd="true">
  <name>04-03-01: Add bounded PNG/JPEG image resources and supported display-list resources</name>
  <read_first>
    - crates/flow-core/src/asset/mod.rs
    - crates/flow-core/src/store/mod.rs
    - crates/flow-core/src/pdf/display_list.rs
  </read_first>
  <files>crates/flow-core/src/pdf/assets.rs, crates/flow-core/src/pdf/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/pdf_resources.rs</files>
  <behavior>
    - Export accepts only a matching revision-owned asset record whose content hash, media type, encoded length, dimensions, and bytes pass existing limits.
    - PNG/JPEG resources are deterministic and are referenced once per content identity; invalid, missing, oversized, or mismatched assets fail closed.
    - Image rectangles use display-list fixed-point coordinates and do not trigger browser-side scaling/layout.
  </behavior>
  <action>Implement the asset provider and PDF image resource writer using the existing bounded decoder/admission rules. Keep physical asset bytes outside semantic DTOs, sort resources by stable asset identity, and return explicit diagnostics for unsupported formats or missing records.</action>
  <acceptance_criteria>
    - Tests cover valid PNG/JPEG, deduplication, content-hash mismatch, dimension/decoded-byte limits, missing assets, and deterministic resource numbering.
    - No asset bytes appear in diagnostic strings or export provenance JSON.
  </acceptance_criteria>
  <verify><automated>cargo test -p flow-core --test pdf_resources</automated></verify>
  <done>Supported image content is safely represented in the owned PDF subset.</done>
</task>

<task id="04-03-02" type="auto" tdd="true">
  <name>04-03-02: Add deterministic metadata, outlines, links, and unsupported-feature reporting</name>
  <read_first>
    - crates/flow-core/src/model/mod.rs
    - crates/flow-core/src/pdf/mod.rs
    - .planning/phases/FLOWPDF-04-owned-pdf-preview-and-export/04-CONTEXT.md
  </read_first>
  <files>crates/flow-core/src/pdf/metadata.rs, crates/flow-core/src/pdf/mod.rs, crates/flow-core/tests/pdf_resources.rs</files>
  <behavior>
    - Export options provide bounded title/author/subject/language and optional typed outline/link entries with deterministic names and destinations.
    - Only source-model constructs with explicit support are emitted; fields, actions, annotations, paths, or links not represented by the current model are reported rather than inferred.
    - The output explicitly excludes JavaScript, launch, external-file, and encryption entries.
  </behavior>
  <action>Add a typed metadata/outline/link export option and a stable unsupported-content report. Preserve PDF name/string escaping and deterministic ordering; keep the support matrix visible in the export result so later phases can extend it without changing existing claims.</action>
  <acceptance_criteria>
    - Tests assert metadata escaping, outline/page destination ordering, supported-link validation, unsupported diagnostics, and forbidden-action absence by byte scan/structural inspection.
    - Export hashes change only when source/options/resources change.
  </acceptance_criteria>
  <verify><automated>cargo test -p flow-core --test pdf_resources && cargo test -p flow-core</automated></verify>
  <done>PDFX-05's supported resource/document subset is explicit, bounded, and deterministic.</done>
</task>
</tasks>

<artifacts>Image resources, metadata/outline/link support, and unsupported-feature diagnostics.</artifacts>
<verification>Run resource-focused and full Rust tests.</verification>
<success_criteria>No supported image/resource is silently dropped and no unsupported action is emitted.</success_criteria>
<output>Create `04-03-SUMMARY.md` after execution.</output>
