---
phase: FLOWPDF-04-owned-pdf-preview-and-export
plan: "04"
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - crates/flow-core/src/pdf/provenance.rs
  - crates/flow-core/src/pdf/recovery.rs
  - crates/flow-core/src/pdf/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/pdf_recovery.rs
autonomous: true
requirements: [PDFX-07, PDFX-08, QUAL-07]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Every completed export records the exact semantic/layout/font/data/options inputs needed to reproduce it."
    - "An owned PDF with a valid private source payload recovers the exact canonical FlowDocument and hash; absent or corrupt payloads are not called exact."
    - "Recovery is bounded and does not execute PDF actions or trust ordinary viewer metadata as canonical source."
  artifacts:
    - "Versioned export manifest and private source-payload stream with integrity checks."
    - "Rust recovery API and adversarial tests for truncation, hash mismatch, wrong revision, and payload limits."
  key_links:
    - "Immutable export request -> manifest/source payload -> PDF metadata stream -> validated recovery result."
---

<objective>Make owned export reproducible and distinguish exact source recovery from unavailable external reconstruction.</objective>

<context>
@.planning/phases/FLOWPDF-04-owned-pdf-preview-and-export/04-CONTEXT.md
@.planning/phases/FLOWPDF-04-owned-pdf-preview-and-export/04-RESEARCH.md
@crates/flow-core/src/canonical/mod.rs
@crates/flow-core/src/provenance/mod.rs
@crates/flow-core/src/pdf/mod.rs
</context>

<tasks>
<task id="04-04-01" type="auto" tdd="true">
  <name>04-04-01: Add versioned reproducibility manifest and source-payload stream</name>
  <read_first>
    - crates/flow-core/src/canonical/mod.rs
    - crates/flow-core/src/provenance/mod.rs
    - crates/flow-core/src/pdf/mod.rs
  </read_first>
  <files>crates/flow-core/src/pdf/provenance.rs, crates/flow-core/src/pdf/mod.rs, crates/flow-core/tests/pdf_recovery.rs</files>
  <behavior>
    - Manifest includes source document/revision/hash, layout/result hash, engine version, font face/catalog identities and hashes, hyphenation identity, schema/export versions, and normalized options.
    - Source payload is bounded, canonical JSON only, namespaced/versioned, and hash-bound; ordinary PDF metadata never becomes the source of truth.
    - Manifest serialization and stream placement are deterministic and do not include raw font/image bytes.
  </behavior>
  <action>Implement the typed manifest and private metadata stream over the COS writer. Validate canonical bytes against the current document before embedding and reject payloads that exceed the explicit recovery budget.</action>
  <acceptance_criteria>
    - Tests prove manifest byte stability, source hash binding, omission of sensitive bytes, and export-option changes altering the manifest/result hash.
  </acceptance_criteria>
  <verify><automated>cargo test -p flow-core --test pdf_recovery</automated></verify>
  <done>An export carries auditable, reproducibility-bound owned-source material.</done>
</task>

<task id="04-04-02" type="auto" tdd="true">
  <name>04-04-02: Implement exact owned-source recovery and failure classification</name>
  <read_first>
    - crates/flow-core/src/pdf/provenance.rs
    - crates/flow-core/src/pdf/mod.rs
    - crates/flow-core/tests/pdf_recovery.rs
  </read_first>
  <files>crates/flow-core/src/pdf/recovery.rs, crates/flow-core/src/pdf/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/pdf_recovery.rs</files>
  <behavior>
    - Recovery accepts only a validated embedded payload and returns canonical JSON/hash/revision/provenance identity matching the manifest.
    - Missing payload, unsupported external PDF, invalid canonical bytes, wrong document ID, hash mismatch, truncation, and budget overflow return distinct stable codes.
    - Recovery never executes actions, follows external references, or mutates the active editor.
  </behavior>
  <action>Add a bounded recovery function over an explicitly supplied owned-PDF source envelope. Keep parsing limited to the private namespaced stream until the later controlled reader phase; use canonical Rust revalidation for the final result and clearly label unavailable external reconstruction.</action>
  <acceptance_criteria>
    - Adversarial tests cover all failure classifications and assert no false `exact` result is possible.
    - Full Rust tests and provenance checks pass.
  </acceptance_criteria>
  <verify><automated>cargo test -p flow-core --test pdf_recovery && cargo test -p flow-core</automated></verify>
  <done>PDFX-07 has a bounded exact-owned recovery contract without external-PDF claims.</done>
</task>
</tasks>

<artifacts>Reproducibility manifest, private source payload, exact recovery API, and failure taxonomy.</artifacts>
<verification>Run focused adversarial and full Rust suites.</verification>
<success_criteria>Owned recovery is exact only when every source/manifest/hash check passes.</success_criteria>
<output>Create `04-04-SUMMARY.md` after execution.</output>
