---
phase: FLOWPDF-04-owned-pdf-preview-and-export
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/flow-core/src/pdf/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/pdf_cos.rs
autonomous: true
requirements: [PDFX-02, PDFX-08, QUAL-07]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Rust can serialize a bounded owned PDF object graph with deterministic object IDs, references, streams, xref, and trailer."
    - "Repeated export of the same synthetic page plan produces byte-identical PDF output and a stable export fingerprint."
    - "Malformed, oversized, indirect-cycle, and unsupported COS inputs fail with stable diagnostics before partial output is published."
  artifacts:
    - "flow-core pdf module with closed COS values, fixed-point coordinate formatter, export request/result, and deterministic writer."
    - "Rust contract tests covering escaping, object ordering, xref offsets, limits, and repeatability."
  key_links:
    - "Export request validation -> owned COS graph -> deterministic byte writer -> structural export result."
---

<objective>Establish the owned, deterministic PDF syntax foundation before adding fonts or browser export.</objective>

<context>
@.planning/phases/FLOWPDF-04-owned-pdf-preview-and-export/04-CONTEXT.md
@.planning/phases/FLOWPDF-04-owned-pdf-preview-and-export/04-RESEARCH.md
@.planning/phases/FLOWPDF-04-owned-pdf-preview-and-export/04-PATTERNS.md
@crates/flow-core/src/layout/mod.rs
</context>

<tasks>
<task id="04-01-01" type="auto" tdd="true">
  <name>04-01-01: Add bounded COS values and canonical PDF token serialization</name>
  <read_first>
    - crates/flow-core/src/layout/mod.rs
    - crates/flow-core/src/canonical/mod.rs
    - crates/flow-core/src/lib.rs
  </read_first>
  <files>crates/flow-core/src/pdf/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/pdf_cos.rs</files>
  <behavior>
    - Typed null, boolean, integer, fixed decimal, name, byte string, array, dictionary, stream, and indirect-reference values serialize with PDF escaping and no host-dependent ordering.
    - `LayoutUnit` coordinates are formatted from raw 1/64-point integers through one checked decimal helper; negative, zero, and boundary values have golden tokens.
    - Limits bound object count, nesting, names, strings, streams, and final bytes; no generic `serde_json::Value` or arbitrary property bag enters the PDF writer.
  </behavior>
  <action>Implement the private/closed COS model and canonical token writer. Keep object numbers assigned by insertion order from an explicit resource plan, reject duplicate IDs and dangling references, and expose only stable error codes plus immutable bytes at the public export boundary.</action>
  <acceptance_criteria>
    - Unit tests prove canonical name/string escaping, dictionary ordering, exact fixed-point formatting, depth/size rejection, and no partial output on failure.
    - The writer has no browser, filesystem, clock, random, or floating-point dependency.
  </acceptance_criteria>
  <verify><automated>cargo test -p flow-core --test pdf_cos</automated></verify>
  <done>The core can build and validate a deterministic bounded COS graph.</done>
</task>

<task id="04-01-02" type="auto" tdd="true">
  <name>04-01-02: Write a deterministic minimal PDF page/export envelope</name>
  <read_first>
    - crates/flow-core/src/pdf/mod.rs
    - crates/flow-core/tests/pdf_cos.rs
    - .planning/phases/FLOWPDF-04-owned-pdf-preview-and-export/04-CONTEXT.md
  </read_first>
  <files>crates/flow-core/src/pdf/mod.rs, crates/flow-core/tests/pdf_cos.rs</files>
  <behavior>
    - A validated export request writes a PDF header, catalog, page tree, one or more bounded page dictionaries, content streams, xref, trailer, and startxref.
    - The export result reports source/layout identities and a byte hash without exposing mutable writer state.
    - Two runs with the same request are byte-identical; changing page order or an export option changes the result hash deterministically.
  </behavior>
  <action>Add the minimal page/export envelope over a typed page-plan input used only by tests and the later display-list adapter. Keep text/font resources intentionally explicit placeholders until Plan 04-02; do not claim selectable production text from this plan. Validate every indirect reference and xref offset before returning bytes.</action>
  <acceptance_criteria>
    - Tests parse the emitted bytes with an internal structural scanner and assert one root, a complete page tree, matching xref offsets, bounded streams, and a deterministic trailer ID derived from the request fingerprint.
    - The result is rejected when source revision/hash or layout fingerprint is absent or inconsistent.
  </acceptance_criteria>
  <verify><automated>cargo test -p flow-core --test pdf_cos</automated></verify>
  <done>A deterministic, structurally valid owned PDF envelope is available to downstream plans without overstating text/font support.</done>
</task>
</tasks>

<artifacts>Bounded COS model, deterministic PDF page envelope, and Rust syntax tests.</artifacts>
<verification>Run the focused Rust test and the full flow-core suite before committing.</verification>
<success_criteria>Repeated valid inputs produce identical bytes and all malformed/oversized graph cases fail closed.</success_criteria>
<output>Create `04-01-SUMMARY.md` after execution.</output>
