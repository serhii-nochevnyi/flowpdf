---
phase: FLOWPDF-04-owned-pdf-preview-and-export
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - crates/flow-core/src/pdf/display_list.rs
  - crates/flow-core/src/pdf/font.rs
  - crates/flow-core/src/pdf/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/pdf_text.rs
  - fixtures/layout/phase4-text-corpus.json
autonomous: true
requirements: [PDFX-02, PDFX-03, PDFX-04, PDFX-06]
estimate: { tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "The same Rust-shaped glyph/source ranges that drive pagination drive PDF text placement."
    - "Ukrainian, English, combining-mark, emoji/unsupported, and mixed-direction fixtures either extract through explicit Unicode mappings or fail with an explicit diagnostic."
    - "Admitted font resources and widths are deterministic and bound to the checked-in font catalog/provenance."
  artifacts:
    - "Rust display-list adapter with source ranges, glyph IDs, fixed-point placements, and page ordering."
    - "PDF font resource writer and ToUnicode mapping tests for the admitted fixture matrix."
  key_links:
    - "PaginationResult + canonical FlowDocument -> display list -> font resource/content stream -> selectable PDF bytes."
---

<objective>Connect Phase 3 shaped layout to deterministic selectable PDF text and explicit font resources.</objective>

<context>
@.planning/phases/FLOWPDF-04-owned-pdf-preview-and-export/04-CONTEXT.md
@.planning/phases/FLOWPDF-04-owned-pdf-preview-and-export/04-RESEARCH.md
@crates/flow-core/src/layout/mod.rs
@crates/flow-core/src/model/mod.rs
@crates/flow-core/src/pdf/mod.rs
</context>

<tasks>
<task id="04-02-01" type="auto" tdd="true">
  <name>04-02-01: Build a revision-bound Rust display list from fragments and source runs</name>
  <read_first>
    - crates/flow-core/src/layout/mod.rs
    - crates/flow-core/src/model/mod.rs
    - crates/flow-core/src/pdf/mod.rs
  </read_first>
  <files>crates/flow-core/src/pdf/display_list.rs, crates/flow-core/src/pdf/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/pdf_text.rs</files>
  <behavior>
    - Each emitted text/image/page item carries source node/range, page index, fixed-point rect/placement, font identity, and the accepted revision/hash/result fingerprint.
    - Fragment traversal is stable, derived repeated bands are marked, and unsupported or missing source mappings produce bounded diagnostics rather than guessed text.
    - Display-list construction refuses stale or cross-catalog inputs before producing entries.
  </behavior>
  <action>Implement the adapter that resolves supported paragraph/heading/table-cell/header/footer fragments to canonical inline text and Phase 3 shaped glyph runs. Preserve UTF-8/UTF-16 cluster ranges and direction; keep all display-list structures derived and non-serializable as canonical document state.</action>
  <acceptance_criteria>
    - Fixtures prove page order, source mapping, repeated header/footer provenance, and stale revision/hash rejection.
    - No browser measurement, CSS line breaking, or text re-shaping occurs in the adapter.
  </acceptance_criteria>
  <verify><automated>cargo test -p flow-core --test pdf_text</automated></verify>
  <done>A verified display list is available to both PDF and preview adapters.</done>
</task>

<task id="04-02-02" type="auto" tdd="true">
  <name>04-02-02: Emit embedded font resources and Unicode mappings</name>
  <read_first>
    - crates/flow-core/src/layout/mod.rs
    - crates/flow-core/src/provenance/mod.rs
    - crates/flow-core/src/pdf/display_list.rs
  </read_first>
  <files>crates/flow-core/src/pdf/font.rs, crates/flow-core/src/pdf/mod.rs, crates/flow-core/tests/pdf_text.rs, fixtures/layout/phase4-text-corpus.json</files>
  <behavior>
    - Every emitted glyph has an admitted font resource, deterministic width/placement, and a valid source Unicode mapping or an explicit unsupported-glyph diagnostic.
    - Font bytes, subset/resource identity, license/provenance, and engine inputs are included in the export fingerprint; host fonts are never consulted.
    - PDF text streams use deterministic resource names and canonical hex/text encoding with no authored-text leakage in diagnostics.
  </behavior>
  <action>Add the admitted-font PDF resource writer and ToUnicode CMap generation. Use the checked-in Noto Sans fixture first, with a bounded subset strategy and fail-closed handling for unsupported glyphs, malformed font tables, duplicate faces, bidi clusters, and non-BMP mappings. Keep the existing provenance verifier as the source of truth for fixture identity.</action>
  <acceptance_criteria>
    - Tests cover Ukrainian/English logical extraction mappings, combining clusters, RTL control samples, missing glyphs, deterministic resource names, and font bytes not duplicated into diagnostics.
    - Full flow-core tests continue to pass with the new PDF module.
  </acceptance_criteria>
  <verify><automated>cargo test -p flow-core --test pdf_text && cargo test -p flow-core</automated></verify>
  <done>Owned PDF text is selectable/searchable through explicit embedded font resources for the admitted matrix.</done>
</task>
</tasks>

<artifacts>Revision-bound display list, embedded font resources, ToUnicode maps, and text corpus.</artifacts>
<verification>Run focused and full Rust suites plus provenance checks.</verification>
<success_criteria>Export text uses the exact Rust source/glyph mapping and never silently substitutes an unknown font or glyph.</success_criteria>
<output>Create `04-02-SUMMARY.md` after execution.</output>
