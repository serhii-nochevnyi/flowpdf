---
phase: FLOWPDF-04-owned-pdf-preview-and-export
plan: "05"
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - crates/flow-wasm/src/lib.rs
  - crates/flow-core/src/pdf/mod.rs
  - web/src/pdf/pdf-protocol.ts
  - web/src/pdf/pdf-worker.ts
  - web/src/pdf/pdf-preview.tsx
  - web/src/pdf/pdf-preview.css
  - web/src/editor/editor-app.tsx
  - web/src/editor/editor-store.ts
  - web/tests/pdf-preview.browser.test.ts
  - web/tests/pdf-worker.test.ts
autonomous: true
requirements: [PDFX-01, PDFX-02, PDFX-03, PDFX-06]
estimate: { tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "The browser can request export from one accepted revision and publish bytes/result only when request, source, layout, and manifest identities still match."
    - "Preview is virtualized, zoomable, navigable, searchable, and selection-aware while semantic DOM remains the only authoring/accessibility truth."
    - "A newer edit keeps input active and discards stale export/preview results without mutating the editor."
  artifacts:
    - "String-only WASM export/recovery boundary and revision-safe PDF worker protocol."
    - "Accessible PDF preview adapter with zoom/page navigation/search/selection and browser evidence."
  key_links:
    - "Rust export result -> worker identity guards -> atomic editor store -> PDF preview and download state."
---

<objective>Expose the owned export and preview through the browser without creating a second semantic editor.</objective>

<context>
@.planning/phases/FLOWPDF-04-owned-pdf-preview-and-export/04-CONTEXT.md
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-06-SUMMARY.md
@web/src/layout/layout-worker.ts
@web/src/layout/page-viewport.tsx
@web/src/editor/editor-app.tsx
@crates/flow-wasm/src/lib.rs
</context>

<tasks>
<task id="04-05-01" type="auto" tdd="true">
  <name>04-05-01: Add revision-safe WASM/worker export and recovery protocol</name>
  <read_first>
    - crates/flow-wasm/src/lib.rs
    - web/src/layout/layout-protocol.ts
    - web/src/layout/layout-worker.ts
    - web/src/editor/editor-controller.ts
  </read_first>
  <files>crates/flow-wasm/src/lib.rs, crates/flow-core/src/pdf/mod.rs, web/src/pdf/pdf-protocol.ts, web/src/pdf/pdf-worker.ts, web/tests/pdf-worker.test.ts</files>
  <behavior>
    - Requests are bounded and string-only at the WASM boundary; the worker publishes only complete bytes/results matching request ID, source revision/hash, layout result hash, and export options.
    - Abort/supersede and recovery failures are explicit, privacy-safe diagnostics; raw authored text is not logged by the worker.
    - Export bytes are returned as an immutable result for a caller-owned download, never stored as canonical document state.
  </behavior>
  <action>Add closed PDF request/result DTOs and a single-flight worker using the Phase 3 scheduler pattern. Keep binary transport explicit and bounded at the final adapter, with the Rust side owning all PDF construction and recovery validation.</action>
  <acceptance_criteria>
    - Unit tests cover stale response, mismatched hashes, abort, malformed response, size limit, and exact recovery classification.
  </acceptance_criteria>
  <verify><automated>npm exec -- vitest run --project unit web/tests/pdf-worker.test.ts && cargo test -p flow-wasm</automated></verify>
  <done>Browser export/recovery is revision-safe and cannot publish stale PDF bytes.</done>
</task>

<task id="04-05-02" type="auto" tdd="true">
  <name>04-05-02: Build the virtualized accessible PDF preview adapter</name>
  <read_first>
    - web/src/layout/page-viewport.tsx
    - web/src/layout/page-viewport.css
    - web/src/editor/semantic-document.tsx
    - web/src/editor/editor-store.ts
  </read_first>
  <files>web/src/pdf/pdf-preview.tsx, web/src/pdf/pdf-preview.css, web/src/editor/editor-app.tsx, web/src/editor/editor-store.ts, web/tests/pdf-preview.browser.test.ts</files>
  <behavior>
    - Preview supports bounded zoom, page navigation/virtualization, source-backed search, and selection projection without duplicating semantic authored content into an accessible canvas tree.
    - Preview status, export progress, errors, and unavailable reference validation are visible and announced through the existing semantic status/alert pattern.
    - Editing and focus remain available while preview/export work is pending; new revisions invalidate old preview state.
  </behavior>
  <action>Extend the page viewport/display-list adapter with a PDF preview mode. Reuse Rust fixed-point rectangles and source ranges, keep visual layers `aria-hidden`, map search/selection back to semantic DOM anchors, and add browser fixtures for zoom, virtualization, stale export, keyboard focus, and accessible status.</action>
  <acceptance_criteria>
    - Browser tests prove semantic content remains present once, preview pages are not the input host, search selects a source range, and stale results do not replace the active revision.
    - No CSS line-breaking or browser text measurement enters export or preview geometry.
  </acceptance_criteria>
  <verify><automated>npm run build:web && npm exec -- vitest run --project browser web/tests/pdf-preview.browser.test.ts web/tests/editor-accessibility.browser.test.ts</automated></verify>
  <done>PDFX-01 and PDFX-06 browser behavior is covered by an accessible revision-bound preview.</done>
</task>
</tasks>

<artifacts>WASM/worker export protocol, virtualized preview, zoom/search/selection, and browser tests.</artifacts>
<verification>Run focused worker/browser tests and the existing Phase 3 gate.</verification>
<success_criteria>Preview/export follows accepted Rust identities and never becomes a stale or inaccessible second editor.</success_criteria>
<output>Create `04-05-SUMMARY.md` after execution.</output>
