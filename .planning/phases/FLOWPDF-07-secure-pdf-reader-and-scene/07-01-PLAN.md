---
phase: FLOWPDF-07-secure-pdf-reader-and-scene
plan: "01"
title: "Build the lazy bounded PDF syntax reader"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/flow-core/src/pdf/reader.rs
  - crates/flow-core/src/pdf/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/pdf_reader.rs
requirements: [PDFI-01, PDFI-05, QUAL-05, QUAL-06]
delivery:
  ticket: T-07-01
  risk: high
  human_checkpoint: false
estimate: { tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Opening a supported PDF indexes bounded classic-xref offsets without parsing every indirect object eagerly."
    - "Malformed, encrypted, unsupported-xref, oversized, cyclic, and over-budget input returns stable bounded diagnostics."
    - "Active PDF actions and external resources are reported as data and never executed or followed."
  artifacts:
    - "Request-local PdfReader with bounded limits, lazy object access, and stable reader errors."
    - "Closed read report/diagnostic DTOs and focused hostile-input fixtures."
  key_links:
    - "PDF bytes -> header/xref index -> on-demand COS object -> bounded error/report"
---

<objective>Establish the secure syntax boundary that later scene plans can trust.</objective>

<context>
@.planning/phases/FLOWPDF-07-secure-pdf-reader-and-scene/07-CONTEXT.md
@.planning/phases/FLOWPDF-07-secure-pdf-reader-and-scene/07-RESEARCH.md
@crates/flow-core/src/pdf/mod.rs
</context>

<tasks>
  <task id="07-01-01" type="auto" tdd="true">
    <name>07-01-01: Add lazy classic-xref indexing and a bounded COS reader</name>
    <files>crates/flow-core/src/pdf/reader.rs, crates/flow-core/src/pdf/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/pdf_reader.rs</files>
    <behavior>
      - Valid PDF 1.4–1.7 headers and classic xref tables produce an index without eagerly parsing all objects.
      - Direct/indirect null, bool, integer, real, names, strings, arrays, dictionaries, and bounded streams parse with checked offsets.
      - Identity, Flate, DCT, and unsupported filters are classified explicitly; encryption, object streams, xref streams, and malformed offsets fail closed or report as declared.
    </behavior>
    <action>Implement a private byte cursor/COS representation, classic xref scanner, request-local PdfReader, default limits, and stable diagnostics. Make all allocations and recursion budgeted; keep authored string data out of diagnostics.</action>
    <acceptance_criteria>
      - A test can prove open does not parse a malformed unreachable object until that object is requested.
      - Every input length/offset/stream-length path is checked and no parser path panics on arbitrary bytes.
      - Active keys are detected without invoking URLs, scripts, launches, files, or external resource loaders.
    </acceptance_criteria>
    <verify><automated>cargo test --locked -p flow-core --test pdf_reader -- --nocapture</automated></verify>
    <done>The reader can safely index and lazily access the declared PDF syntax subset.</done>
  </task>
  <task id="07-01-02" type="auto" tdd="true">
    <name>07-01-02: Add page-tree discovery and bounded read requests</name>
    <files>crates/flow-core/src/pdf/reader.rs, crates/flow-core/tests/pdf_reader.rs</files>
    <behavior>
      - Catalog/page-tree traversal returns page references and media boxes under page/depth budgets.
      - Page reads are selected by first-page/page-count request and do not eagerly materialize unrelated page streams.
      - Missing roots, cycles, bad page counts, and budget exhaustion use stable errors.
    </behavior>
    <action>Expose document summary/page selection methods over the private reader. Preserve object numbers and source offsets for downstream scene provenance; leave content interpretation to Plan 07-02.</action>
    <acceptance_criteria>
      - Tests cover multi-page lazy selection, inherited page boxes/resources, page-tree cycles, and page-count limits.
      - Summary output includes source byte hash, declared page count, and report state without PDF bytes.
    </acceptance_criteria>
    <verify><automated>cargo test --locked -p flow-core --test pdf_reader -- --nocapture</automated></verify>
    <done>Scene code has a bounded, provenance-preserving page iterator.</done>
  </task>
</tasks>

<verification>Run the focused reader test, flow-core tests, formatting, and diff checks.</verification>
<success_criteria>A hostile byte buffer cannot make the reader hang, allocate unbounded data, execute an action, or hide a structural/unsupported condition.</success_criteria>
<output>Create `07-01-SUMMARY.md` after execution.</output>

