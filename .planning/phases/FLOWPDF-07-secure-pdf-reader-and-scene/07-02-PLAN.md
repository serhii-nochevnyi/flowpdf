---
phase: FLOWPDF-07-secure-pdf-reader-and-scene
plan: "02"
title: "Project supported pages into a provenance-rich PDF scene"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - crates/flow-core/src/pdf/scene.rs
  - crates/flow-core/src/pdf/reader.rs
  - crates/flow-core/src/pdf/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/pdf_scene.rs
requirements: [PDFI-02, PDFI-03, PDFI-05, QUAL-05, QUAL-06]
delivery:
  ticket: T-07-02
  risk: high
  human_checkpoint: false
estimate: { tokens: 34000, raw_tokens: 34000, tasks: 3, confidence: medium }
must_haves:
  truths:
    - "Supported text, JPEG images, paths, transforms, clipping, local form XObjects, links, annotations, and widgets appear as typed scene elements."
    - "Mapped text carries glyph geometry and source object/operator provenance; missing mappings and uncertain order are explicit diagnostics."
    - "Unknown operators, filters, fonts, malformed annotations, and active content never disappear silently."
  artifacts:
    - "Serializable PdfScene/PdfScenePage/PdfSceneElement DTOs with fixed-point geometry and provenance."
    - "Content-stream interpreter, bounded font mapping, image admission, and focused scene fixtures."
  key_links:
    - "lazy page/resources -> content operators -> fixed-point scene element -> report diagnostic"
---

<objective>Turn the safe syntax reader into a read-only internal scene for the controlled subset.</objective>

<context>
@.planning/phases/FLOWPDF-07-secure-pdf-reader-and-scene/07-CONTEXT.md
@.planning/phases/FLOWPDF-07-secure-pdf-reader-and-scene/07-PATTERNS.md
@crates/flow-core/src/pdf/display_list.rs
@crates/flow-core/src/layout/mod.rs
</context>

<tasks>
  <task id="07-02-01" type="auto" tdd="true">
    <name>07-02-01: Define scene DTOs and fixed-point provenance records</name>
    <files>crates/flow-core/src/pdf/scene.rs, crates/flow-core/src/pdf/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/pdf_scene.rs</files>
    <behavior>
      - Scene pages retain page/object identity, media boxes, partial status, elements, and report references.
      - Text glyphs retain code, optional Unicode, fixed-point baseline/rect/advance, font identity, object number, byte offset, and operator.
      - Paths/clips/images/forms/links/annotations/widgets have closed typed payloads and bounded fields.
    </behavior>
    <action>Add serializable scene/report types and deterministic hashes. Keep raw bytes out of diagnostics and cap scene element/glyph/image payload counts.</action>
    <acceptance_criteria>
      - DTO serialization is camelCase/deny-unknown-fields compatible with existing WASM protocols.
      - Scene hashes are stable for the same bytes/limits/request and change when report or scene content changes.
    </acceptance_criteria>
    <verify><automated>cargo test --locked -p flow-core --test pdf_scene -- --nocapture</automated></verify>
    <done>Browser/WASM code has a closed read-only scene contract.</done>
  </task>
  <task id="07-02-02" type="auto" tdd="true">
    <name>07-02-02: Interpret text, paths, clips, transforms, forms, and images</name>
    <files>crates/flow-core/src/pdf/scene.rs, crates/flow-core/src/pdf/reader.rs, crates/flow-core/tests/pdf_scene.rs</files>
    <behavior>
      - Bounded operator tokenization handles graphics state, path construction/stroking/filling, clipping, text matrices, simple text, `ToUnicode`, form recursion, and admitted JPEG image XObjects.
      - Coordinates are transformed and quantized through checked LayoutUnit conversion; operator/object offsets are retained.
      - Unknown/unsupported operators and resources produce report entries while safe partial scene items remain marked.
    </behavior>
    <action>Implement a stack-bounded content interpreter and resource resolver over the Plan 07-01 reader. Support direct and indirect resources, inherited page resources, standard ASCII/simple mappings, bounded ToUnicode BFChar/BFRange, identity/Flate stream admission as configured, and JPEG metadata/data limits. Never evaluate strings as code.</action>
    <acceptance_criteria>
      - Fixtures assert mapped Ukrainian/ASCII samples where ToUnicode exists, glyph rectangles, path geometry, CTM effects, clip records, image dimensions/data, and form recursion depth.
      - Tests assert no authored content is inserted into diagnostic codes and unsupported content is reported.
    </acceptance_criteria>
    <verify><automated>cargo test --locked -p flow-core --test pdf_scene -- --nocapture</automated></verify>
    <done>A selected page produces a bounded scene that downstream renderers can inspect without reparsing PDF syntax.</done>
  </task>
  <task id="07-02-03" type="auto" tdd="true">
    <name>07-02-03: Inspect links, annotations, widgets, and active-content fences</name>
    <files>crates/flow-core/src/pdf/scene.rs, crates/flow-core/tests/pdf_scene.rs</files>
    <behavior>
      - Internal page destinations and widget geometry are represented as inspection elements.
      - URI, GoToR, Launch, JavaScript, OpenAction, additional actions, file specs, embedded files, and external resources are report-only blocked entries.
      - Unsupported annotation subtypes remain visible as opaque annotation metadata with a warning.
    </behavior>
    <action>Add recursive key/action classification over catalog, page, annotation, resource, and form dictionaries. Do not expose executable callbacks or clickable external URLs.</action>
    <acceptance_criteria>
      - Security fixtures prove active action bytes are never called, external resources are never loaded, and every forbidden key is reported.
      - Internal links and form/widget records retain source object provenance and bounded field/value summaries.
    </acceptance_criteria>
    <verify><automated>cargo test --locked -p flow-core --test pdf_scene -- --nocapture</automated></verify>
    <done>Phase 7 scene inspection is explicit about both supported interactive records and blocked active content.</done>
  </task>
</tasks>

<verification>Run focused scene/reader tests, complete flow-core tests, formatting, and diff checks.</verification>
<success_criteria>The scene contains all admitted visual/inspection content with fixed geometry and provenance, while unsupported and active content remains visible in the report.</success_criteria>
<output>Create `07-02-SUMMARY.md` after execution.</output>

