---
phase: FLOWPDF-07-secure-pdf-reader-and-scene
plan: "03"
title: "Expose the reader through WASM and a report-first browser panel"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - crates/flow-wasm/src/lib.rs
  - crates/flow-wasm/tests/pdf_reader.rs
  - web/src/pdf/pdf-protocol.ts
  - web/src/pdf/pdf-reader-worker.ts
  - web/src/pdf/pdf-reader-worker-entry.ts
  - web/src/pdf/pdf-reader-panel.tsx
  - web/src/pdf/pdf-reader-panel.css
  - web/src/editor/editor-app.tsx
  - web/src/layout/layout-runtime.ts
  - web/tests/pdf-reader.test.ts
  - web/tests/pdf-reader.browser.test.ts
requirements: [PDFI-01, PDFI-02, PDFI-05, QUAL-05, QUAL-06]
delivery:
  ticket: T-07-03
  risk: high
  human_checkpoint: false
estimate: { tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: medium }
must_haves:
  truths:
    - "WASM accepts bounded PDF bytes and returns only a revision/request-bound scene or stable error; it never executes actions or returns a mutable reader handle."
    - "Worker cancellation/stale results cannot replace a newer reader scene."
    - "The browser can choose a PDF, inspect pages and warnings, and render read-only supported text/paths/JPEGs/forms/links without mutating the semantic editor."
  artifacts:
    - "Closed reader protocol and Rust/WASM adapter with hex/scene-size limits."
    - "Worker scheduler/entry and accessible reader panel with report/status surface."
    - "Unit and Chromium evidence for cancellation, report visibility, and read-only rendering."
  key_links:
    - "file input -> reader worker -> validated scene/report -> visual projection + semantic status"
---

<objective>Make the bounded reader usable through the existing worker architecture without creating a second editor model.</objective>

<context>
@.planning/phases/FLOWPDF-07-secure-pdf-reader-and-scene/07-CONTEXT.md
@.planning/phases/FLOWPDF-07-secure-pdf-reader-and-scene/07-PATTERNS.md
@web/src/pdf/pdf-worker.ts
@web/src/editor/editor-app.tsx
</context>

<tasks>
  <task id="07-03-01" type="auto" tdd="true">
    <name>07-03-01: Add the bounded Rust/WASM reader protocol</name>
    <files>crates/flow-wasm/src/lib.rs, crates/flow-wasm/tests/pdf_reader.rs, web/src/pdf/pdf-protocol.ts</files>
    <behavior>
      - Requests validate protocol/version/request ID/hex and page-window limits before Rust parsing.
      - Results carry source hash, page summary/scene, report, result hash, and no executable or mutable state.
      - Malformed/oversized/active-content fixtures return stable error codes and no partial accepted result.
    </behavior>
    <action>Serialize the core reader request/result through the existing string-only WASM style, verify scene hashes and identity, and add TypeScript DTO guards. Keep binary bytes at the final hex ingress only.</action>
    <acceptance_criteria>
      - Rust and WASM tests cover byte limits, page-window limits, response hash verification, and action-free processing.
      - Unknown JSON fields, invalid hex, and malformed scene responses are rejected fail-closed.
    </acceptance_criteria>
    <verify><automated>cargo test --locked -p flow-wasm --test pdf_reader -- --nocapture && npm run typecheck</automated></verify>
    <done>The reader has a verified browser boundary.</done>
  </task>
  <task id="07-03-02" type="auto" tdd="true">
    <name>07-03-02: Add a cancellable reader worker and runtime bridge</name>
    <files>web/src/pdf/pdf-reader-worker.ts, web/src/pdf/pdf-reader-worker-entry.ts, web/src/layout/layout-runtime.ts, web/src/pdf/pdf-protocol.ts, web/tests/pdf-reader.test.ts</files>
    <behavior>
      - A newer request cancels/supersedes an older read; only identity-matching scenes publish.
      - Worker startup, message, decode, and scene-hash failures become payload-free diagnostics.
      - Runtime disposal cancels and terminates the reader worker independently from export.
    </behavior>
    <action>Implement the reader engine/scheduler using the existing PDF worker patterns and instantiate it in the default runtime with the generated WASM reader exports.</action>
    <acceptance_criteria>
      - Unit tests cover accepted, cancelled, stale, malformed, and worker-failure paths.
      - Existing export scheduler behavior remains unchanged and all current PDF worker tests pass.
    </acceptance_criteria>
    <verify><automated>npm run typecheck && PLAYWRIGHT_BROWSERS_PATH=./work/playwright npx --no-install vitest run --project unit web/tests/pdf-reader.test.ts</automated></verify>
    <done>The app owns a safe background reader path.</done>
  </task>
  <task id="07-03-03" type="auto" tdd="true">
    <name>07-03-03: Render the read-only scene and report in an accessible panel</name>
    <files>web/src/pdf/pdf-reader-panel.tsx, web/src/pdf/pdf-reader-panel.css, web/src/editor/editor-app.tsx, web/tests/pdf-reader.browser.test.ts</files>
    <behavior>
      - Labeled file input and status/report list expose load, partial, unsupported, and blocked-action states.
      - Text/path/image/form/annotation scene projection is visual-only/aria-hidden; semantic status and report remain accessible DOM.
      - The panel never dispatches editor commands or exposes external/action links.
    </behavior>
    <action>Add the panel and browser-facing scene projection with page navigation/zoom bounded to the accepted scene window. Wire it only as an optional reader dependency so existing editor startup/tests remain deterministic.</action>
    <acceptance_criteria>
      - Chromium tests load a deterministic fixture through a fake reader scheduler, assert scene/report rendering, and prove editor semantic content is unchanged.
      - Visual output includes provenance hooks for page/object/operator inspection without echoing active URLs or script payloads.
    </acceptance_criteria>
    <verify><automated>npm run build:web && PLAYWRIGHT_BROWSERS_PATH=./work/playwright npx --no-install vitest run --project browser web/tests/pdf-reader.browser.test.ts</automated></verify>
    <done>A user can inspect a supported PDF subset and see every unsupported/blocked condition without editing or executing it.</done>
  </task>
</tasks>

<verification>Run WASM/TypeScript focused tests, the browser reader test, and full web build.</verification>
<success_criteria>The reader is usable, cancellable, revision-safe, accessible at its status/report surface, and visually read-only.</success_criteria>
<output>Create `07-03-SUMMARY.md` after execution.</output>

