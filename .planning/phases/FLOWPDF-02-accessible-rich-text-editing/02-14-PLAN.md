---
phase: FLOWPDF-02-accessible-rich-text-editing
plan: "14"
type: execute
wave: 14
depends_on: ["02-13"]
files_modified:
  - crates/flow-wasm/src/lib.rs
  - web/persistence/indexeddb-store.ts
  - web/tests/indexeddb-store.test.ts
  - crates/flow-core/tests/recovery.rs
  - web/src/editor/embedded-blocks.tsx
  - web/src/editor/editor-messages.ts
  - web/src/editor/editor.css
  - web/tests/image-lifecycle.browser.test.ts
autonomous: true
requirements: [EDIT-05, QUAL-03, QUAL-04]
estimate: { tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Uint8Array bytes cross only stage_asset; semantic lifecycle commands contain receipt/reference/metadata/accessibility but never bytes."
    - "IndexedDB atomically commits document, transaction, audit, added/reachable asset records, removals proven safe, and head; any fault leaves the prior complete state."
    - "Visible keyboard-accessible controls provide Замінити/Replace, Змінити опис/Change description, and Видалити/Delete with localized confirmation and focus semantics."
    - "Insert, replace, accessibility change, remove, undo/redo, reload, and recovery use real Rust/WASM/IndexedDB and never leak object URLs or raw bytes."
  artifacts:
    - "crates/flow-wasm/src/lib.rs exposes dedicated bounded staging and immutable lifecycle results."
    - "web/persistence/indexeddb-store.ts persists binary envelopes and reachability changes atomically."
    - "web/src/editor/embedded-blocks.tsx provides insertion and complete selected-image action flow."
    - "web/tests/image-lifecycle.browser.test.ts proves complete accessible durable lifecycle."
  key_links:
    - "Uint8Array -> stage_asset -> receipt-only command -> Rust persistence plan -> guarded IndexedDB transaction."
    - "Image action bar/confirmation -> same controller command -> accepted semantic figure -> focus/recovery."
---

<objective>
Carry the complete image lifecycle through WASM, atomic IndexedDB persistence, and accessible browser controls.

Purpose: Turn the Rust D-14 contract into a real durable product flow without byte or authority leakage.
Output: Narrow binary ingress, fault-safe storage, image dialogs/action bar/confirmation, and real Chromium lifecycle tests.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-UI-SPEC.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-13-SUMMARY.md
</context>

<tasks>
<task id="02-14-01" type="auto" tdd="true">
  <name>02-14-01: Persist staged image lifecycle atomically across WASM and IndexedDB</name>
  <read_first>
    - crates/flow-wasm/src/lib.rs
    - web/persistence/indexeddb-store.ts
    - web/tests/indexeddb-store.test.ts
    - crates/flow-core/tests/recovery.rs
  </read_first>
  <files>crates/flow-wasm/src/lib.rs, web/persistence/indexeddb-store.ts, web/tests/indexeddb-store.test.ts, crates/flow-core/tests/recovery.rs</files>
  <behavior>
    - stage_asset(metadata_json, Uint8Array) is the sole binary ingress and returns one immutable receipt result.
    - New records use a versioned binary envelope; legacy readonly-number[] records remain readable and reverify the same canonical blake3: hash.
    - One guarded transaction commits semantic records, added assets, safe removals, and head; each injected failure produces all-or-none state.
  </behavior>
  <action>Extend the immutable wasm-bindgen boundary with only the dedicated bounded staging call and byte-free semantic image commands. Version the physical asset envelope while preserving legacy reads. Apply Rust-planned additions/removals in the existing guarded snapshot+transaction+audit+assets+head transaction, resolve only on oncomplete, abort on identity/hash/reachability mismatch, then Rust-query recovery before publication. Fault-inject every write/error/abort/quota step plus shared-reference and exact-retry cases.</action>
  <acceptance_criteria>
    - Boundary tests prove no raw bytes or mutable asset/document handles enter semantic commands.
    - Every fault yields either the entire accepted plan or the exact previous state; shared assets never disappear.
    - Legacy/new records reopen with the same canonical BLAKE3 identity.</acceptance_criteria>
  <verify><automated>npx --no-install vitest run web/tests/indexeddb-store.test.ts &amp;&amp; cargo test -p flow-core --test recovery -- image_asset --nocapture</automated></verify>
  <done>Image lifecycle persistence is binary-efficient, legacy-compatible, shared-safe, and crash-atomic.</done>
</task>

<task id="02-14-02" type="auto" tdd="true">
  <name>02-14-02: Deliver accessible insert, replace, description, and delete image controls</name>
  <read_first>
    - web/src/editor/editor-controller.ts
    - web/src/editor/semantic-document.tsx
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-UI-SPEC.md (ImageInsertDialog, ImageBlock, DestructiveConfirmDialog)
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-RESEARCH.md (resolved Q4)
  </read_first>
  <files>web/src/editor/embedded-blocks.tsx, web/src/editor/editor-messages.ts, web/src/editor/editor.css, web/tests/image-lifecycle.browser.test.ts</files>
  <behavior>
    - Insert/replace requires valid staging plus nonempty description or explicit decorative choice; MissingLegacy remains reachable for review.
    - Selected ImageBlock exposes visible labeled Replace, Change description, and Delete controls with equivalent keyboard paths.
    - Delete opens the specified native confirmation with safe initial focus; cancel/Escape preserves state and all exits restore editor selection/focus.
    - Object URLs are revoked on replacement/removal/cancel/reject/unmount and never enter accepted snapshots, diagnostics, or audit.
  </behavior>
  <action>Per D-09/D-10/D-11/D-12/D-13/D-14/D-16, implement the native file/accessibility dialog, bounded preview, figure/img/figcaption projection, selected-image action bar, accessibility-edit dialog, and whole-image destructive confirmation. Route each visible/keyboard action to the same Rust command; do not trust browser MIME/preview or compute reachability/capabilities in React. Test described/decorative/MissingLegacy resolution, replace with shared asset, description change, cancel/confirm delete, stale/replay/substitution/quota failure, focus/status/alert, undo/redo/reload/recovery, locales, and required viewports.</action>
  <acceptance_criteria>
    - Complete D-14 controls and confirmation are labeled, keyboard accessible, focus-correct, and modality-equivalent.
    - Real browser lifecycle proves exact accepted state and unchanged prior state for every failure.
    - Object URLs/raw bytes/receipt secrets are absent from accepted/public diagnostics.</acceptance_criteria>
  <verify><automated>npx --no-install vitest run --project browser web/tests/image-lifecycle.browser.test.ts</automated></verify>
  <done>Users can insert, replace, describe/decorate, and remove images safely through the real durable architecture.</done>
</task>
</tasks>

## Artifacts this phase produces

- Narrow `stage_asset` WASM binary ingress and immutable lifecycle responses.
- Versioned binary IndexedDB assets with atomic add/remove/reachability handling and fault tests.
- Accessible image insertion/action/description/confirmation UI, localized messages/styles, and full lifecycle browser suite.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser Uint8Array/metadata -> WASM | Bytes and claims are hostile and bounded. |
| Rust persistence plan -> IndexedDB | Partial writes or unsafe removal must never publish. |
| Asset/alt state -> DOM/object URL | Content and temporary URLs can leak or execute if mishandled. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-14-01 | Tampering | binary/semantic boundary | high | mitigate | Dedicated stage_asset only; deny byte-bearing semantic DTOs. |
| T-02-14-02 | Tampering | partial asset commit/removal | high | mitigate | One guarded transaction, reachability proof, CAS, abort-on-error, postcommit Rust recovery. |
| T-02-14-03 | Information Disclosure | object URLs/bytes/alt/receipt | high | mitigate | Central URL lifecycle and safe-code diagnostics; boundary/browser canaries. |
| T-02-14-04 | Elevation of Privilege | UI capability/confirmation bypass | high | mitigate | Render Rust capabilities/risk and use shared guarded controller. |
| T-02-14-05 | Cross-site Scripting | image text projection | high | mitigate | Native figure/img/figcaption with text/closed attributes only. |
| T-02-14-SC | Tampering | dependencies | high | mitigate | Consume exact Plan 02-01 locks only. |
</threat_model>

<verification>Run the exact persistence/recovery command and complete Chromium lifecycle command.</verification>
<success_criteria>D-14 image insertion and lifecycle are complete, atomic, accessible, shared-safe, and recoverable.</success_criteria>
<output>Create `.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-14-SUMMARY.md` when done.</output>
