---
phase: FLOWPDF-02-accessible-rich-text-editing
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - fixtures/unicode/17.0.0/GraphemeBreakTest.txt
  - fixtures/unicode/17.0.0/provenance.json
  - scripts/verify-unicode-corpus.mjs
  - scripts/verify-unicode-corpus.test.mjs
  - scripts/verify-wasm-size.mjs
  - artifacts/benchmarks/phase2-wasm-size.json
  - tests/contracts/phase1-boundary.test.mjs
autonomous: true
requirements: [EDIT-01, QUAL-03, QUAL-04]
estimate: { tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Official Unicode 17 GraphemeBreakTest bytes, URL, byte count, SHA-256, ICU crate/data tags, and provenance fail closed on drift."
    - "One canonical `scripts/verify-wasm-size.mjs` builds identical baseline/candidate release configurations and enforces <=512 KiB raw and <=160 KiB deterministic-gzip delta."
    - "Phase-aware boundary checks admit approved editor dependencies while retaining Rust ownership, unsafe-DOM, mutable-WASM, and deferred-scope prohibitions."
  artifacts:
    - "Unicode corpus/provenance and dedicated verifier/tests."
    - "verify-wasm-size.mjs and numerical benchmark artifact."
    - "Phase-aware retained Phase 1 boundary test."
  key_links:
    - "Unicode provenance -> corpus verifier -> later Rust conformance suite."
    - "Identical release builds -> raw/gzip artifact -> fail-closed dependency payload decision."
---

<objective>
Pin Unicode conformance data, measure the ICU release-WASM delta, and retain Phase 1 architectural boundaries.

Purpose: Establish deterministic Unicode authority and numerical payload evidence before schema/editor work.
Output: Verified corpus/provenance, one canonical WASM-size gate, benchmark artifact, and phase-aware boundary test.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-RESEARCH.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-PATTERNS.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-01-SUMMARY.md
</context>

<tasks>
<task id="02-02-01" type="auto" tdd="true">
  <name>02-02-01: Vendor and verify the exact Unicode 17 grapheme corpus</name>
  <read_first>
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-RESEARCH.md (Pattern 3)
    - artifacts/provenance/phase2-dependencies.json
  </read_first>
  <files>fixtures/unicode/17.0.0/GraphemeBreakTest.txt, fixtures/unicode/17.0.0/provenance.json, scripts/verify-unicode-corpus.mjs, scripts/verify-unicode-corpus.test.mjs</files>
  <behavior>
    - Exact official URL, 126570 bytes, SHA-256 e2d134d2c52919bace503ebb6a551c1855fe1a1faec18478c78fff254a1793ec, Unicode 17, and ICU data tags are independently checked.
    - Altered bytes, metadata, hash, size, version, or source fail with safe diagnostics.
  </behavior>
  <action>Check in the exact official Unicode 17 corpus and adjacent machine-readable provenance. Implement one consistent verifier and adversarial Node tests; do not hand-author a replacement corpus or fetch mutable data during normal verification.</action>
  <acceptance_criteria>
    - Repeated verification is deterministic and offline.
    - Every independent provenance/corpus drift fixture fails.</acceptance_criteria>
  <verify><automated>node --test scripts/verify-unicode-corpus.test.mjs &amp;&amp; node scripts/verify-unicode-corpus.mjs</automated></verify>
  <done>Unicode 17 grapheme data identity is exact and reproducible.</done>
</task>

<task id="02-02-02" type="auto" tdd="true">
  <name>02-02-02: Measure release-WASM growth and make Phase 1 boundaries editor-aware</name>
  <read_first>
    - scripts/build-web.mjs
    - tests/contracts/phase1-boundary.test.mjs
    - artifacts/provenance/phase2-dependencies.json
  </read_first>
  <files>scripts/verify-wasm-size.mjs, artifacts/benchmarks/phase2-wasm-size.json, tests/contracts/phase1-boundary.test.mjs</files>
  <behavior>
    - Baseline/candidate builds bind identical source/toolchain/features/bindgen/flags and deterministic gzip settings.
    - Forged totals/deltas/stale inputs or either over-budget delta fail.
    - Phase 1 negative fixtures still reject semantic TypeScript authority, unsafe DOM, mutable WASM, layout/PDF/voice/backend scope.
  </behavior>
  <action>Build isolated Phase 1-compatible baseline and ICU candidate outputs, record raw/gzip totals/deltas with source/toolchain identity, and reject above either budget. Make the Phase 1 boundary contract phase-aware through parsed manifest/AST/Rust fixtures, changing only the exact React/Vite/editor exclusion while retaining every semantic and deferred-scope rule.</action>
  <acceptance_criteria>
    - `scripts/verify-wasm-size.mjs` is the only size-script name used by plans/validation/final gate.
    - Adversarial boundary and size fixtures fail without weakening Phase 1.</acceptance_criteria>
  <verify><automated>node scripts/verify-wasm-size.mjs &amp;&amp; node --test tests/contracts/phase1-boundary.test.mjs &amp;&amp; npm run check</automated></verify>
  <done>The Unicode dependency payload is numerically accepted and Phase 1 boundaries remain intact.</done>
</task>
</tasks>

## Artifacts this phase produces

- Exact Unicode corpus/provenance/verifier/tests.
- Canonical `verify-wasm-size.mjs` and numerical artifact.
- Phase-aware retained Phase 1 boundary contract.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Official corpus/provenance -> repository | Bytes or metadata can be substituted. |
| Build outputs -> size evidence | Stale/different builds can fabricate acceptable delta. |
| New editor files -> Phase 1 boundary | Allowlisting can accidentally weaken ownership/scope rules. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-02-01 | Tampering | Unicode corpus/provenance | high | mitigate | Exact offline hash/size/source/data-tag verifier and adversarial tests. |
| T-02-02-02 | Spoofing | WASM size artifact | medium | mitigate | Source/toolchain/flags binding and recomputed raw/gzip values. |
| T-02-02-03 | Denial of Service | ICU payload | medium | mitigate | Automatic raw/gzip delta budgets with no waiver. |
| T-02-02-04 | Elevation of Privilege | boundary allowlist | high | mitigate | Narrow phase-aware AST/export rules plus retained negative fixtures. |
| T-02-02-SC | Tampering | dependencies | high | mitigate | Consume only Plan 02-01 accepted locks. |
</threat_model>

<verification>Run both exact automated commands; schema work starts only with corpus/size/boundary green.</verification>
<success_criteria>Unicode identity and payload are deterministic without weakening existing architecture.</success_criteria>
<output>Create `.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-02-SUMMARY.md` when done.</output>
