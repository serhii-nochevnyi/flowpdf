---
phase: FLOWPDF-02-accessible-rich-text-editing
plan: "08"
type: execute
wave: 8
depends_on: ["02-07"]
files_modified:
  - web/src/editor/input-adapter.ts
  - web/src/editor/semantic-document.tsx
  - web/tests/rich-text-structure.browser.test.ts
autonomous: true
requirements: [EDIT-02, EDIT-03, QUAL-03, QUAL-04]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Enter/Backspace/Delete translate to closed Rust structural commands only; TypeScript never computes eligibility or adopts DOM mutation."
    - "Native paragraph/heading/list/table-cell structure follows accepted Rust preorder and survives undo/redo/reload/recovery."
    - "Visible split/merge controls and structural keyboard paths yield equal commands/hash/revision/undo when enabled and equal stable rejection when disabled."
  artifacts:
    - "input-adapter structural intent translation and accepted semantic projection."
    - "Real Chromium structural keyboard/parity/durability suite."
  key_links:
    - "Native key/visible action -> same Rust structural command -> durable commit -> accepted semantic DOM/focus."
---

<objective>
Attach native structural keys and visible split/merge controls to the exact Rust algebra.

Purpose: Turn core EDIT-03 semantics into accessible durable product behavior without UI authority.
Output: Structural input translation, semantic projection integration, and real Chromium lifecycle/parity evidence.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-UI-SPEC.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-07-SUMMARY.md
</context>

<tasks>
<task id="02-08-01" type="auto" tdd="true">
  <name>02-08-01: Route Enter, Backspace, Delete, split, and merge through Rust</name>
  <read_first>
    - web/src/editor/input-adapter.ts
    - web/src/editor/semantic-document.tsx
    - crates/flow-core/src/editor_view/mod.rs
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-UI-SPEC.md (Structural transformations)
  </read_first>
  <files>web/src/editor/input-adapter.ts, web/src/editor/semantic-document.tsx, web/tests/rich-text-structure.browser.test.ts</files>
  <behavior>
    - Keys/actions dispatch only when accepted Rust capability permits; ordinary DOM edit is prevented/restored around one command.
    - Paragraph/heading/list/table-cell boundary behavior matches D-08 and focus returns to Rust-selected logical position.
    - Incompatible/atomic/stale cases retain DOM, selection, document, revision, and history exactly.
  </behavior>
  <action>Per D-02/D-08/D-11/D-12/D-13, map native structural intents and visible split/merge controls to the closed Rust commands. Prevent native mutation only when dispatching an accepted intent, keep one command in flight, publish no optimistic structure, and restore only the Rust-returned selection after IndexedDB completion/re-query. Render p/h1-h6/ol/ul/li/table-cell structure declaratively from accepted DTOs.</action>
  <acceptance_criteria>
    - Chromium covers complete key boundary matrix, visible route, rejection, and focus restoration.
    - DOM order equals Rust preorder and never becomes canonical input.</acceptance_criteria>
  <verify><automated>npx --no-install vitest run --project browser web/tests/rich-text-structure.browser.test.ts --grep "structural keys and controls"</automated></verify>
  <done>Native and visible structural intents use the exact Rust command/capability path.</done>
</task>

<task id="02-08-02" type="auto" tdd="true">
  <name>02-08-02: Prove durable structural parity, undo, reload, and recovery</name>
  <read_first>
    - web/tests/walking-skeleton.browser.test.ts
    - web/tests/rich-text-structure.browser.test.ts
    - web/src/editor/editor-controller.ts
  </read_first>
  <files>web/src/editor/input-adapter.ts, web/src/editor/semantic-document.tsx, web/tests/rich-text-structure.browser.test.ts</files>
  <behavior>
    - Visible and keyboard structural traces produce equal command sequence/hash/revision/history.
    - Empty/final-block, field deletion-review, delete/undo/redo/undo, cold reload, and interrupted recovery are exact.
    - Foundation Inspector remains reachable and unchanged Phase 1 behavior stays green.
  </behavior>
  <action>Extend the real-browser suite over paragraphs, headings, list items, table cells, compatible/incompatible neighbors, and anchored fields. Run equivalent visible/keyboard traces, save/reload/recover, and compare semantic DOM, canonical hashes, revisions, directional selection, field states, and undo history. Include Foundation Inspector reachability without making it primary.</action>
  <acceptance_criteria>
    - Durable lifecycle is exact for every supported structural family and unchanged for every rejection.
    - Visible/keyboard parity is non-vacuous and Phase 1 diagnostics remain intact.</acceptance_criteria>
  <verify><automated>npx --no-install vitest run --project browser web/tests/rich-text-structure.browser.test.ts --grep "durable structural parity"</automated></verify>
  <done>Structural editing is accessible, modality-equivalent, durable, and recoverable through the production path.</done>
</task>
</tasks>

## Artifacts this phase produces

- Structural intent translation and semantic projection integration.
- Real Chromium structural key/control/parity/undo/reload/recovery tests.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser key/action/DOM -> controller | Eligibility, selection, and mutation are untrusted. |
| Rust accepted tree -> DOM | Authored structure/text must stay projection only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-08-01 | Elevation of Privilege | UI structural eligibility | high | mitigate | Rust capabilities and revalidation; no TypeScript structure inference. |
| T-02-08-02 | Tampering | DOM/native edit | high | mitigate | Prevent/restore accepted projection and publish only durable Rust result. |
| T-02-08-03 | Information Disclosure | structural errors | medium | mitigate | Stable safe codes/reasons without authored text/payload. |
| T-02-08-SC | Tampering | dependencies | high | mitigate | Exact locked graph only. |
</threat_model>

<verification>Run both exact browser filters and Phase 1 boundary/Inspector checks.</verification>
<success_criteria>All structural product paths are Rust-authoritative, parity-complete, and durable.</success_criteria>
<output>Create `.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-08-SUMMARY.md` when done.</output>
