---
phase: FLOWPDF-02-accessible-rich-text-editing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase2-dependencies.mjs
  - scripts/verify-phase2-dependencies.test.mjs
  - config/dependency-provenance.json
  - artifacts/provenance/phase2-dependencies.json
  - package.json
  - package-lock.json
  - vite.config.ts
  - crates/flow-core/Cargo.toml
  - Cargo.lock
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05, QUAL-03, QUAL-04]
estimate: { tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Every Phase 2 dependency is an exact release accepted only from official identity, integrity, deprecation, lifecycle, legitimacy, and lock evidence; SUS/SLOP blocks without approval bypass."
    - "React/Vite, ICU4X, and PNG/JPEG decoder features are exactly pinned; lifecycle scripts stay disabled during lock materialization."
    - "Vite admits the editor shell while retaining the Foundation Inspector and all existing named Vitest projects."
  artifacts:
    - "scripts/verify-phase2-dependencies.mjs and its adversarial tests provide fail-closed selection evidence."
    - "artifacts/provenance/phase2-dependencies.json binds accepted exact candidates."
    - "Manifest/lock/Vite files materialize only the accepted graph."
  key_links:
    - "Official metadata -> accepted provenance report -> exact manifest/lock equality."
---

<objective>
Admit and lock the exact Phase 2 dependency/build graph before editor production work.

Purpose: Prevent unverified packages, lifecycle execution, or floating versions from entering the durable editor contract.
Output: Automated provenance gate, accepted report, exact lockfiles, and Vite entry configuration.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-RESEARCH.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-PATTERNS.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-01-SUMMARY.md
</context>

<tasks>
<task id="02-01-01" type="tracer" tdd="true">
  <name>02-01-01: Prove one dependency candidate from official metadata to an accepted exact pin</name>
  <read_first>
    - scripts/verify-dependency-provenance.mjs
    - scripts/verify-dependency-locks.mjs
    - config/dependency-provenance.json
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-RESEARCH.md (Package Legitimacy Audit)
  </read_first>
  <files>scripts/verify-phase2-dependencies.mjs, scripts/verify-phase2-dependencies.test.mjs, config/dependency-provenance.json, artifacts/provenance/phase2-dependencies.json</files>
  <behavior>
    - Exact registry version, publication time, numeric snapshot, official repository/tarball/integrity, deprecation, lifecycle, and legitimacy evidence bind one report.
    - Missing/contradictory/SUS/SLOP evidence fails before package-manager mutation with no override.
    - Repeated successful verification is deterministic except explicit observation timestamp.
  </behavior>
  <action>Extend the Phase 1 provenance verifier for React/react-dom, Vite/plugin/types, icu_segmenter, and image. Treat research versions only as candidates, require every official-source and lock-intent check, write a blocker on non-OK evidence, and never install inside the verifier. This implements agent discretion while preserving D-02/D-12 supply-chain constraints.</action>
  <acceptance_criteria>
    - Adversarial tests reject substituted repository/tarball/integrity, deprecation, lifecycle, floating version, stale lock, and non-OK legitimacy.
    - Successful report contains only exact accepted versions and deterministic evidence.</acceptance_criteria>
  <verify><automated>node --test scripts/verify-phase2-dependencies.test.mjs &amp;&amp; node scripts/verify-phase2-dependencies.mjs</automated></verify>
  <done>The selector produces an exact accepted report or blocks before installation.</done>
</task>

<task id="02-01-02" type="auto">
  <name>02-01-02: Lock accepted React/Vite, ICU4X, and PNG/JPEG dependencies without lifecycle execution</name>
  <read_first>
    - artifacts/provenance/phase2-dependencies.json
    - package.json
    - crates/flow-core/Cargo.toml
    - vitest.config.ts
  </read_first>
  <files>package.json, package-lock.json, vite.config.ts, crates/flow-core/Cargo.toml, Cargo.lock</files>
  <behavior>
    - Exact accepted npm and Cargo versions materialize in manifests and lockfiles with lifecycle scripts disabled.
    - The resolved ICU feature graph contains compiled_data without auto/LSTM, and the image feature graph contains only PNG/JPEG support.
    - Existing Phase 1 Vitest projects and the Foundation Inspector route remain configured after the Vite entry is added.
  </behavior>
  <action>Consume only Task 02-01-01 OK candidates. Add exact React/react-dom/Vite/plugin/type pins, exact icu_segmenter with default features disabled and compiled_data only, and exact image with png/jpeg only. Materialize locks with lifecycle scripts disabled, configure the existing web/WASM entry, and retain all named Vitest projects and the Foundation Inspector route.</action>
  <acceptance_criteria>
    - Direct versions equal the accepted report and locks contain official integrity/checksum data.
    - Cargo feature tree has no ICU auto/LSTM or image formats beyond PNG/JPEG.
    - Vite configuration does not remove Phase 1 diagnostics/tests.</acceptance_criteria>
  <verify><automated>npm ci --ignore-scripts &amp;&amp; node scripts/verify-dependency-locks.mjs &amp;&amp; npm ls --all &amp;&amp; RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo tree --locked -e features</automated></verify>
  <done>Only automatically accepted exact releases are locked and buildable.</done>
</task>
</tasks>

## Artifacts this phase produces

- Phase 2 dependency verifier/tests/config/report.
- Exact npm/Cargo manifests and locks plus Vite editor configuration.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Registry/repository metadata -> report | Metadata may be stale, substituted, or contradictory. |
| Accepted report -> manifests/locks | An unaccepted candidate must not become executable code. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01-SC | Tampering / Elevation of Privilege | npm/Cargo intake | high | mitigate | 02-01-01 verifies exact official identity/integrity/lifecycle/legitimacy before install. |
| T-02-01-02 | Tampering | manifests/locks | high | mitigate | 02-01-02 compares all direct versions/checksums to accepted report after ignore-scripts materialization. |
| T-02-01-03 | Denial of Service | dependency feature graph | medium | mitigate | Closed ICU/image feature assertions reject unused heavy features. |
</threat_model>

<verification>Run both exact automated commands; downstream work starts only with an OK report and exact locks.</verification>
<success_criteria>No unaccepted dependency is installed and the approved web/Rust build seam remains Phase 1 compatible.</success_criteria>
<output>Create `.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-01-SUMMARY.md` when done.</output>
