---
phase: FLOWPDF-02-accessible-rich-text-editing
plan: "04"
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - crates/flow-core/src/anchor/mod.rs
  - crates/flow-core/src/editor_view/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-wasm/src/lib.rs
  - crates/flow-core/tests/grapheme_conformance.rs
  - crates/flow-core/tests/editor_session.rs
autonomous: true
requirements: [EDIT-01, EDIT-04, QUAL-03, QUAL-04]
estimate: { tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "[EDGE EDIT-01 empty] Empty text block has exactly offset 0; nonzero endpoints reject without state change."
    - "[EDGE EDIT-01 encoding] Only UTF-16 offsets at pinned UAX #29 extended-grapheme boundaries are accepted; surrogate/combining/ZWJ/regional interiors reject without snapping in native/WASM."
    - "[EDGE EDIT-01 boundary] Text accepts 0..=len at grapheme edges; atomic image/table/page-break accepts only 0/Forward before and 1/Backward after with no interior caret."
    - "[EDGE EDIT-01 ordering] Directional anchor/focus and affinity survive validation/session/formatting; command-local ordering never replaces the published selection."
    - "Rust-owned EditorSessionState binds selection, pending marks, mixed formatting, capabilities, revision, and generation without document persistence/history."
  artifacts:
    - "anchor/mod.rs provides ICU4X grapheme/atomic position validation."
    - "editor_view/mod.rs provides immutable revision-bound editor session/view DTOs."
    - "Native conformance and editor-session tests plus immutable WASM exports."
  key_links:
    - "Verified Unicode corpus -> ICU byte boundaries -> UTF-16 membership -> stable Rust/WASM result."
    - "Document revision/session generation -> Rust editor DTO -> immutable WASM response."
---

<objective>
Make Rust the grapheme, directional-position, atomic-position, and editor-session authority before mutations cross the browser.

Purpose: Give every later modality one deterministic validation/session contract.
Output: ICU grapheme map, atomic sentinels, directional session DTOs, immutable WASM functions, and conformance tests.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-CONTEXT.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-RESEARCH.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-03-SUMMARY.md
</context>

<tasks>
<task id="02-04-01" type="auto" tdd="true">
  <name>02-04-01: Implement pinned grapheme and atomic logical-position validation</name>
  <read_first>
    - crates/flow-core/src/anchor/mod.rs
    - fixtures/unicode/17.0.0/provenance.json
    - fixtures/unicode/17.0.0/GraphemeBreakTest.txt
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-CONTEXT.md (D-01, D-02)
  </read_first>
  <files>crates/flow-core/src/anchor/mod.rs, crates/flow-core/tests/grapheme_conformance.rs</files>
  <behavior>
    - ICU byte boundaries translate exactly to UTF-16 and match the full pinned Unicode corpus.
    - Stored text is never normalized and invalid positions reject with stable distinct codes.
    - Atomic 0/Forward and 1/Backward edges plus whole-atom directional selections are exhaustive.
  </behavior>
  <action>Per D-01/D-02, layer ICU4X extended-grapheme membership above existing scalar converters. Build one bounded boundary vector per touched immutable block/revision, validate membership without snapping/normalization, and add atomic edge validation distinguished by node kind. Test the entire checked-in corpus plus Ukrainian/decomposed/emoji/non-BMP/empty and invalid sentinel cases.</action>
  <acceptance_criteria>
    - The four EDIT-01 predicates have named native assertions.
    - Invalid targets leave document/session unchanged and return stable distinct errors.</acceptance_criteria>
  <verify><automated>RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-core --test grapheme_conformance -- --nocapture</automated></verify>
  <done>Every public editor position is grapheme/atomic valid under the pinned Rust authority.</done>
</task>

<task id="02-04-02" type="auto" tdd="true">
  <name>02-04-02: Implement revision-bound directional editor session and immutable WASM projection</name>
  <read_first>
    - crates/flow-core/src/lib.rs
    - crates/flow-wasm/src/lib.rs
    - crates/flow-core/src/anchor/mod.rs
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-RESEARCH.md (Pattern 6)
  </read_first>
  <files>crates/flow-core/src/editor_view/mod.rs, crates/flow-core/src/lib.rs, crates/flow-wasm/src/lib.rs, crates/flow-core/tests/editor_session.rs</files>
  <behavior>
    - DirectionalSelection preserves anchor/focus/affinity; only command compilation derives ordered endpoints.
    - Session owns selection, pending/mixed formatting, capabilities, revision, and generation without revision/history/audit/persistence changes.
    - Stale document revision invalidates session actions; unknown fields/DOM/page coordinates reject.
  </behavior>
  <action>Per D-01/D-07/D-11/D-12, create EditorSessionState/EditorViewDto/DirectionalSelection/formatting/capability DTOs in Rust and immutable request-response native/WASM functions. Session-only selection/formatting actions advance generation only. Reject client DOM paths/ranges/page coordinates and unknown/stale values. Do not expose mutable Rust handles or persist session state.</action>
  <acceptance_criteria>
    - Native and serialized WASM results/errors are equal for valid/invalid/stale session actions.
    - Session-only actions generate no persistence plan, document revision, history, or audit.</acceptance_criteria>
  <verify><automated>RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-core --test editor_session -- --nocapture &amp;&amp; npm run build:wasm &amp;&amp; npm run typecheck</automated></verify>
  <done>Every accepted editor session/capability snapshot is immutable, directional, revision-bound, and Rust-owned.</done>
</task>
</tasks>

## Artifacts this phase produces

- ICU4X grapheme and atomic-position validation plus full conformance suite.
- Rust editor session/view/capability DTOs, native entry points, immutable WASM exports, and session tests.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Public position/session DTO -> Rust | Offsets, node kind, affinity, revision, and fields are untrusted. |
| Rust session/view -> browser | Stale or mutable projection could grant false capability. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04-01 | Tampering | grapheme/atomic position | high | mitigate | Pinned corpus membership, exact affinity/node-kind checks, no snapping. |
| T-02-04-02 | Spoofing | session/capability DTO | high | mitigate | Revision/generation binding and Rust computation only. |
| T-02-04-03 | Denial of Service | boundary vectors/DTOs | medium | mitigate | Existing text/envelope bounds and per-touched-block vectors. |
| T-02-04-04 | Elevation of Privilege | mutable WASM handle | high | mitigate | Immutable serialized request-response functions and boundary tests. |
| T-02-04-SC | Tampering | Unicode dependency | high | mitigate | Plans 02-01/02 exact lock/corpus/size evidence. |
</threat_model>

<verification>Run both exact commands; no mutation/tracer begins without grapheme/session/WASM evidence.</verification>
<success_criteria>All four EDIT-01 predicates and session ownership invariants pass natively and at the WASM boundary.</success_criteria>
<output>Create `.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-04-SUMMARY.md` when done.</output>
