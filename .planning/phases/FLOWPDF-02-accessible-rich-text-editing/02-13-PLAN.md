---
phase: FLOWPDF-02-accessible-rich-text-editing
plan: "13"
type: execute
wave: 13
depends_on: ["02-12"]
files_modified:
  - crates/flow-core/src/asset/mod.rs
  - crates/flow-core/src/model/mod.rs
  - crates/flow-core/src/transaction/mod.rs
  - crates/flow-core/src/store/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/src/editor_view/mod.rs
  - crates/flow-core/tests/asset_staging.rs
  - crates/flow-core/tests/recovery.rs
autonomous: true
requirements: [EDIT-01, EDIT-05, QUAL-03, QUAL-04]
estimate: { tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "[EDGE EDIT-05 concurrency] Revision, selection, placement, and receipt are validated together; stale/replayed/substituted commands publish neither image reference nor asset state."
    - "[EDGE EDIT-05 boundary] Images enforce 8 MiB encoded, 8,192 each axis, 24M pixels, and 96 MiB decoded; every zero/min/max/max+1 case is stable."
    - "[EDGE EDIT-05 encoding] Rust accepts only bounded decoded PNG/JPEG, preserves exact Unicode accessibility text, and raw bytes never enter semantic command JSON."
    - "D-14: InsertImage, ReplaceImage, SetImageAccessibility, and RemoveImage are Rust-owned closed commands with capabilities, risk/confirmation metadata, exact inverses, and recovery tests."
    - "Canonical asset `content_hash` remains the existing lowercase `blake3:` result from `canonical::asset_hash`; any staging receipt digest is separately named `staging_digest`, noncanonical, and never replaces content_hash."
    - "Removing/replacing an image removes no shared bytes; asset garbage collection is permitted only when the accepted persistence plan proves zero reachable references after the transaction and undo retention policy."
  artifacts:
    - "crates/flow-core/src/asset/mod.rs owns bounded validation, staging receipts, canonical BLAKE3 identity, and reachability accounting."
    - "crates/flow-core/src/transaction/mod.rs owns the complete image lifecycle and exact inverses."
    - "crates/flow-core/src/store/mod.rs plans atomic asset/reference changes without deleting shared bytes."
  key_links:
    - "Hostile bytes -> bounded Rust staging -> one-use receipt -> Insert/ReplaceImage semantic command."
    - "Image lifecycle command -> reachability-aware persistence plan -> exact undo/recovery."
  prohibitions:
    - id: P2-02
      requirements: [EDIT-05, QUAL-04]
      statement: "MUST NOT invent alt text or silently classify a legacy image with empty/unknown alt semantics as decorative; it remains `MissingLegacy`/needs-review until an explicit user accessibility decision."
      status: unverified
      flagged: true
      verification_tier: test
---

<objective>
Implement the complete safe image lifecycle in Rust, retaining canonical BLAKE3 identity and shared-asset reachability.

Purpose: Fulfill D-14 beyond insertion while keeping hostile bytes bounded and every mutation reversible.
Output: Staging/validation, insert/replace/accessibility/remove commands, capabilities, reachability-aware persistence plans, and recovery evidence.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-CONTEXT.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-RESEARCH.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-PATTERNS.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-12-SUMMARY.md
</context>

<tasks>
<task id="02-13-01" type="auto" tdd="true">
  <name>02-13-01: Validate hostile images and stage one-use receipts without changing canonical identity</name>
  <read_first>
    - crates/flow-core/src/canonical/mod.rs (`asset_hash` and `blake3:` contract)
    - crates/flow-core/src/model/mod.rs
    - crates/flow-core/src/store/mod.rs
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-RESEARCH.md (Pattern 9)
  </read_first>
  <files>crates/flow-core/src/asset/mod.rs, crates/flow-core/src/model/mod.rs, crates/flow-core/src/store/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/asset_staging.rs, crates/flow-core/tests/recovery.rs</files>
  <behavior>
    - StageAsset validates signature/decode/size/dimensions/pixels/allocation before retaining bounded bytes.
    - Canonical content_hash is exactly canonical::asset_hash with blake3: prefix; optional staging_digest is distinct, receipt-private, and noncanonical.
    - At most two receipts/16 MiB per session expire at five minutes and bind session/document/revision/content_hash/metadata for one redemption.
    - InsertImage requires Described(nonempty) or explicit Decorative; MissingLegacy cannot be newly authored.
  </behavior>
  <action>Per D-02/D-09/D-13/D-14/D-16, implement bounded PNG/JPEG staging with admitted decoder features only. Ignore caller MIME/filename/dimensions/hash, compute canonical `content_hash` through the existing `canonical::asset_hash` BLAKE3 path, and if a separate receipt digest is useful name it `staging_digest` and exclude it from canonical schema/asset identity. Issue opaque revision-bound one-use receipts and redeem only inside accepted InsertImage. Cover P2-02, malformed/truncated/bomb, zero/min/max/max+1, expiry/replay/substitution/stale, and privacy-safe diagnostics.</action>
  <acceptance_criteria>
    - Tests assert the exact blake3: canonical contract and reject any SHA-256 substitution for content_hash.
    - Receipt/staged bytes/staging_digest never appear in canonical JSON, public editor DTO, audit, or error content.
    - P2-02 remains explicit and fails if MissingLegacy becomes decorative without user command.</acceptance_criteria>
  <verify><automated>cargo test -p flow-core --test asset_staging --test recovery -- stage_insert --nocapture</automated></verify>
  <done>Hostile bytes are bounded and staged once while canonical asset identity remains the existing BLAKE3 contract.</done>
</task>

<task id="02-13-02" type="auto" tdd="true">
  <name>02-13-02: Implement ReplaceImage, SetImageAccessibility, RemoveImage, and shared reachability</name>
  <read_first>
    - crates/flow-core/src/transaction/mod.rs
    - crates/flow-core/src/store/mod.rs
    - crates/flow-core/src/editor_view/mod.rs
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-UI-SPEC.md (ImageBlock and confirmation)
  </read_first>
  <files>crates/flow-core/src/asset/mod.rs, crates/flow-core/src/transaction/mod.rs, crates/flow-core/src/store/mod.rs, crates/flow-core/src/editor_view/mod.rs, crates/flow-core/tests/asset_staging.rs, crates/flow-core/tests/recovery.rs</files>
  <behavior>
    - ReplaceImage redeems one staged receipt, preserves image node identity/placement, updates accessibility only from explicit input, and stores exact old/new asset references in inverse.
    - SetImageAccessibility changes only Described/Decorative intent and is an explicit-target no-op at current state.
    - RemoveImage requires destructive confirmation metadata, returns deterministic adjacent editable focus, and exact undo restores node/accessibility/reference.
    - Shared bytes remain reachable after one of multiple references is replaced/removed; zero-reference cleanup respects undo retention and is atomic.
  </behavior>
  <action>Per D-09/D-11/D-13/D-14, add closed ReplaceImage, SetImageAccessibility, and RemoveImage commands, Rust capabilities, stable risk/confirmation metadata, exact operations/inverses, and anchor mappings. Compute reachability from the candidate document plus undo-retention policy; never infer deletion from a single node removal and never let TypeScript request physical deletion. Persistence plans must atomically add redeemed bytes/reference and remove only proven unreachable physical records. Test shared reference counts, replacement with same/different asset, described/decorative changes, MissingLegacy review resolution, stale/duplicate/confirmation failure, remove→undo→redo, interruption, and recovery.</action>
  <acceptance_criteria>
    - Every image lifecycle command has exact inverse, capability, focus, and confirmation behavior.
    - Shared assets survive removal/replacement of another reference and recovery rejects dangling references or premature deletion.
    - Atomic failure publishes the complete previous state only.</acceptance_criteria>
  <verify><automated>cargo test -p flow-core --test asset_staging --test recovery -- image_lifecycle --nocapture</automated></verify>
  <done>The complete image lifecycle is Rust-owned, shared-asset safe, atomic, recoverable, and exactly reversible.</done>
</task>
</tasks>

## Artifacts this phase produces

- `asset/mod.rs`: bounded validation, opaque receipts, BLAKE3 canonical identity, optional noncanonical staging digest, and reachability logic.
- Model/transaction/store/editor-view additions for Insert/Replace/SetAccessibility/Remove image lifecycle.
- Core/recovery tests for limits, P2-02, canonical hash distinction, capabilities, inverses, shared references, interruption, and replay.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser bytes/metadata -> Rust staging | Compressed payloads, dimensions, claimed type/hash, and Unicode text are hostile. |
| Receipt -> semantic command | Receipt can be stale, replayed, substituted, or leaked. |
| Candidate references -> physical asset plan | Premature shared-byte deletion or dangling references would corrupt durability. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-13-01 | Denial of Service | image decode | high | mitigate | Signature, encoded/dimension/pixel/allocation/decoded bounds before full decode. |
| T-02-13-02 | Tampering | receipt/hash substitution | high | mitigate | Opaque one-use binding plus exact canonical BLAKE3 verification; staging digest is noncanonical. |
| T-02-13-03 | Tampering | shared asset cleanup | high | mitigate | Candidate reachability plus undo-retention proof; atomic plan rejects dangling/premature deletion. |
| T-02-13-04 | Elevation of Privilege | invented accessibility intent/confirmation | high | mitigate | Closed explicit intent and Rust risk/capability metadata with P2-02 tests. |
| T-02-13-05 | Information Disclosure | bytes/text/receipt diagnostics | high | mitigate | Stable codes/counts only; no content, bytes, hashes beyond safe canonical IDs, or receipts. |
| T-02-13-SC | Tampering | decoder dependency | high | mitigate | Exact Plan 02-01 provenance/integrity lock and PNG/JPEG features only. |
</threat_model>

<verification>Run both exact core/recovery commands and inspect canonical-hash/shared-asset/confirmation assertions.</verification>
<success_criteria>All three remaining EDIT-05 predicates and complete D-14 lifecycle pass without identity drift or asset loss.</success_criteria>
<output>Create `.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-13-SUMMARY.md` when done.</output>
