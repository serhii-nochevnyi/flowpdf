---
phase: FLOWPDF-02-accessible-rich-text-editing
plan: "05"
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - crates/flow-core/src/transaction/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-wasm/src/lib.rs
  - web/persistence/indexeddb-store.ts
  - crates/flow-core/tests/rich_text_transactions.rs
  - web/src/main.tsx
  - web/src/editor/editor-app.tsx
  - web/src/editor/semantic-document.tsx
  - web/tests/walking-skeleton.browser.test.ts
autonomous: true
requirements: [EDIT-01, EDIT-02, QUAL-04]
estimate: { tokens: 34000, raw_tokens: 34000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "One paragraph edit traverses real Rust command validation/inverse -> immutable WASM -> guarded IndexedDB -> Rust re-query/recovery -> React semantic projection."
    - "Stale, duplicate, invalid-grapheme, and failed-persistence edits publish no document/session state."
    - "A real Chromium tracer opens/migrates v2, applies one Ukrainian/English paragraph edit, undoes/redoes, cold-remounts, reloads, and recovers the same hash/revision/selection/history."
    - "The React surface is page-neutral, contains one native paragraph copy, and retains Foundation Inspector diagnostics without making DOM canonical."
  artifacts:
    - "Rust ReplaceSelection command/inverse and immutable native/WASM result."
    - "IndexedDB accepted commit/re-query path for editor results."
    - "Minimal React paragraph product tracer and real browser lifecycle test."
  key_links:
    - "Editor edit intent -> Rust ReplaceSelection -> persistence plan -> IndexedDB completion -> Rust re-query -> React snapshot."
---

<objective>
Prove the production core/WASM/persistence/minimal-React paragraph tracer before expanding browser input machinery.

Purpose: Validate the end-to-end architecture with the smallest durable semantic edit path.
Output: ReplaceSelection core, immutable WASM result, persistence wiring, minimal semantic paragraph shell, and real browser tracer.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-UI-SPEC.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-PATTERNS.md
@.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-04-SUMMARY.md
</context>

<tasks>
<task id="02-05-01" type="tracer" tdd="true">
  <name>02-05-01: Trace one paragraph replacement through Rust, WASM, persistence, and recovery</name>
  <read_first>
    - crates/flow-core/src/transaction/mod.rs
    - crates/flow-core/src/lib.rs
    - crates/flow-wasm/src/lib.rs
    - web/persistence/indexeddb-store.ts
  </read_first>
  <files>crates/flow-core/src/transaction/mod.rs, crates/flow-core/src/lib.rs, crates/flow-wasm/src/lib.rs, web/persistence/indexeddb-store.ts, crates/flow-core/tests/rich_text_transactions.rs</files>
  <behavior>
    - ReplaceSelection validates revision, directional grapheme endpoints, command ID, exact text limits, and generates exact inverse/accepted caret.
    - Accepted result yields the existing guarded persistence plan; rejection yields none.
    - Commit/reload/recovery returns exact v2 hash/revision/text/selection/history; undo/redo is exact.
  </behavior>
  <action>Per D-01/D-02/D-05/D-11/D-12, add the narrow ReplaceSelection semantic command and exact inverse through TransactionService, expose immutable native/WASM request-response functions, and reuse the Phase 1 Rust-planned persistence/recovery sequence. Keep semantic JSON parsing/hashing out of TypeScript and return the accepted editor view only after durable commit plus Rust re-query. This core tracer intentionally precedes input-host/IME work in Plan 02-06.</action>
  <acceptance_criteria>
    - Native test proves happy path, stale, duplicate, invalid grapheme, empty/no-op, undo/redo, and recovery.
    - No rejected edit produces a persistence plan or published session.</acceptance_criteria>
  <verify><automated>cargo test -p flow-core --test rich_text_transactions -- paragraph_tracer --nocapture &amp;&amp; npm run build:wasm</automated></verify>
  <done>The durable paragraph command path is production-real across core, WASM, IndexedDB, and recovery.</done>
</task>

<task id="02-05-02" type="auto" tdd="true">
  <name>02-05-02: Mount the minimal page-neutral semantic paragraph tracer in React</name>
  <read_first>
    - web/src/main.ts
    - web/tests/walking-skeleton.browser.test.ts
    - web/src/foundation-inspector.ts
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-UI-SPEC.md (EditorAppShell, DocumentEditorRegion)
  </read_first>
  <files>web/src/main.tsx, web/src/editor/editor-app.tsx, web/src/editor/semantic-document.tsx, web/tests/walking-skeleton.browser.test.ts</files>
  <behavior>
    - React mounts one page-neutral labeled editor region with one accepted native paragraph and durable status.
    - The tracer invokes the real accepted paragraph edit path, publishes only after commit/re-query, and retains exact cached snapshot identity.
    - Cold remount/reload/recovery and undo/redo preserve exact hash/revision/text/selection/history; Inspector remains reachable as diagnostics.
  </behavior>
  <action>Per D-03/D-10/D-12/D-13/D-16, add the smallest React product shell and semantic paragraph projection around the real core/WASM/IndexedDB path. Keep the accepted snapshot immutable and render authored text as text nodes. Use the tracer test driver to issue the same public paragraph command while the native input host, DOM selection bridge, and IME are added in Plan 02-06; do not create an alternate canonical editing store or parse canonical JSON.</action>
  <acceptance_criteria>
    - Real Chromium crosses every production boundary and verifies exact durable lifecycle.
    - React contains no optimistic semantic tree, duplicate accessible text, fake pages, or removed Phase 1 diagnostics.</acceptance_criteria>
  <verify><automated>npm run build:web &amp;&amp; npx --no-install vitest run --project browser web/tests/walking-skeleton.browser.test.ts --grep "Phase 2 paragraph tracer"</automated></verify>
  <done>The minimal React paragraph tracer proves the full durable architecture before input expansion.</done>
</task>
</tasks>

## Artifacts this phase produces

- Rust/WASM ReplaceSelection command, inverse, result, and focused native test.
- IndexedDB editor commit/re-query reuse.
- Minimal React semantic paragraph product shell and real browser tracer.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Paragraph command -> Rust | Selection, revision, ID, and text are untrusted. |
| Rust result -> IndexedDB -> React | Partial persistence or stale response must not publish. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05-01 | Tampering | ReplaceSelection DTO | high | mitigate | Rust revision/ID/grapheme/envelope validation before candidate mutation. |
| T-02-05-02 | Tampering | persistence/publication | high | mitigate | Guarded atomic commit, Rust re-query identity check, then immutable publish. |
| T-02-05-03 | Cross-site Scripting | paragraph projection | high | mitigate | React text/native semantics only. |
| T-02-05-04 | Information Disclosure | errors/audit | medium | mitigate | Stable safe codes without authored text/canonical payload. |
| T-02-05-SC | Tampering | dependencies | high | mitigate | Exact Plan 02-01 locks and Plan 02-02 boundary gate. |
</threat_model>

<verification>Run both exact commands against real WASM/IndexedDB/Chromium, not controller mocks.</verification>
<success_criteria>The minimal paragraph edit is durable, recoverable, semantic, and Rust-authoritative end to end.</success_criteria>
<output>Create `.planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-05-SUMMARY.md` when done.</output>
