---
phase: FLOWPDF-03-deterministic-reflow-and-pagination
plan: "05"
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - crates/flow-wasm/src/lib.rs
  - crates/flow-core/src/layout/mod.rs
  - web/src/layout/layout-worker.ts
  - web/src/layout/layout-protocol.ts
  - web/src/editor/editor-controller.ts
  - web/tests/layout-worker.test.ts
  - crates/flow-wasm/tests/layout_exports.rs
autonomous: true
requirements: [LAYO-08]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "WASM exposes only bounded serialized layout request/result functions; it does not expose mutable layout handles or raw font/system state."
    - "The worker can paginate later pages while the active editor revision remains interactive."
    - "Out-of-order or stale results are rejected by request ID, source revision, and layout fingerprint before store publication."
    - "Cancellation/abort is cooperative and bounded; it cannot leave a partially published page tree."
  artifacts:
    - "Versioned Rust/WASM layout DTO and worker protocol."
    - "Revision-aware layout scheduler with stale-result/cancel tests."
    - "WASM export tests proving closed DTOs and no raw font/text diagnostics leakage."
  key_links:
    - "Editor accepted revision -> layout request -> worker -> Rust result validation -> accepted layout store."
    - "Worker result fingerprint -> editor revision guard -> visual projection only."
---

<objective>Expose the deterministic layout engine through a safe worker protocol while preserving active-page editability.</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-CONTEXT.md
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-PATTERNS.md
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-04-SUMMARY.md
@crates/flow-wasm/src/lib.rs
@web/src/editor/editor-controller.ts
</context>

<tasks>
<task id="03-05-01" type="auto" tdd="true">
  <name>03-05-01: Add closed WASM layout DTOs and serialized request/result boundary</name>
  <read_first>
    - crates/flow-wasm/src/lib.rs
    - crates/flow-core/src/layout/mod.rs
    - crates/flow-core/src/editor_view/mod.rs
  </read_first>
  <files>crates/flow-wasm/src/lib.rs, crates/flow-core/src/layout/mod.rs, crates/flow-wasm/tests/layout_exports.rs</files>
  <behavior>
    - Layout requests validate source revision/hash, settings, font/data identities, viewport bounds, and serialized byte limits before engine execution.
    - Results include request identity, pages/fragments, diagnostics, and result hash; raw font bytes and authored diagnostic payloads never cross the boundary.
    - Invalid/future DTO versions and stale revisions return stable errors without partial state.
  </behavior>
  <action>Add versioned serde DTOs and narrow WASM functions that call the Rust engine. Reuse project canonical decode/validation, preserve privacy-safe diagnostics, and add export contract tests for malformed input, size limits, identity mismatch, and deterministic output.</action>
  <acceptance_criteria>
    - `cargo test` proves request/result round trips and rejects unknown/future DTO fields according to the existing boundary policy.
    - WASM APIs remain request/response and do not return mutable Rust handles.
  </acceptance_criteria>
  <verify><automated>RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-wasm --test layout_exports -- --nocapture && npm run build:wasm</automated></verify>
  <done>The layout engine has a bounded immutable WASM contract.</done>
</task>

<task id="03-05-02" type="auto" tdd="true">
  <name>03-05-02: Implement revision-aware worker scheduling and stale-result rejection</name>
  <read_first>
    - web/src/editor/editor-controller.ts
    - web/src/editor/editor-store.ts
    - web/src/editor/semantic-document.tsx
    - web/tests/editor-controller.test.ts
  </read_first>
  <files>web/src/layout/layout-worker.ts, web/src/layout/layout-protocol.ts, web/src/editor/editor-controller.ts, web/tests/layout-worker.test.ts</files>
  <behavior>
    - A new accepted document revision supersedes older layout requests and keeps the active page/session usable while the worker runs.
    - A result with wrong request ID, source revision, settings fingerprint, font/data identity, or result hash is discarded and surfaced only as a safe diagnostic.
    - A successful result is published atomically; partial/cancelled pages never replace the accepted result.
  </behavior>
  <action>Add the worker message protocol, scheduler, abort handling, and controller/store integration. Keep semantic editor input independent from worker completion, and make the accepted layout revision visible for later viewport work. Test out-of-order promises, cancellation, repeated requests, and worker failure recovery.</action>
  <acceptance_criteria>
    - Unit tests demonstrate active-page editing continues before a later-page result arrives.
    - No stale worker response mutates semantic document state or replaces a newer layout result.
  </acceptance_criteria>
  <verify><automated>npm run typecheck && npx --no-install vitest run web/tests/layout-worker.test.ts</automated></verify>
  <done>LAYO-08 worker scheduling is revision-safe and independently verified.</done>
</task>
</tasks>

<artifacts>WASM layout boundary, worker protocol, scheduler, stale-result guard, and tests.</artifacts>
<verification>Run Rust/WASM boundary tests, build wasm, typecheck, and execute worker contract tests.</verification>
<success_criteria>Background pagination can run concurrently with editing without stale or partial publication.</success_criteria>
<output>Create `03-05-SUMMARY.md` when done.</output>
