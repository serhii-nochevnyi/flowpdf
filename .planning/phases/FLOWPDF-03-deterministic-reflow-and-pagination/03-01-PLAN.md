---
phase: FLOWPDF-03-deterministic-reflow-and-pagination
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/flow-core/Cargo.toml
  - crates/flow-core/data/NotoSans-Regular.ttf
  - crates/flow-core/data/NotoSans-OFL.txt
  - crates/flow-core/data/uk.standard.bincode
  - crates/flow-core/src/lib.rs
  - crates/flow-core/src/layout/mod.rs
  - crates/flow-core/tests/layout_text.rs
  - crates/flow-core/tests/layout_fixed_point.rs
  - fixtures/layout/phase3-text-corpus.json
  - fixtures/layout/font-provenance.json
autonomous: true
requirements: [LAYO-01, LAYO-02]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "A Rust layout request shapes supported text through an explicit font catalog and returns glyph clusters, direction, fallback identity, and fixed-point advances without consulting host fonts."
    - "ICU line opportunities and versioned Ukrainian hyphenation are represented as source ranges/discretionary breaks and never mutate stored text."
    - "The same input bytes, font/data identities, and engine version produce identical serialized text fragments and result hash."
    - "Malformed font/catalog, unsupported direction, overflow, and illegal break inputs fail with stable bounded diagnostics."
  artifacts:
    - "Owned layout module with fixed-point geometry, font provider, shaping, direction, line-break, and hyphenation seams."
    - "Checked-in corpus and font provenance fixture with no system-font dependency."
    - "Rust unit/property coverage for clusters, fallback, direction, Ukrainian break points, and fixed-point conversion."
  key_links:
    - "FlowDocument + explicit LayoutRequest -> layout::layout_text -> serialized line/glyph fragments and result fingerprint."
    - "Font/data identities -> deterministic request fingerprint -> stable result hash."
---

<objective>
Build the first production-quality Phase 3 vertical tracer: deterministic
fixed-point paragraph shaping and line breaking owned by Rust.

Purpose: establish the typography contract before pagination, schema-v3 section
settings, workers, or page UI depend on it.
Output: layout text module, pinned dependency/data seams, and focused fixtures.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-CONTEXT.md
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-RESEARCH.md
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-PATTERNS.md
@crates/flow-core/src/model/mod.rs
@crates/flow-core/src/anchor/mod.rs
@crates/flow-core/src/lib.rs
</context>

<tasks>
<task id="03-01-01" type="auto" tdd="true">
  <name>03-01-01: Admit the deterministic text-layout dependency and font/data boundary</name>
  <read_first>
    - Cargo.toml
    - crates/flow-core/Cargo.toml
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-RESEARCH.md
    - work/toolchains/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ttf-parser-0.25.1/src/lib.rs
    - work/toolchains/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/rustybuzz-0.20.1/src/lib.rs
  </read_first>
  <files>Cargo.toml, Cargo.lock, crates/flow-core/Cargo.toml, crates/flow-core/data/NotoSans-Regular.ttf, crates/flow-core/data/NotoSans-OFL.txt, crates/flow-core/data/uk.standard.bincode, crates/flow-core/src/lib.rs, crates/flow-core/src/layout/mod.rs, fixtures/layout/font-provenance.json</files>
  <behavior>
    - A closed `FontCatalog` accepts only bounded byte-backed faces with stable family/style/face-index/content identity and rejects missing, malformed, duplicate, or over-budget faces.
    - A `LayoutRequest` records source revision/hash, fixed-point width, locale/direction policy, font catalog identity, ICU/hyphenation data identity, and engine identity.
    - Fixed-point conversion uses checked integer/rational arithmetic and exposes no float in public layout DTOs.
    - RustyBuzz/ttf-parser, unicode-bidi, ICU segmenter, and Ukrainian hyphenation adapters remain behind project-owned types; host font discovery is impossible.
  </behavior>
  <action>Pin the smallest verified dependency set and add the `layout` module. Define fixed-point geometry, catalog/request identities, bounded font validation, stable error codes, and the provider traits/adapters. Record the admitted test-font provenance/license/hash or fail closed with an explicit fixture status; do not copy or use a system font implicitly.</action>
  <acceptance_criteria>
    - `cargo tree --locked -p flow-core` shows only the intended text/layout dependencies and no accidental all-language or host-font feature.
    - Font/data identity and fixed-point conversion tests cover zero, maximum, overflow, duplicate identity, and malformed bytes.
    - The public layout types are serde-stable, closed, revision-bound, and contain only deterministic scalar/byte-identity fields.
  </acceptance_criteria>
  <verify><automated>RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-core --test layout_fixed_point -- --nocapture</automated></verify>
  <done>The Rust layout request and font/data trust boundary exists and is independently testable.</done>
</task>

<task id="03-01-02" type="auto" tdd="true">
  <name>03-01-02: Shape text, resolve fallback/direction, and emit legal line opportunities</name>
  <read_first>
    - crates/flow-core/src/layout/mod.rs
    - fixtures/layout/font-provenance.json
    - .planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-RESEARCH.md
    - crates/flow-core/tests/grapheme_conformance.rs
  </read_first>
  <files>crates/flow-core/src/layout/mod.rs, crates/flow-core/tests/layout_text.rs, fixtures/layout/phase3-text-corpus.json</files>
  <behavior>
    - Ukrainian/English, decomposed Unicode, emoji, mixed-direction controls, and punctuation retain source cluster ranges and deterministic glyph order/advances.
    - Missing glyphs select the first admitted catalog face that covers the cluster, or return a stable unsupported-glyph diagnostic; system fonts are never consulted.
    - ICU line-break opportunities and Ukrainian discretionary hyphenation are returned as source boundaries with explicit metadata; canonical text remains byte-identical.
    - A candidate break that splits an unsafe shaped cluster is rejected, and repeated layout calls serialize identically.
  </behavior>
  <action>Implement the paragraph tracer over the admitted provider: segment source text, resolve directional/font/script runs, shape with RustyBuzz, map clusters back to UTF-16, accumulate checked fixed-point advances, and expose line candidates within a requested width. Add corpus fixtures and focused/property tests for fallback, direction, breaks, hyphenation, no-text-mutation, and stable hashing. Keep page fragmentation out of this task.</action>
  <acceptance_criteria>
    - Tests assert source UTF-16 ranges, glyph IDs/order, fallback face identities, line widths, and discretionary break metadata rather than only “nonempty output”.
    - Ukrainian hyphenation data is versioned and provenance/hash checked; an unavailable or mismatched data artifact fails closed.
    - The tracer does not claim full pagination or PDF output and passes a differential smoke against the chosen reference behavior for the admitted fixture.
  </acceptance_criteria>
  <verify><automated>RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-core --test layout_text -- --nocapture && RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-core --lib layout</automated></verify>
  <done>LAYO-01/02 typography and break data have a deterministic Rust-owned tracer with honest fixture/provenance evidence.</done>
</task>
</tasks>

<artifacts>
- `flow_core::layout` deterministic fixed-point text tracer and typed diagnostics.
- Exact text corpus and font/data provenance fixtures.
- Rust tests for shaping, fallback, direction, clusters, line breaking, Ukrainian hyphenation, and fixed-point arithmetic.
</artifacts>

<threat_model>
| Threat | Mitigation |
|---|---|
| Host font/data substitution | Explicit byte-backed catalog, identity/hash, and fail-closed provider. |
| Text/cluster corruption | Source range mapping, ICU/grapheme boundaries, unsafe-break rejection, and no stored-text mutation. |
| Numeric drift/overflow | One fixed-point type, checked rational conversions, and boundary/property tests. |
| WASM bloat from language data | Ukrainian-only data admission and feature/provenance/size checks. |
</threat_model>

<verification>Run both task commands, inspect serialized fixture hashes, and verify the dependency tree does not add host-font or all-language data.</verification>
<success_criteria>The same explicit inputs produce the same shaped cluster/line output and hash, with correct unsupported-data diagnostics and no semantic text mutation.</success_criteria>
<output>Create `.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-01-SUMMARY.md` when done.</output>
