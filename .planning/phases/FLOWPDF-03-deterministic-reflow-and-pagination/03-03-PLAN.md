---
phase: FLOWPDF-03-deterministic-reflow-and-pagination
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - crates/flow-core/src/layout/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/layout_pagination.rs
  - crates/flow-core/tests/layout_constraints.rs
  - fixtures/layout/phase3-pagination-corpus.json
autonomous: true
requirements: [LAYO-03, LAYO-05, LAYO-06]
estimate: { tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "A changed block height reflows every affected following block and produces deterministic pages/fragments without changing stored text."
    - "Headers/footers repeat at configured section pages and explicit page breaks create stable page boundaries."
    - "Keep-with-next, keep-together, widow/orphan, and simple table row/header repetition use bounded deterministic fallback reasons."
    - "Impossible constraints never loop, drop a node, or silently duplicate a row."
  artifacts:
    - "Owned fragment tree/page paginator with stable source ranges and break reasons."
    - "Pagination corpus covering paragraphs, headings, lists, tables, page breaks, sections, headers, footers, and constraint fallback."
    - "Rust golden/property tests for page movement, row fragmentation, and constraint behavior."
  key_links:
    - "layout text tracer + v3 settings -> fragment tree -> pages with fixed-point geometry."
    - "break reason and carry state -> deterministic next-fragment placement."
---

<objective>Build the deterministic fragment tree and paginator over semantic blocks.</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-CONTEXT.md
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-PATTERNS.md
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-01-SUMMARY.md
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-02-SUMMARY.md
</context>

<tasks>
<task id="03-03-01" type="auto" tdd="true">
  <name>03-03-01: Emit stable block/line fragments and paginate ordinary flow</name>
  <read_first>
    - crates/flow-core/src/layout/mod.rs
    - crates/flow-core/src/model/mod.rs
    - fixtures/layout/phase3-text-corpus.json
  </read_first>
  <files>crates/flow-core/src/layout/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/layout_pagination.rs, fixtures/layout/phase3-pagination-corpus.json</files>
  <behavior>
    - Paragraphs/headings/lists/images/page breaks map to stable fragment/source ranges and fixed-point rectangles across pages.
    - A block height change moves all affected following fragments while unaffected source identity and text ranges remain stable.
    - Sections choose page geometry and repeat static header/footer fragments deterministically.
    - Explicit page breaks and page overflow have stable break reasons and bounded page counts.
  </behavior>
  <action>Implement a depth-first semantic-to-fragment traversal and page cursor. Integrate the text tracer, section settings, fixed-point margins, header/footer bands, block spacing, explicit page breaks, and bounded image placeholder metrics. Emit normalized pages/fragments and safe diagnostics; retain all unsupported constructs as source-linked fallback fragments.</action>
  <acceptance_criteria>
    - Golden tests assert exact page count, source node order, fixed-point rectangles, and break reasons for ordinary flow and section/header/footer fixtures.
    - A representative edit changes only the expected suffix after the first affected block and never changes canonical text.
  </acceptance_criteria>
  <verify><automated>RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-core --test layout_pagination -- --nocapture</automated></verify>
  <done>Ordinary semantic flow reflows and paginates into a stable fragment tree.</done>
</task>

<task id="03-03-02" type="auto" tdd="true">
  <name>03-03-02: Add keep, widow/orphan, and simple-table fragmentation rules</name>
  <read_first>
    - crates/flow-core/src/layout/mod.rs
    - fixtures/layout/phase3-pagination-corpus.json
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>crates/flow-core/src/layout/mod.rs, crates/flow-core/tests/layout_constraints.rs, crates/flow-core/tests/layout_pagination.rs, fixtures/layout/phase3-pagination-corpus.json</files>
  <behavior>
    - Headings keep with their next block when possible; keep-together and widow/orphan rules request bounded rollback before stable fallback.
    - Simple tables split only between rows, repeat a configured header row on continuation pages, and preserve row/cell source ranges.
    - Overlarge rows/blocks use an explicit overflow fallback rather than an infinite retry or silent loss.
    - Repeated runs produce byte-identical normalized fragment trees and diagnostics.
  </behavior>
  <action>Implement the constraint evaluator and bounded rollback queue. Add simple-table row fragmentation/header repetition and explicit fallback metadata. Keep rowspans/merged cells/nested tables out of scope with typed unsupported diagnostics. Add adversarial fixtures for impossible keep constraints and an overlarge table row.</action>
  <acceptance_criteria>
    - Tests prove all semantic content remains represented once, continuation headers are marked as derived repetitions, and fallback is stable.
    - Constraint evaluation has a fixed retry/rollback bound and cannot loop on a document-sized input.
  </acceptance_criteria>
  <verify><automated>RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-core --test layout_constraints --test layout_pagination -- --nocapture</automated></verify>
  <done>LAYO-03/05/06 pagination behavior is deterministic, bounded, and source-preserving.</done>
</task>
</tasks>

<artifacts>Fragment tree, paginator, break reasons, constraint engine, simple-table continuation behavior, and pagination fixtures.</artifacts>
<verification>Run both pagination task commands and inspect normalized fixture hashes under repeated execution.</verification>
<success_criteria>Height-changing edits repaginate following content, constraints produce stable bounded fallbacks, and simple tables split only at legal row boundaries.</success_criteria>
<output>Create `03-03-SUMMARY.md` when done.</output>
