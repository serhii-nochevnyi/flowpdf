---
phase: FLOWPDF-03-deterministic-reflow-and-pagination
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - crates/flow-core/src/model/mod.rs
  - crates/flow-core/src/schema/mod.rs
  - crates/flow-core/src/schema/legacy.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/schema_v3_layout.rs
  - crates/flow-core/tests/migration_golden.rs
  - fixtures/flowdoc/schema-v2-layout-default.json
  - fixtures/flowdoc/schema-v3-sections.json
autonomous: true
requirements: [LAYO-04]
estimate: { tokens: 16000, raw_tokens: 16000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Page geometry, orientation, margins, section boundaries, and static header/footer intent are durable typed settings rather than browser-only options."
    - "Schema v2 documents migrate sequentially to v3 with canonical content, IDs, fields, assets, and provenance preserved."
    - "Invalid section boundaries, oversized header/footer content, and unsupported geometry fail with stable validation codes before layout."
  artifacts:
    - "Versioned v3 layout/section model and migration route."
    - "Migration goldens for v2 no-op/defaults and v3 section/header/footer data."
    - "Schema validation tests for bounds, ordering, and canonical bytes."
  key_links:
    - "FlowDocument schema v3 -> LayoutRequest section settings -> later fragment pagination."
    - "MigrationRegistry v2->v3 -> canonical persistence/open/recover path."
---

<objective>Admit durable page and section settings required by LAYO-04.</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-CONTEXT.md
@.planning/phases/FLOWPDF-03-deterministic-reflow-and-pagination/03-RESEARCH.md
@crates/flow-core/src/model/mod.rs
@crates/flow-core/src/schema/mod.rs
</context>

<tasks>
<task id="03-02-01" type="auto" tdd="true">
  <name>03-02-01: Add bounded v3 geometry, section, and static header/footer model</name>
  <read_first>
    - crates/flow-core/src/model/mod.rs
    - crates/flow-core/src/schema/mod.rs
    - crates/flow-core/src/schema/legacy.rs
    - fixtures/flowdoc/schema-v2-current.json
  </read_first>
  <files>crates/flow-core/src/model/mod.rs, crates/flow-core/src/schema/mod.rs, crates/flow-core/src/schema/legacy.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/schema_v3_layout.rs</files>
  <behavior>
    - Section settings use stable IDs and explicit ordered boundaries anchored to semantic block IDs, with one default section for documents that have no explicit sections.
    - Header/footer content is bounded typed static inline content with locale/style metadata; it is not arbitrary HTML, display-list bytes, or duplicated canonical body nodes.
    - Page size/orientation/margins and header/footer distances validate with checked sums and deterministic limits.
  </behavior>
  <action>Extend the canonical model through a deliberate schema-v3 design, preserving existing page settings where compatible and adding explicit sections/header/footer settings. Add closed types, stable IDs, defaults, validation, and public exports. Ensure layout settings have no floats, DOM paths, or derived coordinates.</action>
  <acceptance_criteria>
    - Existing constructors/tests compile with explicit v3 defaults and schema validation rejects malformed section ordering, duplicate IDs, and overflow.
    - The model retains semantic body identity and does not encode layout fragments.
  </acceptance_criteria>
  <verify><automated>RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-core --test schema_v3_layout -- --nocapture</automated></verify>
  <done>The v3 durable layout settings are typed, bounded, and accepted by the Rust model.</done>
</task>

<task id="03-02-02" type="auto" tdd="true">
  <name>03-02-02: Implement and golden-test the v2-to-v3 migration boundary</name>
  <read_first>
    - crates/flow-core/src/schema/mod.rs
    - crates/flow-core/tests/migration_golden.rs
    - fixtures/flowdoc/schema-v2-current.json
    - .planning/phases/FLOWPDF-02-accessible-rich-text-editing/02-03-SUMMARY.md
  </read_first>
  <files>crates/flow-core/src/schema/mod.rs, crates/flow-core/src/schema/legacy.rs, crates/flow-core/tests/migration_golden.rs, fixtures/flowdoc/schema-v2-layout-default.json, fixtures/flowdoc/schema-v3-sections.json</files>
  <behavior>
    - v2 bytes take exactly one v2->v3 hop, receive deterministic default layout settings, and preserve canonical body/assets/fields/provenance.
    - v3 bytes are a no-op and produce the same canonical hash on repeated migration.
    - Future schema, unknown fields, invalid anchors, invalid geometry, and oversized header/footer content fail closed without publishing a partial document.
  </behavior>
  <action>Freeze the private v2 decoder, register the contiguous v2->v3 migration, validate each candidate before publication, and add exact migration/no-op/invalid goldens. Update schema version fixtures and any persistence helpers without weakening the Phase 2 legacy freeze.</action>
  <acceptance_criteria>
    - The complete migration route tests include v0/v1/v2 legacy paths plus v3 no-op coverage where applicable.
    - Canonical hashes and semantic snapshots prove no body text or existing field anchor changes.
  </acceptance_criteria>
  <verify><automated>RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-core --test migration_golden --test schema_v3_layout -- --nocapture</automated></verify>
  <done>Schema-v3 layout settings migrate losslessly and are ready for pagination.</done>
</task>
</tasks>

<artifacts>Versioned layout settings, v2->v3 migration, validation, and canonical migration fixtures.</artifacts>
<verification>Run schema and migration tests and compare canonical hashes for v2 default and v3 explicit fixtures.</verification>
<success_criteria>LAYO-04 inputs are durable, bounded, and migration-safe without storing derived layout.</success_criteria>
<output>Create `03-02-SUMMARY.md` when done.</output>
