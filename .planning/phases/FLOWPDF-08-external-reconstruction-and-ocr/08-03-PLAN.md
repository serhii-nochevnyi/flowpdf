---
phase: FLOWPDF-08-external-reconstruction-and-ocr
plan: "03"
title: "Persist immutable sources and add side-by-side reconstruction review"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - web/persistence/pdf-source-store.ts
  - web/src/pdf/pdf-reconstruction-panel.tsx
  - web/src/pdf/pdf-reconstruction-panel.css
  - web/src/editor/editor-app.tsx
  - web/src/layout/layout-runtime.ts
  - web/tests/pdf-source-store.test.ts
  - web/tests/pdf-reconstruction.browser.test.ts
requirements: [PDFI-06, PDFI-07, PDFI-08, QUAL-01, QUAL-02]
delivery:
  ticket: T-08-03
  risk: high
  human_checkpoint: false
estimate: { tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: medium }
must_haves:
  truths:
    - "The original PDF bytes are retained under an immutable source identity and are not replaced by a candidate or OCR text."
    - "Original scene and semantic candidate are visible side by side with reports, confidence markers, opaque islands, and OCR review controls."
    - "Acceptance is unavailable until required review decisions and source identity checks pass; the UI never labels the result exact."
  artifacts:
    - "Guarded IndexedDB source store with put-if-absent/conflict tests."
    - "Accessible comparison/review panel integrated as an optional editor surface."
    - "Chromium evidence for source immutability, side-by-side review, OCR warnings, and acceptance gating."
  key_links:
    - "file bytes -> immutable source store + reader scene -> reconstruction candidate -> review -> explicit accept"
---

<objective>Give users a truthful comparison surface that preserves the original while they review and accept external reconstruction.</objective>

<context>
@.planning/phases/FLOWPDF-08-external-reconstruction-and-ocr/08-CONTEXT.md
@.planning/phases/FLOWPDF-08-external-reconstruction-and-ocr/08-PATTERNS.md
@web/src/pdf/pdf-reader-panel.tsx
@web/persistence/indexeddb-store.ts
@web/src/editor/editor-app.tsx
</context>

<tasks>
  <task id="08-03-01" type="auto" tdd="true">
    <name>08-03-01: Add an immutable PDF source store</name>
    <files>web/persistence/pdf-source-store.ts, web/tests/pdf-source-store.test.ts</files>
    <behavior>
      - A source can be stored once under source hash/byte length and read back as cloned bytes.
      - Repeating the same identity with identical bytes is idempotent; conflicting bytes are rejected and no overwrite/delete path exists.
      - Storage limits and malformed identities fail with payload-free stable codes.
    </behavior>
    <action>Implement a separate IndexedDB object store/versioned envelope for imported PDF source records. Keep it independent from semantic snapshots/assets and expose only immutable put/get metadata operations to reconstruction UI.</action>
    <acceptance_criteria>
      - Unit tests prove exact round-trip, clone isolation, idempotent replay, conflict rejection, and byte/record limits.
      - PDF bytes do not appear in candidate FlowDocument, command, audit, or diagnostic DTOs.
    </acceptance_criteria>
    <verify><automated>PLAYWRIGHT_BROWSERS_PATH=./work/playwright npx --no-install vitest run --project unit web/tests/pdf-source-store.test.ts</automated></verify>
    <done>The original import source has a durable immutable browser lane.</done>
  </task>
  <task id="08-03-02" type="auto" tdd="true">
    <name>08-03-02: Build the semantic comparison and review panel</name>
    <files>web/src/pdf/pdf-reconstruction-panel.tsx, web/src/pdf/pdf-reconstruction-panel.css, web/src/editor/editor-app.tsx</files>
    <behavior>
      - File input stores the original before reader/reconstruction requests and displays source identity/status.
      - The original scene remains read-only visual content while candidate blocks are semantic DOM with confidence/review/opaque-island markers.
      - Reports appear before acceptance; low-confidence/OCR items require explicit keep/edit/reject decisions; accepted state remains labeled reconstructed/best-effort.
    </behavior>
    <action>Add the panel with bounded page selection, side-by-side columns, review controls, report-first status, and an explicit accept action routed through the reconstruction scheduler. Keep visual scene `aria-hidden`, do not expose external links, and do not dispatch ordinary editor commands implicitly.</action>
    <acceptance_criteria>
      - The panel handles unavailable OCR, stale source, unsupported visual content, missing mapping, and acceptance failures visibly.
      - Candidate and source identities are rendered for inspection without raw bytes or authored diagnostic payloads.
    </acceptance_criteria>
    <verify><automated>npm run typecheck && npm run build:web</automated></verify>
    <done>A user can inspect the original and candidate in one truthful review surface.</done>
  </task>
  <task id="08-03-03" type="auto" tdd="true">
    <name>08-03-03: Add Chromium review/acceptance evidence</name>
    <files>web/tests/pdf-reconstruction.browser.test.ts, web/src/layout/layout-runtime.ts</files>
    <behavior>
      - A deterministic fake reader/reconstruction adapter can render original/candidate columns and review items in Chromium.
      - Acceptance is disabled before decisions, becomes available after all required decisions, and reports reconstructed/best-effort rather than exact.
      - The existing semantic editor content and reader worker behavior remain unchanged.
    </behavior>
    <action>Wire the optional reconstruction scheduler into the default runtime and add a browser test that asserts source binding, report visibility, opaque-island display, low-confidence/OCR review, and final acceptance status.</action>
    <acceptance_criteria>
      - Browser evidence proves no semantic editor mutation occurs merely by opening or reviewing a PDF.
      - Source-store identity remains stable across the candidate acceptance flow.
    </acceptance_criteria>
    <verify><automated>PLAYWRIGHT_BROWSERS_PATH=./work/playwright npx --no-install vitest run --project browser web/tests/pdf-reconstruction.browser.test.ts</automated></verify>
    <done>Comparison and review are covered by real browser evidence.</done>
  </task>
</tasks>

<verification>Run source-store unit tests, TypeScript/build checks, and the Chromium reconstruction review test.</verification>
<success_criteria>Users can compare, review, and explicitly accept a confidence-scored reconstruction while the original PDF remains immutable and visible.</success_criteria>
<output>Create `08-03-SUMMARY.md` after execution.</output>
