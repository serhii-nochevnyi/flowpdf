---
phase: FLOWPDF-08-external-reconstruction-and-ocr
plan: "02"
title: "Expose reconstruction and OCR through verified Rust/WASM workers"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - crates/flow-wasm/src/lib.rs
  - crates/flow-wasm/tests/pdf_reconstruction.rs
  - web/src/pdf/pdf-reconstruction-protocol.ts
  - web/src/pdf/pdf-reconstruction-worker.ts
  - web/src/pdf/pdf-reconstruction-worker-entry.ts
  - web/src/layout/layout-runtime.ts
  - web/tests/pdf-reconstruction.test.ts
requirements: [PDFI-04, PDFI-08, QUAL-02]
delivery:
  ticket: T-08-02
  risk: high
  human_checkpoint: false
estimate: { tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Only verified scene/source identities and bounded OCR candidates enter reconstruction; malformed or stale data fails closed."
    - "OCR provider absence, cancellation, low confidence, and adapter errors are explicit states, never successful text."
    - "Reconstruction runs in its own worker/scheduler and cannot replace a newer source or reader scene."
  artifacts:
    - "String-only WASM reconstruction/accept boundary and TypeScript validators."
    - "Cancellable reconstruction worker with an injectable OCR adapter seam and unit evidence."
  key_links:
    - "verified reader result + OCR adapter -> reconstruction worker -> verified candidate/review state"
---

<objective>Make reconstruction and OCR review data cross the existing worker boundary without leaking mutable Rust state or provider assumptions.</objective>

<context>
@.planning/phases/FLOWPDF-08-external-reconstruction-and-ocr/08-CONTEXT.md
@.planning/phases/FLOWPDF-08-external-reconstruction-and-ocr/08-PATTERNS.md
@crates/flow-wasm/src/lib.rs
@web/src/pdf/pdf-reader-worker.ts
</context>

<tasks>
  <task id="08-02-01" type="auto" tdd="true">
    <name>08-02-01: Add the verified reconstruction/OCR WASM protocol</name>
    <files>crates/flow-wasm/src/lib.rs, crates/flow-wasm/tests/pdf_reconstruction.rs, web/src/pdf/pdf-reconstruction-protocol.ts</files>
    <behavior>
      - Requests validate protocol, request ID, source hash, scene result hash, page/candidate/text budgets, and OCR confidence ranges.
      - Results carry candidate FlowDocument JSON, mappings, confidence/review items, opaque islands, report, and a result hash but no source bytes or mutable handle.
      - Accept requests require matching source/candidate identities and explicit decisions for every required review item.
    </behavior>
    <action>Expose Rust reconstruction and review acceptance as string-only WASM functions, add duplicate identity/result verification, and add closed TypeScript validators that reject unknown/malformed candidate shapes.</action>
    <acceptance_criteria>
      - Rust/WASM tests cover valid text, OCR candidates, provider errors, oversized candidates, missing decisions, stale scene hashes, and response hash mismatch.
      - The boundary never executes actions, follows links, stores OCR provider callbacks, or echoes PDF bytes in diagnostics.
    </acceptance_criteria>
    <verify><automated>cargo test --locked -p flow-wasm --test pdf_reconstruction -- --nocapture && npm run typecheck</automated></verify>
    <done>Reconstruction has a verified bounded browser protocol.</done>
  </task>
  <task id="08-02-02" type="auto" tdd="true">
    <name>08-02-02: Add a cancellable reconstruction worker and OCR adapter seam</name>
    <files>web/src/pdf/pdf-reconstruction-worker.ts, web/src/pdf/pdf-reconstruction-worker-entry.ts, web/src/layout/layout-runtime.ts, web/src/pdf/pdf-reconstruction-protocol.ts, web/tests/pdf-reconstruction.test.ts</files>
    <behavior>
      - A newer source/scene request cancels or supersedes older reconstruction and OCR work.
      - An injected OCR adapter is called only for explicitly requested scanned pages and returns bounded candidates; absent adapters yield a visible unavailable code.
      - Only the verified matching candidate is published to the panel.
    </behavior>
    <action>Mirror the reader scheduler's stale/cancel design in a separate reconstruction worker, add an adapter interface with AbortSignal, and wire generated WASM exports through the runtime without coupling it to semantic editor transactions.</action>
    <acceptance_criteria>
      - Unit tests cover text-only reconstruction, OCR adapter success/failure/unavailable, cancellation, stale source, malformed result, and review-required publication.
      - Runtime disposal terminates reconstruction independently from reader/export workers.
    </acceptance_criteria>
    <verify><automated>npm run typecheck && PLAYWRIGHT_BROWSERS_PATH=./work/playwright npx --no-install vitest run --project unit web/tests/pdf-reconstruction.test.ts</automated></verify>
    <done>The browser has an isolated, cancellable reconstruction/OCR path.</done>
  </task>
</tasks>

<verification>Run focused Rust/WASM and TypeScript/unit tests, clippy, and formatting.</verification>
<success_criteria>Verified reconstruction results and explicit OCR review state survive the worker boundary without stale publication or provider overclaim.</success_criteria>
<output>Create `08-02-SUMMARY.md` after execution.</output>
