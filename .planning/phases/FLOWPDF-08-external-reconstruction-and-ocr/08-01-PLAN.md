---
phase: FLOWPDF-08-external-reconstruction-and-ocr
plan: "01"
title: "Build the Rust-owned reconstruction candidate and source mappings"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/flow-core/src/model/mod.rs
  - crates/flow-core/src/schema/mod.rs
  - crates/flow-core/src/provenance/mod.rs
  - crates/flow-core/src/pdf/reconstruction.rs
  - crates/flow-core/src/pdf/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/pdf_reconstruction.rs
  - crates/flow-core/tests/provenance.rs
requirements: [PDFI-04, PDFI-06, QUAL-01, QUAL-02]
delivery:
  ticket: T-08-01
  risk: high
  human_checkpoint: false
estimate: { tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "A verified fixed-point scene can produce a deterministic single-column FlowDocument candidate with per-node confidence and source mapping."
    - "Ambiguous ordering and missing mappings remain explicit review/report state rather than guessed text."
    - "Unsupported scene elements remain source-bound opaque islands and external provenance distinguishes the candidate from an owned document."
  artifacts:
    - "Rust reconstruction DTOs, bounded grouping algorithm, confidence/review report, and opaque-island sidecar."
    - "Valid external-reconstruction provenance plus deterministic focused fixtures."
  key_links:
    - "PdfScene -> fixed-point grouping -> candidate FlowDocument + mappings/confidence/islands"
---

<objective>Make Phase 7 scenes convertible into an honest, reviewable semantic candidate without introducing browser-owned document semantics.</objective>

<context>
@.planning/phases/FLOWPDF-08-external-reconstruction-and-ocr/08-CONTEXT.md
@.planning/phases/FLOWPDF-08-external-reconstruction-and-ocr/08-RESEARCH.md
@.planning/phases/FLOWPDF-08-external-reconstruction-and-ocr/08-PATTERNS.md
@crates/flow-core/src/pdf/scene.rs
@crates/flow-core/src/model/mod.rs
</context>

<tasks>
  <task id="08-01-01" type="auto" tdd="true">
    <name>08-01-01: Add bounded scene reconstruction and confidence DTOs</name>
    <files>crates/flow-core/src/pdf/reconstruction.rs, crates/flow-core/src/pdf/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/pdf_reconstruction.rs</files>
    <behavior>
      - Text elements sort deterministically by page/fixed-point geometry/source identity and group into paragraphs only under declared single-column thresholds.
      - ToUnicode/simple/missing mappings and ordering uncertainty produce basis-point confidence plus closed review/report codes.
      - Paths, images, forms, links, annotations, and unsupported visual elements produce opaque-island entries with source provenance and no raw diagnostic payload.
    </behavior>
    <action>Implement private grouping helpers and closed serialized `PdfReconstructionRequest`, `PdfReconstructionResult`, mapping, confidence, review, report, and opaque-island types. Verify the incoming scene source/result identity and reject over-budget pages/elements/text before allocation.</action>
    <acceptance_criteria>
      - A two-column fixture is rejected or marked uncertain; a single-column Ukrainian/English fixture yields stable paragraph order and hashes.
      - Missing Unicode is never replaced by a guessed character and every candidate mapping points to page/rect/object/operator data.
      - The result contains no PDF bytes, URLs, scripts, or executable callbacks.
    </acceptance_criteria>
    <verify><automated>cargo test --locked -p flow-core --test pdf_reconstruction -- --nocapture</automated></verify>
    <done>Rust owns a bounded, deterministic reconstruction candidate and preservation sidecar.</done>
  </task>
  <task id="08-01-02" type="auto" tdd="true">
    <name>08-01-02: Add external provenance and candidate FlowDocument construction</name>
    <files>crates/flow-core/src/model/mod.rs, crates/flow-core/src/schema/mod.rs, crates/flow-core/src/provenance/mod.rs, crates/flow-core/tests/provenance.rs, crates/flow-core/tests/pdf_reconstruction.rs</files>
    <behavior>
      - Candidate documents validate as schema-v3 FlowDocuments with a distinct source-hash-bound external provenance lineage.
      - Stable document/style/node IDs are derived from source identity and candidate order, never from raw text or random process state.
      - Existing local/migrated documents and recovery validation remain unchanged.
    </behavior>
    <action>Add the explicit external-reconstruction provenance variant and construct bounded paragraph/heading candidates with default page settings/styles. Keep opaque islands in the source-bound reconstruction result rather than pretending they are editable semantic nodes.</action>
    <acceptance_criteria>
      - Canonical serialization/hash is deterministic for the same scene/source.
      - Provenance validation rejects empty/malformed source identities and accepts only the external variant's closed fields.
      - Existing `cargo test --locked -p flow-core` remains green.
    </acceptance_criteria>
    <verify><automated>cargo test --locked -p flow-core --test provenance -- --nocapture && cargo test --locked -p flow-core --test pdf_reconstruction -- --nocapture</automated></verify>
    <done>A reconstructed candidate is a valid FlowDocument but cannot be mistaken for an exact owned source.</done>
  </task>
</tasks>

<verification>Run focused reconstruction/provenance tests, the full flow-core suite, clippy, and formatting.</verification>
<success_criteria>Scene content becomes a deterministic, bounded, confidence/review-scored FlowDocument candidate with source mappings and opaque visual preservation.</success_criteria>
<output>Create `08-01-SUMMARY.md` after execution.</output>
