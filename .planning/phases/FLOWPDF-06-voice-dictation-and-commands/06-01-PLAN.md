---
phase: FLOWPDF-06-voice-dictation-and-commands
plan: "01"
title: "Add the Rust-owned bounded voice-intent boundary"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/flow-core/src/voice/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/voice.rs
  - crates/flow-wasm/src/lib.rs
  - crates/flow-wasm/tests/voice_exports.rs
  - tests/contracts/phase1-boundary.test.mjs
requirements: [VOIC-03, VOIC-04, VOIC-05]
delivery:
  ticket: T-06-01
  risk: medium
  human_checkpoint: false
estimate: { tokens: 9000, raw_tokens: 9000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Command-mode speech is admitted only through a versioned, bounded, locale-aware Rust allowlist."
    - "Unknown and ambiguous phrases return explicit diagnostics without a semantic mutation or transcript echo."
    - "Returned intents carry the existing command family, risk, confirmation, and undo metadata rather than inventing a browser command vocabulary."
  artifacts:
    - "Rust voice resolver module with Ukrainian and English exact phrase fixtures."
    - "String-only WASM resolver export and malformed/oversized/unsupported tests."
    - "Boundary contract admission for the new typed export."
  key_links:
    - "bounded transcript request -> Rust normalization/allowlist -> structured voice intent"
    - "existing command capability catalog -> voice risk/confirmation metadata"
objective: "Create the only semantic interpretation boundary for command-mode voice input before adding browser recognition or UI."
context:
  - .planning/phases/FLOWPDF-06-voice-dictation-and-commands/06-CONTEXT.md
  - .planning/phases/FLOWPDF-06-voice-dictation-and-commands/06-RESEARCH.md
  - crates/flow-core/src/capability/mod.rs
  - crates/flow-core/src/transaction/mod.rs
  - crates/flow-wasm/src/lib.rs
tasks:
  - id: 06-01-01
    type: auto
    tdd: true
    name: "06-01-01: Implement bounded locale-aware voice intent resolution"
    files:
      - crates/flow-core/src/voice/mod.rs
      - crates/flow-core/src/lib.rs
      - crates/flow-core/tests/voice.rs
    behavior:
      - "Normalize only bounded Unicode whitespace/case and match exact Ukrainian/English phrases."
      - "Return structured actions for history, selection editing, inline formatting, page-break editing, field navigation/clear, and explicit unsupported/ambiguous outcomes."
      - "Carry source revision/selection preconditions and voice-specific confirmation policy without mutating FlowDocument state."
    action: "Add a pure resolver DTO and table-driven tests. Reuse MutationFamily/CommandCapability metadata; never store or return the raw transcript."
    acceptance_criteria:
      - "Every supported action names a closed command family or a typed editor/form-session action."
      - "Oversized, empty, malformed, unsupported, and ambiguous inputs fail closed with stable privacy-safe codes."
      - "The resolver is deterministic for both supported locales."
    verify:
      automated: "RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-core --test voice"
    done: "Core tests prove command-mode recognition cannot mutate or guess before the browser dispatches an intent."
  - id: 06-01-02
    type: auto
    tdd: true
    name: "06-01-02: Expose and admit the string-only resolver boundary"
    files:
      - crates/flow-wasm/src/lib.rs
      - crates/flow-wasm/tests/voice_exports.rs
      - tests/contracts/phase1-boundary.test.mjs
    behavior:
      - "WASM accepts only a bounded JSON request and returns a versioned JSON response."
      - "Decode/protocol/size failures do not expose transcript content."
      - "The boundary contract recognizes the typed export without permitting mutable Rust handles."
    action: "Add resolve_voice_command(request_json: String) -> String, generated-binding smoke fixtures, and phase-boundary signature/allowlist entries."
    acceptance_criteria:
      - "Focused flow-wasm tests cover success, unsupported, ambiguous, oversized, and future-version requests."
      - "npm run typecheck remains unaffected by the new optional export."
    verify:
      automated: "RUSTUP_HOME=./work/toolchains/rustup CARGO_HOME=./work/toolchains/cargo PATH=./work/toolchains/cargo/bin:$PATH cargo test --locked -p flow-wasm --test voice_exports && node --test tests/contracts/phase1-boundary.test.mjs"
    done: "The browser has a narrow Rust-owned command-intent API and no reason to parse free-form voice itself."
verification: "Run focused Rust/WASM tests, boundary contracts, cargo fmt --all, and git diff --check."
success_criteria: "Command-mode voice interpretation is deterministic, bounded, privacy-safe, and structurally tied to the existing command catalog."
