---
phase: FLOWPDF-06-voice-dictation-and-commands
plan: "03"
title: "Add the ephemeral push-to-talk recognition state machine"
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - web/src/voice/voice-recognition.ts
  - web/src/voice/voice-protocol.ts
  - web/tests/voice-recognition.test.ts
  - web/tests/voice-recognition.browser.test.ts
requirements: [VOIC-01, VOIC-02, VOIC-03, VOIC-06]
delivery:
  ticket: T-06-03
  risk: medium
  human_checkpoint: false
estimate: { tokens: 10000, raw_tokens: 10000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Only an explicit start/stop action starts capture, and the current dictation/command mode is immutable for the active session."
    - "Interim text stays in memory and is exposed as ghost text; final segments are coalesced until stop/end."
    - "Recognition errors, unavailable constructors, aborts, repeated result indices, and permission/service failures end in bounded states."
  artifacts:
    - "Feature-detected SpeechRecognition adapter with prefixed-constructor support."
    - "Ephemeral voice state store with microphone state, mode, interim text, pending final text, and privacy-safe error codes."
    - "Fake-recognition unit/browser tests proving event ordering and one final dispatch."
  key_links:
    - "SpeechRecognition result.isFinal -> memory-only final buffer -> controller voice dispatch on stop"
    - "start/stop/abort/error/end -> accessible state transitions without persistence"
objective: "Implement the browser speech lifecycle without letting a recognition event become semantic state prematurely."
context:
  - .planning/phases/FLOWPDF-06-voice-dictation-and-commands/06-RESEARCH.md
  - .planning/phases/FLOWPDF-06-voice-dictation-and-commands/06-02-PLAN.md
  - web/src/editor/input-adapter.ts
tasks:
  - id: 06-03-01
    type: auto
    tdd: true
    name: "06-03-01: Implement feature-detected recognition lifecycle"
    files:
      - web/src/voice/voice-recognition.ts
      - web/src/voice/voice-protocol.ts
      - web/tests/voice-recognition.test.ts
    behavior:
      - "Start captures the accepted source identity and configures locale/lang, continuous, interimResults, and maxAlternatives."
      - "Result indices are monotonic/deduplicated; only final alternatives enter the final buffer."
      - "Stop finalizes; abort discards; end/error resolves once and releases handlers without retaining audio/transcript history."
    action: "Define minimal local Web Speech interfaces instead of adding an unapproved dependency. Inject the constructor and clock for deterministic tests."
    acceptance_criteria:
      - "The state machine has explicit idle/listening/stopping/preview/error/unavailable states."
      - "No call to getUserMedia, IndexedDB, fetch, analytics, or localStorage is made by the adapter."
    verify:
      automated: "npm run typecheck && npm run test:unit -- web/tests/voice-recognition.test.ts"
    done: "Recognition can be exercised deterministically without a real microphone or speech service."
  - id: 06-03-02
    type: auto
    tdd: true
    name: "06-03-02: Prove explicit mode separation in Chromium"
    files:
      - web/src/voice/voice-recognition.ts
      - web/tests/voice-recognition.browser.test.ts
    behavior:
      - "Dictation mode forwards final text only to the final-dictation path."
      - "Command mode forwards a transcript only to the Rust intent path."
      - "Changing mode while listening is rejected or requires stop; it never reclassifies buffered speech."
    action: "Use an injected fake SpeechRecognition in a real browser test to verify ghost text, mode separation, stop/abort behavior, and no raw transcript persistence hooks."
    acceptance_criteria:
      - "The test observes one voice dispatch after multiple final results and zero dispatch for interim-only/aborted sessions."
      - "The test observes an explicit unavailable/error state when the constructor is absent."
    verify:
      automated: "PLAYWRIGHT_BROWSERS_PATH=./work/playwright npx --no-install vitest run --project browser web/tests/voice-recognition.browser.test.ts"
    done: "The browser voice lifecycle visibly distinguishes dictation and command mode without semantic leakage."
verification: "Run focused unit/browser recognition tests, typecheck, and git diff --check."
success_criteria: "Push-to-talk and speech event timing are safe, deterministic, and entirely ephemeral until the controller accepts one final transaction."
