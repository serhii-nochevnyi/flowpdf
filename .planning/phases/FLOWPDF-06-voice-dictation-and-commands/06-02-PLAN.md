---
phase: FLOWPDF-06-voice-dictation-and-commands
plan: "02"
title: "Route final dictation through one revision-safe transaction"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - web/src/voice/voice-protocol.ts
  - web/src/voice/voice-command.ts
  - web/src/editor/editor-controller.ts
  - web/src/editor/editor-store.ts
  - web/tests/voice-command.test.ts
  - web/tests/editor-controller.test.ts
requirements: [VOIC-02, VOIC-04, QUAL-03]
delivery:
  ticket: T-06-02
  risk: medium
  human_checkpoint: false
estimate: { tokens: 9000, raw_tokens: 9000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "A final dictation utterance is committed as exactly one ordinary voice transaction."
    - "The captured source revision/hash and logical selection are checked before insertion; drift fails closed."
    - "Structured command intents dispatch through existing controller methods and never write canonical state directly."
  artifacts:
    - "Typed voice protocol/bridge validation and privacy-safe response checks."
    - "EditorController voice dispatch and one-transaction dictation helpers."
    - "Unit tests for accepted, stale, malformed, and unsupported voice actions."
  key_links:
    - "accepted editor snapshot -> captured voice precondition -> apply_command modality voice"
    - "Rust intent -> typed controller dispatch -> durable commit/re-query"
objective: "Connect the Rust intent boundary to the existing editor transaction service and establish the final-dictation atomicity fence."
context:
  - .planning/phases/FLOWPDF-06-voice-dictation-and-commands/06-01-PLAN.md
  - web/src/editor/editor-controller.ts
  - web/src/editor/editor-store.ts
  - web/src/forms/form-session.ts
tasks:
  - id: 06-02-01
    type: auto
    tdd: true
    name: "06-02-01: Add typed voice bridge and revision/selection preconditions"
    files:
      - web/src/voice/voice-protocol.ts
      - web/src/voice/voice-command.ts
      - web/src/editor/editor-controller.ts
      - web/tests/voice-command.test.ts
    behavior:
      - "The bridge validates protocol version, intent shape, source revision/hash, command family, risk, and confirmation policy."
      - "No bridge error includes raw transcript or persists it in a diagnostic object."
      - "Dictation payloads use existing input UTF-16/UTF-8 bounds before dispatch."
    action: "Define immutable voice session/capture DTOs, response validators, and controller-facing typed actions. Keep generated WASM optional for injected test boundaries."
    acceptance_criteria:
      - "Malformed or mismatched responses are rejected before controller dispatch."
      - "The bridge exposes no DOM, microphone, persistence, or PDF authority."
    verify:
      automated: "npm run typecheck && npm run test:unit -- web/tests/voice-command.test.ts"
    done: "Browser code can hold a safe, source-bound voice action without interpreting arbitrary speech."
  - id: 06-02-02
    type: auto
    tdd: true
    name: "06-02-02: Commit final dictation and dispatch safe history/editing actions"
    files:
      - web/src/editor/editor-controller.ts
      - web/tests/editor-controller.test.ts
      - web/tests/voice-command.test.ts
    behavior:
      - "One final utterance becomes one replaceSelection/insert transaction with SourceModality::Voice."
      - "Undo, redo, delete-selection, and formatting intents reuse existing controller command paths."
      - "A changed revision or selection produces a visible failure and no mutation."
    action: "Add explicit voice methods that capture accepted identity, call the existing enqueue/persist/publish flow, and expose a typed dispatch result for UI preview/feedback."
    acceptance_criteria:
      - "Tests prove one commit for multiple final transcript segments and no commit after stale drift."
      - "Existing UI/keyboard controller tests remain green."
    verify:
      automated: "npm run test:unit -- web/tests/editor-controller.test.ts web/tests/voice-command.test.ts"
    done: "Final dictation and initial command actions are ordinary durable, undoable voice mutations."
verification: "Run TypeScript checks, focused unit tests, and git diff --check."
success_criteria: "Voice cannot bypass the canonical command bus, and interim/final timing cannot create partial or misplaced document edits."
