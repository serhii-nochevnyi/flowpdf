---
phase: FLOWPDF-01-durable-flow-foundation
plan: "06"
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - crates/flow-wasm/src/lib.rs
  - web/persistence/indexeddb-store.ts
  - web/src/foundation-inspector.ts
  - web/src/styles.css
  - web/src/i18n/uk.ts
  - web/src/i18n/en.ts
  - web/tests/foundation-inspector.test.ts
  - web/tests/walking-skeleton.browser.test.ts
  - web/tests/recovery.browser.test.ts
  - web/tests/accessibility.browser.test.ts
  - scripts/check-phase1.mjs
  - tests/contracts/phase1-boundary.test.mjs
  - package.json
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03, FLOW-04, FLOW-05, EDIT-06, EDIT-07, QUAL-08]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "[EDGE FLOW-05/adjacency] Adjacent lineage/audit entries with equal timestamps or categories remain distinct through command/transaction/revision identity and are never visually coalesced."
    - "[EDGE FLOW-05/empty] The inspector shows an explicit no-document/zero-audit state and explicitly marks unavailable PDF preview/export provenance instead of inventing it."
    - "[EDGE FLOW-05/ordering] Revision, provenance, and audit DTOs render in deterministic durable order with stable identity tie-breakers."
    - "[EDGE FLOW-05/idempotency] Repeated inspector queries and reloads are read-only and return the same revision/hash/provenance without appending history."
    - "[EDGE FLOW-05/concurrency] While a command/save/recovery is in flight, the inspector retains the prior verified state and replaces it only from a successful exact-revision result DTO."
  prohibitions:
    - statement: "The Foundation Inspector MUST NOT fabricate PDF preview/export provenance or present Phase 1 create/migration lineage as proof that a preview or export occurred."
      status: resolved
      verification: judgment
  artifacts:
    - path: "web/src/foundation-inspector.ts"
      provides: "Complete approved local inspector interaction"
    - path: "web/tests/recovery.browser.test.ts"
      provides: "Real IndexedDB reload/failure recovery proof"
    - path: "web/tests/accessibility.browser.test.ts"
      provides: "Landmark/name/status/focus/responsive/accessibility proof"
    - path: "scripts/check-phase1.mjs"
      provides: "Deterministic full phase gate"
  key_links:
    - from: "web/src/foundation-inspector.ts"
      to: "crates/flow-wasm/src/lib.rs"
      via: "create/apply/stale/undo/redo/recover/query DTOs"
      pattern: "baseRevision|commandId|recover"
    - from: "web/persistence/indexeddb-store.ts"
      to: "web/tests/recovery.browser.test.ts"
      via: "atomic completion, reload, crash-window simulation"
      pattern: "complete|reload|recover"
    - from: "package.json"
      to: "scripts/check-phase1.mjs"
      via: "single full-suite command"
      pattern: "check"
---

<objective>
Finish the approved Foundation Inspector and close Phase 1 with real-browser recovery/accessibility evidence plus one deterministic full suite.

Purpose: Prove every user-observable Phase 1 outcome in the actual browser while guarding the locked scope and ownership boundaries.
Output: Complete localized inspector, crash/reload/recovery and accessibility tests, scope contract tests, and `npm run check` phase gate.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-CONTEXT.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-UI-SPEC.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-VALIDATION.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/SKELETON.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task id="01-06-01" type="tracer" tdd="true">
  <name>Tracer: complete browser lifecycle with conflict, undo/redo, migration, reload, and failed/successful recovery</name>
  <read_first>
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-UI-SPEC.md (Interaction Contract, Validation Expectations)
    - crates/flow-wasm/src/lib.rs
    - web/persistence/indexeddb-store.ts
    - web/src/foundation-inspector.ts
  </read_first>
  <files>crates/flow-wasm/src/lib.rs, web/persistence/indexeddb-store.ts, web/src/foundation-inspector.ts, web/tests/walking-skeleton.browser.test.ts, web/tests/recovery.browser.test.ts</files>
  <behavior>
    - Real Chromium creates the sample; applies a typed change; submits a stale command with unchanged state; undoes/redoes with new revisions; saves only after completion; reloads; and recovers the same last durable hash.
    - Opening the supported older fixture migrates through Rust, commits a current snapshot boundary, and displays migration provenance without replaying incompatible old operations.
    - Interrupted/aborted commit, corrupt hash, revision gap, and conflicting duplicate produce visible safe diagnostics and retain the prior verified display/store state.
  </behavior>
  <action>Extend the production tracer using the existing D-05/D-12 contracts: WASM exports only typed create/open/apply/undo/redo/commit-record/recover/query DTO functions; TypeScript performs physical IndexedDB batch operations and platform-error translation; the inspector sends fresh command IDs with displayed base revisions and replaces UI state only from successful exact-revision DTOs. Add real-browser controls and tests for create/open-last, test mutation, deliberate stale command, undo, redo, save, reload, recover, and older-schema open. Simulate worker/page interruption by aborting/incompletely enqueuing the physical transaction and reconstructing the page session. Keep all D-07 failure paths non-mutating and all recovery validation/redaction in Rust.</action>
  <acceptance_criteria>
    - Every UI action crosses the same typed command or store DTO boundary; there is no direct semantic state edit in TypeScript.
    - Real Chromium proves both last-durable recovery and visible fail-closed diagnostics for gap/hash/conflict/abort cases.
    - Migration provenance identifies the supported source/current schema while export provenance remains explicitly unavailable.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:wasm &amp;&amp; npm run test:browser -- walking-skeleton recovery</automated>
  </verify>
  <done>The full Phase 1 lifecycle—including negative conflict/recovery paths—works through real WASM and IndexedDB in a fresh Chromium session.</done>
</task>

<task id="01-06-02" type="auto" tdd="true">
  <name>Implement the approved localized, responsive, privacy-safe inspector contract</name>
  <read_first>
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-UI-SPEC.md (entire approved contract)
    - web/src/foundation-inspector.ts
    - web/src/styles.css
  </read_first>
  <files>web/src/foundation-inspector.ts, web/src/styles.css, web/src/i18n/uk.ts, web/src/i18n/en.ts, web/tests/foundation-inspector.test.ts</files>
  <behavior>
    - Empty/loading/error/populated/partial/overflow/zero-one-many/long-text states follow the approved Ukrainian-default copy and English-key parity.
    - Native buttons, disabled history semantics, visible focus, polite completion, alert failures, semantic landmarks/table/time, full accessible IDs/hashes, and Ctrl/Cmd undo/redo operate without intercepting text-input shortcuts.
    - Audit DOM/accessibility output renders allowlisted DTO fields only; forbidden sentinel text/audio/transcript/argument/storage values are absent.
  </behavior>
  <action>Implement the complete approved `01-UI-SPEC.md` without expanding product scope. Use the exact header/session/command/history/durability/inspector/status inventory, Ukrainian keys and parameterized copy with key-identical English resources, system typography, approved colors/spacing, 44px targets, two-column ≥960px and stacked narrow layout, 320px overflow handling, wrap-safe long copy, copy controls for full IDs/hashes, reduced-motion behavior, and text-first state. Render audit as a semantic table/ordered narrow representation with timestamps and stable source order. Use safe text assignment, never HTML injection or hidden debug dumps. Expose unavailable preview/export provenance verbatim as a phase boundary, not as an error or inferred lineage.</action>
  <acceptance_criteria>
    - Unit DOM tests cover all state categories, localization key parity, history availability, stale failure atomicity, and audit-field allowlist.
    - No label is icon-only, color-only, below 14px, clipped at 320px, or missing a 44px target/focus state.
    - Full identifiers are programmatically accessible even when visually truncated.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- foundation-inspector &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>The complete approved Foundation Inspector contract is localized, responsive, accessible, and contains no semantic editing or sensitive audit surface.</done>
</task>

<task id="01-06-03" type="auto" tdd="true">
  <name>Close Phase 1 with real-browser accessibility and deterministic scope/full-suite gates</name>
  <read_first>
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-VALIDATION.md
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/SKELETON.md (Out of Scope, Walking-Skeleton Proof)
    - package.json
    - vitest.config.ts
  </read_first>
  <files>web/tests/accessibility.browser.test.ts, scripts/check-phase1.mjs, tests/contracts/phase1-boundary.test.mjs, package.json</files>
  <behavior>
    - Chromium asserts landmarks, button names, focus order/return, live/alert roles, disabled semantics, keyboard parity, full hash access, audit redaction, and no clipping/overlap at 320px and 1280px in Ukrainian and English.
    - Boundary tests fail if Phase 1 adds rich-text/contenteditable/canvas/layout/PDF/form UI/voice/backend/auth/collaboration/ORM/schema-push/React runtime surfaces or lets TypeScript own semantic mutation/recovery/redaction.
    - One non-watch command validates format, Clippy warnings-as-errors, all Rust/golden/property tests, WASM target/build, dependency locks, TypeScript, Vitest unit, and real Chromium suites twice where determinism is asserted.
  </behavior>
  <action>Add the final real-browser accessibility/responsive proof using DOM/accessibility assertions and screenshots only as supplemental diagnostics. Implement a structural Phase 1 boundary contract over manifests and source imports/exports so deferred subsystems and forbidden ownership do not enter this phase; scope negative checks to source regions or AST/import semantics rather than brittle whole-file prose greps. Create `scripts/check-phase1.mjs` as a fail-fast orchestrator that preserves subprocess exit codes and runs: dependency/lock verification, `cargo fmt --check`, Clippy all targets with warnings denied, full Rust tests including golden/property/recovery/redaction, `node scripts/verify-recovery-benchmark.mjs` (which requires exactly one valid passing report and no blocker), WASM target check and bindgen build, TypeScript typecheck, unit tests, walking-skeleton/recovery/accessibility Chromium tests, and deterministic canonical/migration replay. Wire `npm run check` to it and print measured focused/full runtimes without asserting fabricated counts.</action>
  <acceptance_criteria>
    - The accessibility browser suite proves the approved behavior at both target widths/locales and uses the real core/store path.
    - Boundary fixtures prove the contract test detects representative Rust/TypeScript ownership and deferred-scope violations.
    - `npm run check` is non-watch, fail-fast, fully deterministic, green from a clean `npm ci --ignore-scripts` plus locked Cargo state, and cannot pass with a missing/failed/ambiguous recovery benchmark artifact.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- accessibility &amp;&amp; node --test tests/contracts/phase1-boundary.test.mjs &amp;&amp; npm run check</automated>
    <human-check>At end-of-phase only: visually inspect the final local page in current Chrome/Edge at 320px and 1280px against 01-UI-SPEC.md; automated execution must not pause for this supplemental check.</human-check>
  </verify>
  <done>Real-browser recovery/accessibility and the complete deterministic Phase 1 suite are green, with deferred-scope and ownership contracts enforced.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- ASVS level: L1
- Blocking threshold: high

## Trust Boundaries

| Boundary | Description |
|---|---|
| DOM control/event → WASM command | Browser events and displayed revisions can be stale or manipulated. |
| IndexedDB/platform error → inspector state | Partial/corrupt records or misleading completion can produce false durability. |
| Audit/provenance DTO → DOM/accessibility tree | Rendering can leak sensitive payloads or fabricate lineage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | Inspector command dispatch | high | mitigate | Task 01-06-01 sends explicit command IDs/base revisions to the shared Rust validator and updates only from successful result DTOs. |
| T-06-02 | Tampering / Denial of Service | Browser commit/recovery | high | mitigate | Task 01-06-01 real-browser tests abort/gap/hash/conflict cases and retains prior verified state on every failure. |
| T-06-03 | Information Disclosure | Audit DOM/accessibility output | high | mitigate | Tasks 01-06-02/03 render allowlisted safe text only and negative-test sensitive sentinels across DOM and accessibility output. |
| T-06-04 | Spoofing | Provenance presentation | medium | mitigate | Task 01-06-01/02 binds lineage to exact revision DTOs and explicitly marks unavailable export provenance. |
| T-06-05 | Elevation of Privilege | Deferred subsystem drift | low | accept | There is no privileged remote surface; Task 01-06-03 structurally blocks auth/backend/active-PDF/voice/editor scope from entering Phase 1. |
</threat_model>

<verification>
Run each task command, then install from lockfiles in a clean workspace and run `npm run check`. The final browser tests must use actual Chromium and IndexedDB, not only fake-indexeddb. Review full-suite output for skipped tests, watch mode, retries masking deterministic failures, or fabricated numeric assertions.
</verification>

<success_criteria>
- All eight Phase 1 requirements and all five FLOW-05 edge truths are observable in the real browser.
- The approved UI/accessibility contract and locked Phase 1 boundary pass automated structural and browser checks.
- The descriptor-less provenance prohibition remains explicitly flagged-unverified for verifier judgment.
- One deterministic non-watch full suite is the phase completion gate.
</success_criteria>

<output>
Create `.planning/phases/FLOWPDF-01-durable-flow-foundation/01-06-SUMMARY.md` when done.
</output>
