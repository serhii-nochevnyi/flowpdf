---
phase: FLOWPDF-01-durable-flow-foundation
plan: "03"
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - crates/flow-core/src/lib.rs
  - crates/flow-core/src/model/mod.rs
  - crates/flow-core/src/canonical/mod.rs
  - crates/flow-core/src/schema/mod.rs
  - crates/flow-core/tests/schema.rs
  - crates/flow-core/tests/persistence_round_trip.rs
  - crates/flow-core/tests/migration_golden.rs
  - fixtures/flowdoc/current.json
  - fixtures/flowdoc/current.hash
  - fixtures/flowdoc/older.json
  - fixtures/flowdoc/migrated.json
  - fixtures/flowdoc/migrated.hash
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "[EDGE FLOW-01/adjacency] Equal or neighboring semantic elements remain distinct ordered nodes; duplicate stable IDs are rejected rather than merged."
    - "[EDGE FLOW-01/empty] Required collections accept and preserve empty/singleton values, while null or omitted required collections fail with a structured schema error."
    - "[EDGE FLOW-01/encoding] User text is preserved without Unicode normalization; canonical storage is UTF-8 and public position lengths are explicitly UTF-16 rather than Rust byte offsets."
    - "[EDGE FLOW-01/ordering] Ordered vectors preserve semantic order and unavoidable map keys use deterministic sorted ordering when canonical bytes are emitted."
    - "[EDGE FLOW-01/idempotency] Constructing the same explicit IDs and values produces identical canonical bytes/hash; an identical stored identity is idempotent and a conflicting duplicate is rejected."
    - "[EDGE FLOW-01/concurrency] Concurrent creation for one document identity cannot partially overwrite state; identical records converge and divergent records conflict atomically."
    - "[EDGE FLOW-03/idempotency] Migrating the same old fixture always yields the same current bytes/hash, and applying the registry to a current document is an exact no-op."
    - "[EDGE FLOW-03/concurrency] An interrupted or concurrent migration never publishes a partial schema hop; only a fully validated current snapshot becomes a replay boundary."
  artifacts:
    - path: "crates/flow-core/src/model/mod.rs"
      provides: "Typed canonical FlowDocument, stable IDs, asset and field descriptors"
    - path: "crates/flow-core/src/canonical/mod.rs"
      provides: "Single deterministic byte and versioned hash path"
    - path: "crates/flow-core/src/schema/mod.rs"
      provides: "Bounded validation and pure sequential migration registry"
    - path: "fixtures/flowdoc/older.json"
      provides: "Supported older-schema golden input"
  key_links:
    - from: "crates/flow-core/src/model/mod.rs"
      to: "crates/flow-core/src/canonical/mod.rs"
      via: "typed serialization only"
      pattern: "FlowDocument"
    - from: "crates/flow-core/src/schema/mod.rs"
      to: "fixtures/flowdoc/migrated.hash"
      via: "sequential migration golden assertion"
      pattern: "migrat"
---

<objective>
Expand the tracer into the complete canonical FlowDocument and deterministic migration contract.

Purpose: Establish the durable semantic bytes every transaction, browser store, owned PDF source payload, and later phase will consume.
Output: Typed model, bounded validation, canonical serializer/hash, asset descriptors, pure migrations, and Ukrainian/English golden fixtures.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-CONTEXT.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-RESEARCH.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-PATTERNS.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task id="01-03-01" type="tracer" tdd="true">
  <name>Tracer: full schema sample → validate → canonical bytes/hash → decode equality</name>
  <reversibility rating="costly">D-01 defines the durable canonical representation consumed by stored documents and later owned-PDF payloads.</reversibility>
  <read_first>
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-RESEARCH.md (Canonical document contract)
    - crates/flow-core/src/lib.rs
  </read_first>
  <files>crates/flow-core/src/model/mod.rs, crates/flow-core/src/canonical/mod.rs, crates/flow-core/src/schema/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/schema.rs</files>
  <behavior>
    - RED first: a complete Ukrainian/English document containing page settings, locale, styles, ordered content, asset descriptor, field descriptor, provenance, IDs, and revision validates and round-trips through one canonical byte path.
    - Equal typed documents produce byte-identical compact UTF-8 and identical versioned BLAKE3 hash envelopes across repeated native runs.
    - Page/PDF/DOM coordinates, JSON floats, invalid identifiers, duplicate IDs, dangling references, unsupported versions, or null required collections are rejected before publication.
  </behavior>
  <action>Extract the walking-skeleton structs into explicit `model`, `schema`, and `canonical` modules without changing its external behavior. Implement D-01 with typed finite integer/fixed-point domain values, opaque UUID-compatible ID newtypes, declaration-ordered structs, `Vec` semantic order, and `BTreeMap` only where a map is unavoidable. Implement D-02 asset descriptors with content hash/metadata and no repeated bytes. Implement the exact schema-v1 field vocabulary from Research R-01: `FieldDescriptor { id, name, label, anchor, kind, required, read_only, default_value, options }`; closed `FieldKind` variants Text (multiline plus Plain/Date/Number/Email hint), Checkbox, RadioGroup, Select (multiple), Signature, Button; typed Empty/Text/Checked/Selected values; and stable option IDs/labels/export values with kind/value/reference constraints. Do not add an arbitrary property bag, widget/PDF coordinates, JavaScript actions, signature bytes, or validation expressions. Keep package/binary forms derived per D-03. Expose one compact canonical UTF-8 serializer and a versioned BLAKE3 hash envelope; document that it is equality/integrity evidence, not authentication. Validate all cross-references and limits before returning a document.</action>
  <acceptance_criteria>
    - The full representative document round-trips with structural equality and identical canonical bytes/hash.
    - Ukrainian combining marks, apostrophes, emoji, mixed Latin, and non-BMP text retain exact scalar/byte content without a shaping or grapheme claim.
    - Every v1 field kind/value/options combination has a valid fixture and incompatible values/options fail with stable codes; no deferred Phase 5/PDF behavior enters the schema.
    - Forbidden coordinate/float/unordered/dangling forms fail with stable machine codes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p flow-core schema -- --nocapture</automated>
  </verify>
  <done>The complete Phase 1 FlowDocument has one deterministic, validated, Rust-owned canonical representation implementing D-01 through D-03 and FLOW-01.</done>
</task>

<task id="01-03-02" type="auto" tdd="true">
  <name>Expand round-trip, asset resolution, edge, and resource-limit contracts</name>
  <read_first>
    - crates/flow-core/src/model/mod.rs
    - crates/flow-core/src/canonical/mod.rs
    - crates/flow-core/src/schema/mod.rs
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-RESEARCH.md (Required fixtures and properties, Security limits)
  </read_first>
  <files>crates/flow-core/src/model/mod.rs, crates/flow-core/src/canonical/mod.rs, crates/flow-core/tests/persistence_round_trip.rs, fixtures/flowdoc/current.json, fixtures/flowdoc/current.hash</files>
  <behavior>
    - Empty, singleton, repeated-equal, reordered, and maximum-accepted collections round-trip according to the eight FLOW-01/FLOW-02 edge truths.
    - Asset descriptors round-trip canonically and resolve separate bytes whose computed content hash must match; mismatches fail without changing the document.
    - Inputs at every checked `DocumentLimits` ceiling pass and inputs one unit beyond fail before unbounded allocation or semantic publication.
  </behavior>
  <action>Create a checked-in current golden fixture and expected hash. Add table/property tests for collection adjacency/ordering/idempotency, exact Unicode preservation, duplicate identities, and canonical re-encoding. Implement the versioned `DocumentLimits::V1` profile fixed by Research R-02: 64 MiB canonical bytes, depth 128, 200,000 nodes, 32 MiB total text, 4 MiB per text node, 4,096 styles, 10,000 asset descriptors, 10,000 field descriptors, 10,000 operations per transaction, 8 MiB per transaction record, and recovery ceilings of 10,000 records / 64 MiB after a snapshot. Check sizes before semantic publication/unbounded allocation and assert every ceiling at N and N+1. Record fixture sizes/costs as evidence, but do not silently raise the locked caps. Implement the asset resolver contract as hash-checked separate bytes, not embedded canonical content.</action>
  <acceptance_criteria>
    - Checked-in current bytes/hash are stable across repeated runs and save/reopen structural equality includes every semantic/style/field/asset descriptor.
    - Asset byte corruption is detected by hash and the canonical document remains unchanged.
    - Every fixed R-02 decode/collection/recovery limit has a passing N boundary, failing N+1 boundary, and stable structured over-limit code; measurements cannot mutate the profile.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p flow-core persistence_round_trip -- --nocapture</automated>
  </verify>
  <done>FLOW-01/FLOW-02 round-trip and resource boundaries are deterministic, golden-backed, and preserve separately stored asset identity.</done>
</task>

<task id="01-03-03" type="auto" tdd="true">
  <name>Implement the pure sequential migration registry and golden replay boundary</name>
  <reversibility rating="one-way">D-04 migration hops become the permanent compatibility path for documents already stored at older schema versions; the decision is locked and must not be re-asked.</reversibility>
  <read_first>
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-CONTEXT.md (D-04)
    - crates/flow-core/src/schema/mod.rs
    - fixtures/flowdoc/current.json
  </read_first>
  <files>crates/flow-core/src/schema/mod.rs, crates/flow-core/tests/migration_golden.rs, fixtures/flowdoc/older.json, fixtures/flowdoc/migrated.json, fixtures/flowdoc/migrated.hash</files>
  <behavior>
    - RED first: every supported older version walks exactly one version per pure hop, validates after each hop, and yields the checked-in current fixture/hash.
    - Unknown future versions, missing hops, nondeterministic output, and invalid intermediate documents fail explicitly without publishing a partially migrated document.
    - Current-version migration is an exact idempotent no-op; two runs and native/WASM callers receive identical bytes/hash and provenance.
  </behavior>
  <action>Implement D-04 as an ordered registry of pure `from_version → to_version` functions with no clock, randomness, I/O, global mutation, or skipped version. Validate the input and every intermediate result. Emit migration provenance that identifies source/current schema and hop sequence. Define successful migration as a new current-schema snapshot/replay boundary while preserving older records for inspection; replay of old-schema operations under changed semantics remains prohibited. Add golden original/migrated bytes and expected hash fixtures, future-version/missing-hop failures, idempotency, and interruption-before-publication tests.</action>
  <acceptance_criteria>
    - Every supported path and each intermediate hop is golden-covered and deterministic.
    - Future/unknown/missing/invalid paths return stable errors with no partial document.
    - Migration provenance and fresh replay-boundary intent are explicit and inspectable.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p flow-core migration_golden -- --nocapture &amp;&amp; cargo check -p flow-wasm --target wasm32-unknown-unknown</automated>
  </verify>
  <done>FLOW-03 and D-04 are implemented as a deterministic, golden, sequential compatibility contract.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- ASVS level: L1
- Blocking threshold: high

## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted JSON/WASM bytes → typed FlowDocument | Bytes may be oversized, malformed, duplicated, inconsistent, or future-versioned. |
| Asset bytes → content-hash resolver | Descriptor identity can be paired with corrupt or substituted bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Tampering | Canonical decode/hash | high | mitigate | Tasks 01-03-01/02 validate typed invariants, cross-references, exact canonical re-encoding, and versioned hash before publication. |
| T-03-02 | Denial of Service | JSON/model decode | high | mitigate | Task 01-03-02 enforces measured record/nesting/node/text/asset/field bounds with N/N+1 tests before allocation growth. |
| T-03-03 | Tampering | Migration registry | high | mitigate | Task 01-03-03 permits contiguous pure hops only and validates/golden-checks every intermediate document. |
| T-03-04 | Spoofing | BLAKE3 hash interpretation | low | accept | Hash metadata is explicitly an equality/integrity diagnostic and makes no authentication or attacker-proof identity claim. |
</threat_model>

<verification>
Run the three focused commands, then `cargo test -p flow-core schema persistence_round_trip migration_golden` and the WASM target check. Diff regenerated golden bytes against the checked-in fixtures; unexplained drift fails.
</verification>

<success_criteria>
- FLOW-01/FLOW-02/FLOW-03 and D-01 through D-04 are fully covered.
- All eight surfaced FLOW-01/FLOW-03 edge truths have explicit passing evidence.
- Canonical bytes and migrations are deterministic across repeated native/WASM builds.
</success_criteria>

<output>
Create `.planning/phases/FLOWPDF-01-durable-flow-foundation/01-03-SUMMARY.md` when done.
</output>
