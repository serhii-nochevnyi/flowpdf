---
phase: FLOWPDF-01-durable-flow-foundation
plan: "05"
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - crates/flow-core/src/lib.rs
  - crates/flow-core/src/store/mod.rs
  - crates/flow-core/src/audit/mod.rs
  - crates/flow-core/src/provenance/mod.rs
  - crates/flow-core/tests/recovery_tracer.rs
  - crates/flow-core/tests/persistence_round_trip.rs
  - crates/flow-core/tests/recovery.rs
  - crates/flow-core/tests/audit_redaction.rs
  - crates/flow-core/tests/provenance.rs
  - fixtures/recovery/cases.json
  - fixtures/recovery/benchmark-200-page.recipe.json
  - crates/flow-core/examples/recovery_benchmark.rs
  - scripts/verify-recovery-benchmark.mjs
  - artifacts/benchmarks/phase1-recovery.json
  - artifacts/benchmarks/phase1-recovery-blocker.json
autonomous: true
requirements: [FLOW-02, FLOW-03, FLOW-04, FLOW-05, QUAL-08]
estimate:
  tokens: 49000
  raw_tokens: 49000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "[EDGE FLOW-02/adjacency] Neighboring equal semantic records remain distinct by stable identity; a duplicate identity with divergent bytes is corruption, not a merge."
    - "[EDGE FLOW-02/empty] Required empty collections survive save/reopen exactly, singleton values stay singleton, and null required values fail validation."
    - "[EDGE FLOW-02/encoding] Save/reopen preserves exact user text without normalization and verifies separately stored asset bytes against their content hashes."
    - "[EDGE FLOW-02/ordering] Save/reopen preserves semantic/style/field/asset order and reproduces identical canonical bytes/hash."
    - "[EDGE FLOW-02/idempotency] Saving the same revision/hash repeatedly is idempotent and does not create a divergent transaction or audit duplicate."
    - "[EDGE FLOW-02/concurrency] Concurrent saves cannot acknowledge partial state; one completed atomic commit wins and a divergent same-identity record conflicts."
    - "[EDGE FLOW-04/idempotency] Repeated recovery from unchanged storage yields the same revision/hash/history/audit projection and does not append or reapply records."
    - "[EDGE FLOW-04/concurrency] Recovery concurrent with an incomplete browser write sees only the last completed IndexedDB transaction and never a partial newer revision."
    - "[EDGE QUAL-08/adjacency] Adjacent same-type audit events remain distinct by command/transaction/revision identity and are not coalesced."
    - "[EDGE QUAL-08/empty] Before a document is open the inspector has an explicit zero state; creation produces exactly one redacted creation event and subsequent queries use an empty array rather than null when no further events exist."
    - "[EDGE QUAL-08/encoding] Audit equality is defined over typed allowlisted scalar fields and deterministic UTF-8 serialization; document text and transcript/audio payloads are not audit fields."
    - "[EDGE QUAL-08/ordering] Audit events are chronological by committed revision/durable sequence with a stable identity tie-breaker, independent of equal timestamps."
    - "[EDGE QUAL-08/idempotency] Re-querying audit is read-only and replaying an already-applied identical transaction does not duplicate its audit event."
    - "[EDGE QUAL-08/concurrency] Snapshot, transaction, and corresponding success audit records commit atomically so no audit success exists without its durable mutation or vice versa."
  prohibitions:
    - statement: "A revision MUST NOT be labelled recovered or durable unless its snapshot hash, schema, identity, and contiguous revision chain have all been verified; a corrupt or gapped history remains visibly failed."
      status: resolved
      verification: judgment
    - statement: "Default audit, analytics, inspector, and accessibility output MUST NOT retain or expose document text, command arguments, raw microphone audio, or raw voice transcripts."
      status: resolved
      verification: test
  artifacts:
    - path: "crates/flow-core/src/store/mod.rs"
      provides: "DocumentStore record port and verified recovery/replay policy"
    - path: "crates/flow-core/src/audit/mod.rs"
      provides: "Allowlisted redacted audit records"
    - path: "crates/flow-core/src/provenance/mod.rs"
      provides: "Revision/create/migration provenance DTOs"
    - path: "fixtures/recovery/cases.json"
      provides: "Crash, gap, corruption, duplicate, and cadence cases"
    - path: "scripts/verify-recovery-benchmark.mjs"
      provides: "Fail-closed recovery benchmark runner and report/blocker validator"
    - path: "artifacts/benchmarks/phase1-recovery.json"
      provides: "Passing p50/p95 recovery evidence; mutually exclusive with blocker artifact"
    - path: "artifacts/benchmarks/phase1-recovery-blocker.json"
      provides: "Fail-closed terminal evidence when no permitted snapshot cadence meets the p95 target; mutually exclusive with passing report"
  key_links:
    - from: "crates/flow-core/src/transaction/mod.rs"
      to: "crates/flow-core/src/store/mod.rs"
      via: "canonical commit records and revision links"
      pattern: "Transaction|Commit"
    - from: "crates/flow-core/src/store/mod.rs"
      to: "crates/flow-core/src/audit/mod.rs"
      via: "atomic commit record set"
      pattern: "Audit"
---

<objective>
Complete Rust-owned durability, recovery, provenance, and privacy-minimized audit semantics behind the already-proven browser adapter.

Purpose: Make refresh, worker failure, corruption, migration, replay, and audit behavior deterministic without letting IndexedDB define semantic truth.
Output: DocumentStore port/records, bounded verified replay, policy-controlled snapshots, provenance/audit DTOs, and crash/corruption fixtures.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-CONTEXT.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-RESEARCH.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-PATTERNS.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task id="01-05-01" type="tracer" tdd="true">
  <name>Tracer: commit one snapshot/log/audit set and recover its exact immutable revision</name>
  <read_first>
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-CONTEXT.md (D-12, D-13, D-15)
    - crates/flow-core/src/transaction/mod.rs
    - crates/flow-core/src/canonical/mod.rs
    - web/persistence/indexeddb-store.ts
  </read_first>
  <files>crates/flow-core/src/store/mod.rs, crates/flow-core/src/audit/mod.rs, crates/flow-core/src/provenance/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/recovery_tracer.rs</files>
  <behavior>
    - RED first: commit a verified snapshot, one immutable transaction, and its redacted success audit through an in-memory DocumentStore adapter, reconstruct a fresh session, and recover identical revision/bytes/hash/history/provenance.
    - Snapshot/log/audit DTO contracts are Rust-owned; a physical adapter can persist bytes but cannot select, validate, replay, redact, or repair them.
    - The audit event contains IDs, revision links, timestamp, command type, modality, outcome, safe code, and allowlisted metadata only.
  </behavior>
  <action>Implement D-12 record/port contracts for snapshots, append-only transactions, separately stored assets, audits, and atomic commit sets. Implement Rust-owned recovery that validates `DocumentLimits`, record format/schema, document identity, canonical bytes/hash, base/new revision, transaction identity, and compatibility before applying one record. Implement create/migration/current-revision provenance DTOs and D-15 audit construction from an allowlist rather than arbitrary metadata maps. Refactor the browser tracer DTOs to use these core contracts without changing TypeScript ownership. Treat persisted bytes as untrusted and make all failures structured/non-publishing.</action>
  <acceptance_criteria>
    - Fresh-session recovery equals the committed immutable revision across every canonical and history field.
    - Store adapters have physical I/O interfaces only; recovery/redaction/provenance policy compiles only in Rust core.
    - The tracer audit serialization has no semantic payload or command arguments.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p flow-core recovery_tracer -- --nocapture &amp;&amp; cargo check -p flow-wasm --target wasm32-unknown-unknown</automated>
  </verify>
  <done>D-12/D-13/D-15 have a real end-to-end core commit/recovery path and adapter-neutral record contract.</done>
</task>

<task id="01-05-02" type="auto" tdd="true">
  <name>Verify contiguous idempotent replay across crash, corruption, migration, and snapshot-cadence cases</name>
  <read_first>
    - crates/flow-core/src/store/mod.rs
    - crates/flow-core/src/schema/mod.rs
    - crates/flow-core/tests/recovery_tracer.rs
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-RESEARCH.md (Persistence and recovery contract)
  </read_first>
  <files>crates/flow-core/src/store/mod.rs, crates/flow-core/tests/persistence_round_trip.rs, crates/flow-core/tests/recovery.rs, fixtures/recovery/cases.json, fixtures/recovery/benchmark-200-page.recipe.json, crates/flow-core/examples/recovery_benchmark.rs, scripts/verify-recovery-benchmark.mjs, artifacts/benchmarks/phase1-recovery.json, artifacts/benchmarks/phase1-recovery-blocker.json</files>
  <behavior>
    - Recovery selects the newest valid snapshot and replays only a bounded contiguous schema-compatible revision chain.
    - An identical already-applied record is skipped idempotently; hash mismatch, revision gap, conflicting duplicate, wrong document, incompatible schema, or over-budget log stops at a structured diagnostic and publishes no guessed state.
    - Crash points before completion, between logical record requests, after log, after snapshot, and after audit expose only the last atomic durable prefix for every tested snapshot policy.
  </behavior>
  <action>Implement D-13 recovery selection/replay and D-14 a configurable snapshot policy whose value never changes correctness. Use the locked Research R-02 default: snapshot on explicit local save, after migration, or after 100 committed transactions / 4 MiB uncheckpointed transaction bytes, whichever occurs first; hard-stop replay above 10,000 records or 64 MiB. Run correctness fixtures under no-intermediate, every-transaction, and periodic policies. Add the checked-in deterministic `benchmark-200-page.recipe.json`, a Rust example that performs 3 warm-ups plus 20 measured recoveries of its generated 200-page-equivalent document and 1,000-transaction history, and a Node runner/validator. Try the default then only lower cadence through 50/2 MiB, 25/1 MiB, and 10/1 MiB. On the first p95 below 2,000 ms, atomically write `phase1-recovery.json` with toolchain/browser/fixture hash, attempts, p50/p95, selected policy, and `passed:true`, while ensuring no blocker exists. If all attempts miss, atomically write only `phase1-recovery-blocker.json` with the same measurements and `blocked:true`, exit nonzero, and do not seal the phase. The validator fails if both artifacts exist, neither exists after a run, fields/fixture hash are malformed, or a success report has p95 at/above threshold. Never raise hard caps, skip checks, or publish partial state. After migration, select the new current-schema snapshot boundary and keep older records inspectable without replaying incompatible operations. Add a table fixture for valid, repeated-identical, gap, conflict, corrupt hash/bytes, wrong identity, unknown schema, interrupted atomic commit, and bounded-work cases.</action>
  <acceptance_criteria>
    - Save/reopen preserves all semantic/style/field/asset descriptors and exact canonical hash.
    - Every corrupt/gap/conflict/over-budget fixture returns its stable diagnostic with no partial publication.
    - All cadence policies recover identical final truth; `node scripts/verify-recovery-benchmark.mjs` proves exactly one valid passing report or failing blocker, and an unmet minimum target exits nonzero rather than weakening R-02.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p flow-core persistence_round_trip recovery -- --nocapture &amp;&amp; node scripts/verify-recovery-benchmark.mjs</automated>
  </verify>
  <done>FLOW-02/FLOW-04, D-13/D-14, and every save/recovery edge truth are deterministic across snapshot policies and crash windows.</done>
</task>

<task id="01-05-03" type="auto" tdd="true">
  <name>Prove revision provenance and audit privacy/ordering under success, failure, replay, and concurrency</name>
  <read_first>
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-CONTEXT.md (D-15)
    - crates/flow-core/src/audit/mod.rs
    - crates/flow-core/src/provenance/mod.rs
    - crates/flow-core/src/store/mod.rs
  </read_first>
  <files>crates/flow-core/src/audit/mod.rs, crates/flow-core/src/provenance/mod.rs, crates/flow-core/tests/audit_redaction.rs, crates/flow-core/tests/provenance.rs</files>
  <behavior>
    - Create, migration, command success, stale/invalid failure, undo, redo, and recovery expose concise typed revision links and safe outcome codes in deterministic order.
    - Serialization and debug/display surfaces exclude document text, command arguments, storage payloads, raw audio, transcripts, and unknown arbitrary metadata keys.
    - Equal timestamps retain stable durable sequence/identity order; repeated query/replay does not duplicate events; atomic commit never splits mutation and success audit.
  </behavior>
  <action>Finish D-15 by making audit metadata a closed enum/typed struct projection with field-level allowlisting and explicit redacted markers; do not accept arbitrary JSON blobs. Test forbidden sentinel values and key names through serialization, Debug/Display, WASM DTO, and error paths so sensitive values cannot reappear via diagnostics. Implement FLOW-05 provenance for exact revision, creation/migration lineage, engine/schema identity, and source hashes; expose the absence of preview/export provenance rather than inventing it. Sort audits by committed durable sequence/revision with identity tie-breaker, and test zero/create/one/many, repeated query, identical replay, failure, and concurrent outcomes.</action>
  <acceptance_criteria>
    - Provenance identifies exactly the document revision and available create/migration lineage.
    - Every prohibited audit sentinel/key is absent from serialized, debug, WASM, and error output while safe IDs/codes remain inspectable.
    - All six QUAL-08 edge truths pass for zero/create/one/many, equal timestamps, replay, and concurrent commits.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p flow-core audit_redaction provenance -- --nocapture</automated>
  </verify>
  <done>FLOW-05/QUAL-08 and D-15 provide inspectable lineage/audit without a shadow semantic or voice-data store.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- ASVS level: L1
- Blocking threshold: high

## Trust Boundaries

| Boundary | Description |
|---|---|
| IndexedDB/native store records → Rust recovery | Persisted bytes, identities, revisions, and hashes are hostile until verified. |
| Command/transaction data → audit projection | Recovery-required semantic content must not leak into audit/analytics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Tampering | Snapshot/log replay | high | mitigate | Tasks 01-05-01/02 verify size/schema/hash/identity/revision continuity and publish only a bounded contiguous valid chain. |
| T-05-02 | Denial of Service | Recovery scan/decode | high | mitigate | Task 01-05-02 applies measured record/log/replay budgets and fails at the first over-budget or corrupt record. |
| T-05-03 | Information Disclosure | Audit DTO/diagnostics | high | mitigate | Tasks 01-05-01/03 construct closed allowlisted records and negative-test serialization, debug, WASM, and error outputs. |
| T-05-04 | Repudiation | Revision/provenance links | medium | mitigate | Task 01-05-03 records immutable command/transaction/revision/schema lineage and safe outcome codes in durable order. |
</threat_model>

<verification>
Run every focused command plus `cargo test -p flow-core persistence_round_trip recovery provenance audit_redaction`. Re-run the Plan 02 walking-skeleton browser test after the DTO refactor.
</verification>

<success_criteria>
- FLOW-02/FLOW-04/FLOW-05/QUAL-08 and D-12 through D-15 are implemented in Rust core.
- All fourteen save/recovery/audit edge truths have explicit automated evidence.
- Both fallback prohibitions remain descriptor-less and fail closed as flagged-unverified rather than being auto-dismissed.
</success_criteria>

<output>
Create `.planning/phases/FLOWPDF-01-durable-flow-foundation/01-05-SUMMARY.md` when done.
</output>
