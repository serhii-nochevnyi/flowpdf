---
phase: FLOWPDF-01-durable-flow-foundation
plan: "04"
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - crates/flow-core/src/lib.rs
  - crates/flow-core/src/anchor/mod.rs
  - crates/flow-core/src/transaction/mod.rs
  - crates/flow-core/tests/transaction_tracer.rs
  - crates/flow-core/tests/preconditions.rs
  - crates/flow-core/tests/transaction_properties.rs
  - crates/flow-core/proptest-regressions/transaction_properties.txt
autonomous: true
requirements: [EDIT-06, EDIT-07]
estimate:
  tokens: 47000
  raw_tokens: 47000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "[EDGE EDIT-06/adjacency] Neighboring edits remain distinct transactions; forward operations execute in declared order and inverse operations are stored in executable rollback order."
    - "[EDGE EDIT-06/empty] Undo or redo with no available history returns a structured non-mutating error; a single committed transaction reverses and reapplies exactly."
    - "[EDGE EDIT-06/ordering] Forward, inverse, anchor-mapping, history, and revision ordering is deterministic for the same command sequence."
    - "[EDGE EDIT-06/idempotency] A duplicate undo/redo command ID never applies twice, and complete undo/redo cycles restore the expected canonical semantic hash."
    - "[EDGE EDIT-06/concurrency] Commands racing from the same base revision cannot both mutate; one valid commit advances revision and every later contender returns a stale conflict."
    - "[EDGE EDIT-07/empty] Missing, null, deleted-unmapped, or out-of-range targets return stable invalid-target/anchor errors with bytes, revision, and history unchanged."
    - "[EDGE EDIT-07/encoding] Public offsets are UTF-16 units, reject surrogate-half/out-of-range positions, preserve text without normalization, and make no Phase 1 grapheme-boundary claim."
    - "[EDGE EDIT-07/idempotency] An already-seen command ID is deterministically rejected and repeated stale/invalid attempts cannot create a mutation."
    - "[EDGE EDIT-07/concurrency] Revision preconditions prevent stale commands from being silently rebased or redirected to nearby content."
  prohibitions:
    - statement: "A stale or invalid logical target MUST NOT be silently rebased, guessed, or moved to nearby content while the operation is reported as successful."
      status: resolved
      verification: judgment
  artifacts:
    - path: "crates/flow-core/src/anchor/mod.rs"
      provides: "Explicit UTF-16 public anchors and transformation mappings"
    - path: "crates/flow-core/src/transaction/mod.rs"
      provides: "Typed command, immutable transaction, conflict, undo/redo history service"
    - path: "crates/flow-core/tests/transaction_properties.rs"
      provides: "Stateful reversible-sequence and concurrency properties"
  key_links:
    - from: "crates/flow-core/src/anchor/mod.rs"
      to: "crates/flow-core/src/transaction/mod.rs"
      via: "validated targets and emitted mappings"
      pattern: "Anchor|Utf16|mapping"
    - from: "crates/flow-core/src/transaction/mod.rs"
      to: "crates/flow-core/src/canonical/mod.rs"
      via: "pre/post transaction canonical hash assertions"
      pattern: "canonical"
---

<objective>
Make every Phase 1 mutation safely targetable, atomic, immutable, auditable, and reversible.

Purpose: Establish the one command boundary that keyboard, UI, future voice, and APIs must share.
Output: Public logical anchors, typed commands/operations, immutable transactions, conflict semantics, undo/redo, and property tests.
</objective>

<execution_context>
@/Users/serhii/.codex/gsd-core/workflows/execute-plan.md
@/Users/serhii/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-CONTEXT.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-RESEARCH.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-PATTERNS.md
@.planning/phases/FLOWPDF-01-durable-flow-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task id="01-04-01" type="tracer" tdd="true">
  <name>Tracer: typed command → immutable transaction/inverse → new revision/hash</name>
  <reversibility rating="costly">D-05/D-06 define the shared mutation ABI every later input modality will compile against.</reversibility>
  <read_first>
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-CONTEXT.md (D-05, D-06, D-07)
    - crates/flow-core/src/model/mod.rs
    - crates/flow-core/src/canonical/mod.rs
    - crates/flow-wasm/src/lib.rs
  </read_first>
  <files>crates/flow-core/src/anchor/mod.rs, crates/flow-core/src/transaction/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/transaction_tracer.rs, crates/flow-core/tests/preconditions.rs</files>
  <behavior>
    - RED first: one typed replace-text test command with commandId/baseRevision/modality/anchor becomes one immutable transaction with ordered forward/inverse operations, mapping, new revision, and changed canonical hash.
    - Stale revision, duplicate command ID, invalid target/range, broken invariant, and invalid UTF-16 boundary return distinct stable codes and preserve pre-command bytes/revision/history.
    - The same command service is called by native and WASM entry points; neither adapter can directly mutate FlowDocument.
  </behavior>
  <action>Implement D-05/D-06 as a typed command enum and atomic application service. Capture command ID, base/new revision, modality, ordered forward operations, inverse operations derived from the untouched pre-state, and explicit anchor mapping before publishing a new immutable state. Implement the D-07 validate-then-construct boundary so all preconditions are checked against the unchanged document and no partial operation leaks. Refactor the walking-skeleton command to call this service through the same Rust export. Keep form descriptors as data that can participate in generic structure/field operations required by EDIT-06, without adding form UI or validation.</action>
  <acceptance_criteria>
    - One happy command produces exactly one transaction/revision and a reproducible inverse.
    - Every named failure compares canonical bytes, revision, history cursor, and document equality before/after.
    - Native and WASM calls share the identical command application implementation.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p flow-core transaction_tracer preconditions -- --nocapture &amp;&amp; cargo check -p flow-wasm --target wasm32-unknown-unknown</automated>
  </verify>
  <done>D-05 through D-07 have one production command-to-transaction path with atomic success and exact non-mutating failures.</done>
</task>

<task id="01-04-02" type="auto" tdd="true">
  <name>Enforce public UTF-16 anchors, explicit mappings, and never-guessed invalidation</name>
  <reversibility rating="costly">D-09 is the public position contract later selection, fields, comments, voice targeting, source maps, and WASM bindings inherit.</reversibility>
  <read_first>
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-CONTEXT.md (D-09, D-10, D-11)
    - crates/flow-core/src/anchor/mod.rs
    - crates/flow-core/src/transaction/mod.rs
  </read_first>
  <files>crates/flow-core/src/anchor/mod.rs, crates/flow-core/src/transaction/mod.rs, crates/flow-core/tests/preconditions.rs</files>
  <behavior>
    - `nodeId + UTF-16 offset + affinity` converts to a valid internal string boundary and back without using DOM paths, absolute document indices, page rectangles, or PDF coordinates.
    - Insert/replace/delete operations emit deterministic mappings for surviving anchors; a deleted anchor without a mapping becomes explicitly invalid.
    - Surrogate-half, out-of-range, stale, missing-node, deleted-unmapped, and conflicting concurrent targets retain the exact old canonical state.
  </behavior>
  <action>Implement D-09 with explicit `Utf16Offset` and internal-index newtypes/conversion helpers. Reject offsets within a surrogate pair and out of range; preserve stored text unchanged and reserve grapheme-safe enforcement for its assigned later phase. Implement D-10 transformation results for before/inside/after positions and affinity, and an explicit invalid state for deleted-unmapped targets. Enforce D-11 structurally by keeping coordinate/DOM/absolute-index variants out of the public canonical anchor enum and by adding serialization/compile-time contract tests. Test concurrent same-base commands so only the revision-valid command can publish and no fallback target is selected.</action>
  <acceptance_criteria>
    - UTF-16/native conversions cover BMP, combining marks, emoji, and non-BMP code points at every valid/invalid boundary.
    - Mapping/invalidation is deterministic for insert, replace, delete, and deleted target cases.
    - The public serialized anchor vocabulary contains only node ID, UTF-16 offset, and affinity.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p flow-core preconditions -- --nocapture</automated>
  </verify>
  <done>D-09 through D-11 and every EDIT-07 edge truth are enforced without grapheme, DOM, layout, or PDF scope leakage.</done>
</task>

<task id="01-04-03" type="auto" tdd="true">
  <name>Make undo/redo ordinary transactions and prove reversible histories</name>
  <read_first>
    - .planning/phases/FLOWPDF-01-durable-flow-foundation/01-CONTEXT.md (D-08)
    - crates/flow-core/src/transaction/mod.rs
    - crates/flow-core/tests/transaction_tracer.rs
  </read_first>
  <files>crates/flow-core/src/transaction/mod.rs, crates/flow-core/tests/transaction_properties.rs, crates/flow-core/proptest-regressions/transaction_properties.txt</files>
  <behavior>
    - Undo and redo each use fresh command IDs/base revisions and create ordinary immutable transactions with their own revision links.
    - Empty undo/redo, duplicate IDs, stale concurrent history commands, and repeated reversal fail without state/history mutation.
    - Generated valid content/style/structure/field operation sequences undo to the original semantic hash and redo to the pre-undo semantic hash; failing seeds shrink and persist.
  </behavior>
  <action>Implement D-08 history entries and cursor semantics without recomputing an inverse from a later state. Undo applies the stored inverse in executable order; redo reapplies the stored forward operation set, and both pass through the same D-05/D-07 validation/application service with new revisions. Use `proptest` state machines to generate mixed valid sequences, inject one failure dimension at a time, exercise empty/single/many/adjacent/concurrent/idempotent cases, assert deterministic ordering, and persist shrinking regressions.</action>
  <acceptance_criteria>
    - Every committed operation category required by EDIT-06 has a tested inverse and redo path.
    - Generated full undo/redo cycles restore their expected canonical semantic hashes and deterministic history ordering.
    - All failure cases preserve bytes, revision, cursor, and history contents exactly.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p flow-core transaction_properties -- --nocapture</automated>
  </verify>
  <done>EDIT-06, D-08, and all EDIT-06 edge truths are property-proven through the same transaction boundary.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- ASVS level: L1
- Blocking threshold: high

## Trust Boundaries

| Boundary | Description |
|---|---|
| Input modality/WASM DTO → command service | IDs, revisions, targets, offsets, and arguments are untrusted. |
| Stored history → undo/redo application | A stale or altered inverse could target different content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | Command target/revision | high | mitigate | Tasks 01-04-01/02 validate base revision, identity, range, UTF-16 boundary, invariant, and mapping before constructing immutable state. |
| T-04-02 | Tampering | Undo/redo inverse history | high | mitigate | Task 01-04-03 stores inverses from pre-state, revision-checks reversal, and property-proves canonical hash restoration. |
| T-04-03 | Repudiation | Command identity/outcome | medium | mitigate | Every success/failure exposes stable command/transaction/revision links and safe outcome codes for Plan 05 audit construction. |
| T-04-04 | Spoofing | Source modality | low | accept | Modality is local audit metadata, not an authentication/authorization claim; later security boundaries must not trust it as identity. |
</threat_model>

<verification>
Run every focused command and then `cargo test -p flow-core transaction`. Confirm persisted proptest regressions remain green and the real walking-skeleton mutation still calls the shared service.
</verification>

<success_criteria>
- EDIT-06/EDIT-07 and D-05 through D-11 are fully implemented.
- All nine transaction/anchor edge truths have automated evidence.
- The descriptor-less bespoke anchor prohibition remains explicitly flagged for verifier judgment and is not silently dismissed.
</success_criteria>

<output>
Create `.planning/phases/FLOWPDF-01-durable-flow-foundation/01-04-SUMMARY.md` when done.
</output>

