---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "08"
type: execute
wave: 1
depends_on: ["05-07"]
files_modified:
  - web/src/editor/field-navigation.tsx
  - web/src/editor/editor-messages.ts
  - web/src/editor/editor.css
  - web/tests/form-controls.browser.test.ts
  - web/tests/editor-accessibility.browser.test.ts
requirements: [FORM-01, FORM-02, FORM-05, QUAL-03, QUAL-04, QUAL-07]
estimate: { tokens: 10000, raw_tokens: 10000, tasks: 2, confidence: high }
must_haves:
  truths:
    - "An already-authored valid field can be placed at the current collapsed Rust editor caret through one SetField transaction."
    - "The placement action preserves field identity, descriptor metadata, and option/default state while changing only the grapheme-safe anchor."
    - "A non-collapsed selection, stale target, invalid anchor, or review-region field cannot move a field or mutate canonical state."
    - "An accepted anchor move rebinds the existing noncanonical form session to the new canonical revision/hash."
  artifacts:
    - "Localized accessible place-at-caret action in the valid field descriptor editor."
    - "Stable Chromium evidence for successful and fenced anchor placement in Ukrainian and English."
  key_links:
    - "accepted Rust editor selection -> typed GraphemeSafe anchor -> SetField -> accepted snapshot -> source-bound form session"
---

<objective>Expose safe anchor placement for already-authored semantic fields without introducing a browser-owned position model.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-08-RESEARCH.md
@crates/flow-core/src/transaction/mod.rs
@crates/flow-core/src/editor_view/mod.rs
@web/src/editor/field-navigation.tsx
@web/src/forms/form-session.ts
</context>

<tasks>
<task id="05-08-01" type="auto" tdd="true">
  <name>05-08-01: Route the accepted Rust caret into SetField placement</name>
  <read_first>
    - web/src/editor/field-navigation.tsx
    - web/src/editor/input-adapter.ts
    - web/src/editor/editor-controller.ts
    - crates/flow-core/src/transaction/mod.rs
  </read_first>
  <files>web/src/editor/field-navigation.tsx, web/src/editor/editor-messages.ts, web/src/editor/editor.css</files>
  <behavior>
    - A valid field card exposes a keyboard-reachable, localized placement action.
    - The action reads only the accepted Rust editor selection and is enabled only for a collapsed selection.
    - Submission preserves the descriptor and field ID and dispatches one typed setField command with a GraphemeSafe anchor.
    - Review cards and direct review-only mounts expose no placement action.
  </behavior>
  <action>Add a place-at-caret button to the existing valid-field disclosure, construct the anchor from the accepted command-target snapshot, and keep the existing Rust persistence/error path authoritative. Add focus-visible/minimum-hit-target styling and localized unavailable/placement labels.</action>
  <acceptance_criteria>
    - No DOM selection, canonical JSON, form-session value, or field-card object is mutated directly.
    - The action cannot submit while the accepted selection is non-collapsed or unavailable.
    - The current descriptor editor remains usable after an accepted or rejected placement.
  </acceptance_criteria>
  <verify><automated>npm run typecheck</automated></verify>
  <done>Valid authored fields can request a Rust-owned anchor move through the existing command bus.</done>
</task>

<task id="05-08-02" type="auto" tdd="true">
  <name>05-08-02: Prove placement preservation, rebinding, and selection fences</name>
  <read_first>
    - web/tests/form-controls.browser.test.ts
    - web/tests/editor-accessibility.browser.test.ts
    - crates/flow-core/tests/field_accessibility.rs
  </read_first>
  <files>web/tests/form-controls.browser.test.ts, web/tests/editor-accessibility.browser.test.ts</files>
  <behavior>
    - In both locales, moving a valid field to a collapsed accepted caret advances revision once, preserves ID/metadata/options/default, and rebinds the source-bound form control.
    - A non-collapsed accepted selection leaves revision, anchor, descriptor, and session state unchanged.
    - Review cards continue to have no placement action, and existing accessibility/hit-target contracts remain green.
  </behavior>
  <action>Add deterministic browser evidence using controller.setEditorSelection with Rust-projected positions, assert the exact anchor and descriptor preservation, and cover the non-collapsed fence and review absence without treating local draft or DOM state as canonical.</action>
  <acceptance_criteria>
    - Both locales cover one successful placement and one canonical no-op fence.
    - Existing form value controls remain source-bound after the revision change.
    - Full browser, focused Rust, TypeScript, and repository gates remain green.
  </acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; cargo test --locked -p flow-core --test field_accessibility &amp;&amp; PLAYWRIGHT_BROWSERS_PATH=./work/playwright npx --no-install vitest run --project browser web/tests/form-controls.browser.test.ts web/tests/editor-accessibility.browser.test.ts</automated></verify>
  <done>Field anchor placement is accessible, Rust-owned, reversible, and covered by real Chromium evidence.</done>
</task>
</tasks>

<artifacts>Localized place-at-caret field action and focused placement evidence.</artifacts>
<verification>Run focused type, Rust, and Chromium checks, then the full repository gate.</verification>
<success_criteria>Only an accepted Rust SetField result changes a field anchor; non-collapsed, review, stale, and invalid paths remain canonical no-ops.</success_criteria>
