---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "13"
type: execute
wave: 1
depends_on: ["05-12"]
files_modified:
  - crates/flow-core/src/pdf/forms.rs
  - crates/flow-core/src/pdf/mod.rs
  - crates/flow-core/src/pdf/provenance.rs
  - crates/flow-core/tests/pdf_forms.rs
  - crates/flow-core/tests/pdf_recovery.rs
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-13-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-13-PLAN.md
requirements: [FORM-07, QUAL-07]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "A caller must explicitly select field IDs for flattening; ordinary form exports remain editable and unchanged by the empty default."
    - "Selected accepted fields become bounded page content at their fixed-point rectangles, while unselected fields retain editable AcroForm widgets and appearances."
    - "Flattening changes the export fingerprint and reproducibility manifest but never mutates canonical FlowDocument or form-session state; exact recovery still returns the source document."
    - "Unknown, duplicate, oversized, or plan-less selections fail before an invalid COS graph is published."
  artifacts:
    - "Explicit PdfExportOptions selection, deterministic manifest/fingerprint binding, and owned page-content flattening integration."
    - "Focused form/recovery tests for empty, partial, all-field, deterministic, source-bound, and fail-closed flattening paths."
  key_links:
    - "explicit export selection -> validated PdfFormPlan IDs -> derived page content + retained AcroForm remainder"
    - "flatten selection -> export fingerprint/manifest -> recoverable canonical source identity"

<objective>Add explicit, derived flattening of selected semantic form fields to owned PDF page content without replacing the editable source or silently dropping unselected widgets.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-13-RESEARCH.md
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-12-SUMMARY.md
@crates/flow-core/src/pdf/forms.rs
@crates/flow-core/src/pdf/mod.rs
@crates/flow-core/src/pdf/provenance.rs
@crates/flow-core/tests/pdf_forms.rs
@crates/flow-core/tests/pdf_recovery.rs
</context>

<tasks>
<task id="05-13-01" type="auto" tdd="true">
  <name>05-13-01: Add explicit flatten selection and derived page-content emission</name>
  <read_first>
    - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-13-RESEARCH.md
    - crates/flow-core/src/pdf/forms.rs
    - crates/flow-core/src/pdf/mod.rs
    - crates/flow-core/src/pdf/provenance.rs
  </read_first>
  <files>crates/flow-core/src/pdf/forms.rs, crates/flow-core/src/pdf/mod.rs, crates/flow-core/src/pdf/provenance.rs</files>
  <behavior>
    - Empty selections preserve ordinary form export behavior and default fingerprints/manifests.
    - Valid selections are normalized in canonical field order, validated against the accepted plan, and emitted into bounded page content using the existing derived appearance commands.
    - Partial selection retains unflattened field/widget/AP state; all-field selection omits the AcroForm catalog entry and page annotations while retaining page resources/content and the private source envelope.
    - Selection validation rejects duplicates, unknown IDs, no form plan, invalid geometry, and limits before COS publication.
  </behavior>
  <action>Extend the export option and provenance identity contract, thread the normalized selection into the form emitter, replace selected page content streams with translated appearance bodies, and retain only unselected AcroForm fields/widgets.</action>
  <acceptance_criteria>
    - The operation is export-only and derived; no canonical/session schema or transaction changes are introduced.
    - Page content uses fixed-point coordinates and shared bounded resources; no raw field text is emitted as an operator or name.
    - Existing no-form and empty-selection exports remain byte-compatible with their pre-flattening contract.
  </acceptance_criteria>
  <verify><automated>cargo fmt --all &amp;&amp; cargo test --locked -p flow-core --test pdf_forms -- --nocapture</automated></verify>
  <done>Owned exports can explicitly flatten selected accepted form fields while preserving unselected editable widgets and source identity.</done>
</task>

<task id="05-13-02" type="auto" tdd="true">
  <name>05-13-02: Prove flattening selection, identity, and recovery fences</name>
  <read_first>
    - crates/flow-core/tests/pdf_forms.rs
    - crates/flow-core/tests/pdf_recovery.rs
    - crates/flow-core/src/pdf/forms.rs
    - crates/flow-core/src/pdf/provenance.rs
  </read_first>
  <files>crates/flow-core/tests/pdf_forms.rs, crates/flow-core/tests/pdf_recovery.rs</files>
  <behavior>
    - Empty, partial, reordered, and all-field selections produce the expected AP/Annots/AcroForm/page-content structure and deterministic bytes.
    - Flattening selection changes export identity and manifest options while canonical source hash/revision and exact recovery remain unchanged.
    - Unknown, duplicate, oversized, and plan-less selections fail with stable typed errors before result publication.
  </behavior>
  <action>Add focused assertions over owned PDF bytes, manifest identity, source recovery, and rejection cases; retain the existing appearance, session, and no-form regressions.</action>
  <acceptance_criteria>
    - Tests prove explicit selection and source preservation without invoking an unavailable external viewer.
    - `npm run check` and `npm run check:phase4` remain green, with external/reference rows still reported honestly as unavailable.
  </acceptance_criteria>
  <verify><automated>cargo test --locked -p flow-core --test pdf_forms -- --nocapture &amp;&amp; cargo test --locked -p flow-core --test pdf_recovery -- --nocapture &amp;&amp; npm run check &amp;&amp; npm run check:phase4</automated></verify>
  <done>FORM-07's explicit derived core export path is covered by deterministic, source-bound, fail-closed evidence.</done>
</task>
</tasks>

<artifacts>Explicit form flatten selection, derived page content, retained editable remainder, and recovery-bound export evidence.</artifacts>
<verification>Run focused form/recovery tests, then full repository and Phase 4 gates.</verification>
<success_criteria>Only an explicit validated selection can flatten accepted form fields; canonical/session state and unselected editable fields remain intact.</success_criteria>
