---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "03"
type: execute
wave: 1
depends_on: ["05-02"]
files_modified:
  - crates/flow-core/src/forms/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/forms.rs
autonomous: true
requirements: [FORM-04, FORM-05, QUAL-07]
estimate: { tokens: 10000, raw_tokens: 10000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "A widget projection exposes authored default and effective session value as separate derived fields."
    - "Session-aware placement remains bound to the same document/display-list revision, source hash, and fixed-point geometry."
    - "Invalid or stale form sessions cannot publish a widget projection."
  artifacts:
    - "Versioned session-aware FormWidgetProjection API and typed session propagation error."
    - "Focused tests for default/effective values, projection schema/hash determinism, reflow, and stale session rejection."
  key_links:
    - "FormSessionState explicit override -> FormWidget.value while FieldDescriptor.default_value remains FormWidget.default_value"
---

<objective>Connect Rust form sessions to the derived widget projection without persisting page or appearance state.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-CONTEXT.md
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-03-RESEARCH.md
@crates/flow-core/src/forms/mod.rs
@crates/flow-core/src/pdf/display_list.rs
</context>

<tasks>
<task id="05-03-01" type="auto" tdd="true">
  <name>05-03-01: Add effective value to the versioned widget projection</name>
  <read_first>
    - crates/flow-core/src/forms/mod.rs
    - crates/flow-core/src/lib.rs
    - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-02-PLAN.md
  </read_first>
  <files>crates/flow-core/src/forms/mod.rs, crates/flow-core/src/lib.rs</files>
  <behavior>
    - Keep `resolve_form_widgets` deterministic for authored defaults while adding `resolve_form_widgets_with_session` for effective current values.
    - Reject a stale, mismatched, unsupported, forged, or invalid session before any widget is published.
    - Bump the derived projection schema and include effective values in the deterministic result hash without changing canonical document bytes.
  </behavior>
  <action>Add `FormWidget.value`, a session-aware resolver, and a typed `FormProjectionError::Session` path. Reuse the existing source mapping/geometry implementation and preserve explicit review behavior.</action>
  <acceptance_criteria>
    - The serialized projection distinguishes authored defaults from effective values and contains no PDF object numbers, DOM coordinates, or raw diagnostics.
    - The old default-only resolver remains a stable convenience that uses `default_value` as the effective value.
  </acceptance_criteria>
  <verify><automated>cargo fmt --all &amp;&amp; cargo test --locked -p flow-core --test forms -- --nocapture</automated></verify>
  <done>Rust can derive a widget projection for either authored defaults or a validated noncanonical form session.</done>
</task>

<task id="05-03-02" type="auto" tdd="true">
  <name>05-03-02: Prove effective-value projection and stale session rejection</name>
  <read_first>
    - crates/flow-core/tests/forms.rs
    - crates/flow-core/src/forms/mod.rs
  </read_first>
  <files>crates/flow-core/tests/forms.rs</files>
  <behavior>
    - A filled session changes only `FormWidget.value`; authored `default_value`, placement, source identity, and document bytes stay unchanged.
    - Clearing the session override restores the default value and changes the derived projection hash deterministically.
    - Stale document/session, source-hash, unsupported-schema, and forged-value paths fail closed with stable codes.
  </behavior>
  <action>Add focused integration tests for projection round trips, effective/default separation, reset, deterministic hashes, and error propagation.</action>
  <acceptance_criteria>
    - `cargo test --locked -p flow-core --test forms -- --nocapture` passes with no ignored session projection branch.
    - `cargo clippy --locked -p flow-core --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <verify><automated>cargo test --locked -p flow-core --test forms -- --nocapture &amp;&amp; cargo clippy --locked -p flow-core --all-targets -- -D warnings</automated></verify>
  <done>FORM-04/05's Rust projection path is session-aware while AcroForm and browser transport remain deferred.</done>
</task>
</tasks>

<artifacts>Versioned session-aware forms projection, typed error propagation, focused tests.</artifacts>
<verification>Run formatting, focused form tests, and locked clippy.</verification>
<success_criteria>Effective values are derived only from a validated session; canonical defaults and PDF appearances remain separate.</success_criteria>
