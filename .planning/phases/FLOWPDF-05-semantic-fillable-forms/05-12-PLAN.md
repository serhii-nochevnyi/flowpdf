---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "12"
type: execute
wave: 1
depends_on: ["05-11"]
files_modified:
  - crates/flow-core/src/pdf/forms.rs
  - crates/flow-core/tests/pdf_forms.rs
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-12-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-12-PLAN.md
requirements: [FORM-06, QUAL-07]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Accepted PdfFormPlan values produce explicit bounded normal appearance streams without changing canonical FlowDocument or noncanonical form-session state."
    - "Text-like, checkbox, radio, push-button, signature, and choice fields receive deterministic appearance resources and state dictionaries appropriate to their closed PDF type."
    - "The AcroForm declares deterministic resources/default appearance and NeedAppearances=false; no action, JavaScript, or viewer-specific fallback is emitted."
    - "Forged/stale/oversized appearance inputs fail before publication, while repeated exports from one accepted source/session remain byte-identical."
  artifacts:
    - "Rust-owned bounded AP/DR/DA/NeedAppearances emission integrated with the existing AcroForm writer."
    - "PDF form tests for explicit appearance structure, all current field kinds/states, deterministic bytes, source/session identity, and rejection fences."
  key_links:
    - "validated PdfFormPlan -> deterministic appearance resources/streams -> widget /AP and /AS -> owned COS graph"
    - "source/session-bound plan -> export fingerprint -> repeatable appearance bytes"
---

<objective>Add explicit, deterministic AcroForm appearances to the owned PDF form export while keeping appearance data derived, bounded, and honest about Unicode/viewer limits.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-12-RESEARCH.md
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-04-PLAN.md
@crates/flow-core/src/pdf/forms.rs
@crates/flow-core/src/pdf/mod.rs
@crates/flow-core/tests/pdf_forms.rs
</context>

<tasks>
<task id="05-12-01" type="auto" tdd="true">
  <name>05-12-01: Emit bounded deterministic AcroForm appearance resources and streams</name>
  <read_first>
    - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-12-RESEARCH.md
    - crates/flow-core/src/pdf/forms.rs
    - crates/flow-core/src/pdf/mod.rs
    - crates/flow-core/src/layout/mod.rs
    - crates/flow-core/tests/pdf_forms.rs
  </read_first>
  <files>crates/flow-core/src/pdf/forms.rs</files>
  <behavior>
    - The emitter creates one deterministic shared built-in font resource and explicit AcroForm `/DR`, `/DA`, and `/NeedAppearances false` entries.
    - Text, choice, signature, and push-button widgets receive a normal appearance stream with checked rectangle geometry and bounded hex text/state content.
    - Checkbox/radio widgets receive `/AP /N` state dictionaries with `/Off` and admitted on-state names; `/AS` is emitted only from validated Rust values.
    - Stream construction rejects numeric overflow and bounded output violations before an invalid COS value is published.
  </behavior>
  <action>Extend the existing form-object emitter with internal appearance resource/stream helpers, fixed-point rectangle validation, UTF-16BE hex text encoding, state-specific button appearances, and deterministic AcroForm resource/default entries. Keep the public plan and canonical/session schemas unchanged.</action>
  <acceptance_criteria>
    - Appearance generation consumes only the already-validated `PdfFormPlan`; browser or PDF viewer state never enters canonical data.
    - Object insertion order and stream bytes are stable across repeated exports and field tab-order changes remain the only ordering input.
    - No actions, JavaScript, external references, or target-viewer claims are added.
  </acceptance_criteria>
  <verify><automated>cargo fmt --all -- --check &amp;&amp; cargo test --locked -p flow-core --test pdf_forms</automated></verify>
  <done>The owned AcroForm writer emits explicit bounded appearance structure for every current field kind.</done>
</task>

<task id="05-12-02" type="auto" tdd="true">
  <name>05-12-02: Prove appearance/state coverage and export determinism</name>
  <read_first>
    - crates/flow-core/src/pdf/forms.rs
    - crates/flow-core/tests/pdf_forms.rs
    - crates/flow-core/tests/pdf_cos.rs
    - crates/flow-core/src/pdf/recovery.rs
  </read_first>
  <files>crates/flow-core/tests/pdf_forms.rs</files>
  <behavior>
    - Repeated default and session-filled exports are byte-identical per source identity and differ when accepted values differ.
    - PDF bytes contain explicit AP normal streams, button state dictionaries, shared resources, default appearance, and `NeedAppearances=false` for the admitted form plan.
    - Empty/off/on/radio/choice/signature states are represented without leaking raw values into PDF names or operators.
    - Forged plan hashes, invalid tab order/geometry, review projections, and overlong values remain rejected before export publication.
  </behavior>
  <action>Replace the old no-appearance assertion with structural AP evidence, add state/value matrix assertions for the current sample field vocabulary, and retain deterministic, source-bound, and fail-closed export tests without claiming target-viewer compatibility.</action>
  <acceptance_criteria>
    - Tests prove the appearance contract through owned bytes and typed errors, not by invoking an unavailable external viewer.
    - Existing form projection, session, tab-order, and PDF resource tests remain green.
  </acceptance_criteria>
  <verify><automated>cargo test --locked -p flow-core --test pdf_forms -- --nocapture &amp;&amp; npm run check &amp;&amp; npm run check:phase4</automated></verify>
  <done>Appearance evidence is deterministic, bounded, source/session-bound, and explicit about the remaining viewer/Unicode limits.</done>
</task>
</tasks>

<artifacts>Derived AcroForm appearance resources/streams, explicit AP state coverage, and deterministic export evidence.</artifacts>
<verification>Run focused PDF form tests, then full repository and Phase 4 gates.</verification>
<success_criteria>Only accepted form plans produce explicit bounded appearances; canonical/session state remains unchanged and unavailable viewer evidence is not fabricated.</success_criteria>
