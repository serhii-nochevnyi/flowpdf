---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "07"
type: execute
wave: 1
depends_on: ["05-06"]
files_modified:
  - web/src/editor/editor-controller.ts
  - web/src/editor/semantic-document.tsx
  - web/src/editor/field-navigation.tsx
  - web/src/editor/editor-messages.ts
  - web/src/editor/editor.css
  - web/tests/form-controls.browser.test.ts
  - web/tests/editor-accessibility.browser.test.ts
requirements: [FORM-01, FORM-05, QUAL-03, QUAL-04, QUAL-07]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "An already-authored valid field exposes an accessible descriptor editor without making the DOM the semantic authority."
    - "A submitted descriptor edit is one Rust SetField transaction that preserves the field ID and anchor and publishes the accepted canonical revision."
    - "Rust rejects incompatible, duplicate, or out-of-bounds descriptor drafts without changing canonical revision/history or form-session persistence."
    - "Review-region fields remain read-only and do not expose descriptor authoring controls."
  artifacts:
    - "Typed setField command admitted by the existing TypeScript structural command bus."
    - "Localized accessible descriptor editor covering the current text, checkbox, radio, select, signature, and button vocabulary."
    - "Chromium evidence for accepted metadata/default/option edits, invalid no-op rejection, and review-control absence."
  key_links:
    - "accepted Rust field projection -> local descriptor draft -> SetField command -> persisted accepted editor snapshot"
    - "accepted descriptor revision/hash -> existing form-session coordinator -> source-bound fill controls"
---

<objective>Expose bounded accessible configuration for already-authored semantic fields through the existing Rust SetField transaction path.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-07-RESEARCH.md
@crates/flow-core/src/model/mod.rs
@crates/flow-core/src/transaction/mod.rs
@web/src/editor/editor-controller.ts
@web/src/editor/field-navigation.tsx
@web/src/forms/form-session.ts
</context>

<tasks>
<task id="05-07-01" type="auto" tdd="true">
  <name>05-07-01: Route descriptor drafts through Rust SetField</name>
  <read_first>
    - crates/flow-core/src/transaction/mod.rs
    - crates/flow-core/src/schema/mod.rs
    - web/src/editor/editor-controller.ts
    - web/src/editor/editor-store.ts
  </read_first>
  <files>web/src/editor/editor-controller.ts, web/src/editor/semantic-document.tsx, web/src/editor/field-navigation.tsx</files>
  <behavior>
    - A descriptor draft preserves the Rust-projected field ID and anchor and constructs one closed setField DTO.
    - Accepted drafts use the normal persistence/revision queue and restart the existing source-bound form session through the accepted snapshot.
    - The browser never patches canonical JSON, field arrays, option IDs, or editor view objects directly.
  </behavior>
  <action>Extend the structural command DTO with the existing SetField shape, pass the command target into FieldNavigation, and implement a local draft builder that covers the current closed descriptor variants while preserving identity/anchor data.</action>
  <acceptance_criteria>
    - TypeScript accepts the command without weakening the WASM boundary to arbitrary field-shaped objects.
    - Review cards and direct FieldNavigation test mounts without a command target expose no authoring action.
    - The editor remains the canonical and persistence authority after descriptor submission.
  </acceptance_criteria>
  <verify><automated>npm run typecheck</automated></verify>
  <done>Accessible descriptor drafts can be submitted to the existing Rust transaction bus.</done>
</task>

<task id="05-07-02" type="auto" tdd="true">
  <name>05-07-02: Cover descriptor variants, validation failures, and review fences</name>
  <read_first>
    - web/src/editor/field-navigation.tsx
    - web/src/editor/editor-messages.ts
    - web/tests/form-controls.browser.test.ts
    - web/tests/editor-accessibility.browser.test.ts
  </read_first>
  <files>web/src/editor/field-navigation.tsx, web/src/editor/editor-messages.ts, web/src/editor/editor.css, web/tests/form-controls.browser.test.ts, web/tests/editor-accessibility.browser.test.ts</files>
  <behavior>
    - Native labeled controls expose name, label, required/read-only flags, text hint/multiline settings, select multiplicity, option labels/export values, and typed defaults.
    - A valid edit advances canonical revision and remains undoable through the existing controller; a Rust-rejected draft leaves the prior revision and descriptor unchanged.
    - Review fields have no configure controls, and signature/button descriptors remain explicit about their current non-actionable value semantics.
  </behavior>
  <action>Add a localized accessible disclosure editor with bounded option/default editing, stable selectors, focus-visible styling, and browser evidence in Ukrainian and English. Keep error handling on the existing editor status/error path and assert that successful descriptor edits rebind the noncanonical session to the accepted source.</action>
  <acceptance_criteria>
    - Both locales cover at least one successful metadata/default/option edit and one invalid descriptor rejection without canonical mutation.
    - Existing field ordering, native value controls, review selectors, hit-target checks, and shell accessibility contracts remain green.
    - No test treats local draft state as the semantic document truth.
  </acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; PLAYWRIGHT_BROWSERS_PATH=./work/playwright npx --no-install vitest run --project browser web/tests/form-controls.browser.test.ts web/tests/editor-accessibility.browser.test.ts</automated></verify>
  <done>Existing semantic field descriptors are configurable through accessible native UI while Rust remains the sole canonical validator and transaction authority.</done>
</task>
</tasks>

<artifacts>Rust-routed descriptor editor, localized field configuration UI, and focused Chromium evidence.</artifacts>
<verification>Run TypeScript, focused Rust field projection tests, focused browser form/accessibility tests, and then the full repository gate.</verification>
<success_criteria>Only accepted Rust SetField results alter canonical descriptor state; invalid drafts, review fields, and UI-local draft changes never mutate the document or durable form session.</success_criteria>
