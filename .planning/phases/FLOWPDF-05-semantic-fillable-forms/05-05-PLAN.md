---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "05"
type: execute
wave: 1
depends_on: ["05-04"]
files_modified:
  - crates/flow-core/src/forms/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/forms.rs
  - crates/flow-wasm/src/lib.rs
  - crates/flow-wasm/tests/form_sessions.rs
  - web/persistence/indexeddb-store.ts
  - web/src/forms/form-session.ts
  - web/tests/form-session.test.ts
  - web/tests/indexeddb-store.test.ts
autonomous: true
requirements: [FORM-03, FORM-04, QUAL-07]
estimate: { tokens: 20000, raw_tokens: 20000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Rust remains the only authority that creates or changes a form session value; browser code can carry only accepted DTOs and error codes."
    - "A form session is durable independently from canonical document recovery and cannot be applied to a different document identity, revision, or source hash."
    - "Retries with the same session generation are idempotent, while divergent or stale generations fail closed before publication."
    - "No form session record contains page coordinates, widget object references, appearance state, or canonical document mutation."
  artifacts:
    - "Versioned Rust FormSessionAction/Request/Response protocol exposed through core and WASM JSON/JS adapters."
    - "Versioned IndexedDB form-session store with source identity, deterministic envelope, and generation conflict guards."
    - "TypeScript bridge tests covering accepted Rust actions, malformed/error responses, durable retries, stale identities, and conflicts."
  key_links:
    - "canonical_json + FormSessionAction -> Rust FormSessionState -> accepted DTO -> IndexedDB form-sessions-v1"
    - "document identity/revision/hash + generation -> durable session key and optimistic write guard"
---

<objective>Expose the Rust-owned noncanonical form session through a bounded WASM/browser transport and durable IndexedDB record boundary.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-CONTEXT.md
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-05-RESEARCH.md
@crates/flow-core/src/forms/mod.rs
@crates/flow-wasm/src/lib.rs
@web/persistence/indexeddb-store.ts
</context>

<tasks>
<task id="05-05-01" type="auto" tdd="true">
  <name>05-05-01: Add closed Rust/WASM form-session actions</name>
  <read_first>
    - crates/flow-core/src/forms/mod.rs
    - crates/flow-core/src/lib.rs
    - crates/flow-wasm/src/lib.rs
    - crates/flow-wasm/tests/pdf_exports.rs
  </read_first>
  <files>crates/flow-core/src/forms/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/forms.rs, crates/flow-wasm/src/lib.rs, crates/flow-wasm/tests/form_sessions.rs</files>
  <behavior>
    - Start, validate, set, and clear actions decode one canonical document and return only a new validated FormSessionState.
    - Stale source identity, missing sessions, unknown fields, read-only fields, invalid values, unknown actions, and malformed JSON return stable safe error codes without partial state.
    - JSON and JsValue adapters use the existing ApiResponse envelope and never expose canonical bytes in the response.
  </behavior>
  <action>Implement the versioned form-session request/action/response DTOs in Rust, add the core dispatcher and WASM bindings, and exercise successful set/clear plus stale/invalid/replay guards at both core and boundary levels.</action>
  <acceptance_criteria>
    - Existing canonical document and editor transaction APIs remain unchanged.
    - The protocol cannot mutate canonical revision/history or serialize page/widget/PDF state.
  </acceptance_criteria>
  <verify><automated>cargo fmt --all &amp;&amp; cargo test --locked -p flow-core --test forms -- --nocapture &amp;&amp; cargo test --locked -p flow-wasm --test form_sessions -- --nocapture</automated></verify>
  <done>Rust/WASM can create and immutably update a source-bound form session with executable fail-closed evidence.</done>
</task>

<task id="05-05-02" type="auto" tdd="true">
  <name>05-05-02: Persist accepted sessions with IndexedDB generation guards</name>
  <read_first>
    - web/persistence/indexeddb-store.ts
    - web/tests/indexeddb-store.test.ts
    - web/src/editor/editor-controller.ts
    - crates/flow-core/src/forms/mod.rs
  </read_first>
  <files>web/persistence/indexeddb-store.ts, web/src/forms/form-session.ts, web/tests/form-session.test.ts, web/tests/indexeddb-store.test.ts</files>
  <behavior>
    - A separate form-sessions-v1 object store is created atomically with the existing database upgrade and does not participate in canonical recovery records.
    - Only source-bound, bounded accepted session DTOs are stored; exact retries converge, divergent same-generation writes conflict, and stale generation/source identity writes fail before put.
    - The TypeScript bridge routes Start/Validate/Set/Clear through the Rust boundary and persists only successful responses; Rust error codes remain intact.
  </behavior>
  <action>Raise the IndexedDB version for one additive store, add the guarded form-session adapter and Rust boundary bridge, and cover fresh storage, reload, idempotent retry, generation/source conflicts, clear-to-empty overrides, and malformed responses.</action>
  <acceptance_criteria>
    - Form session records remain separate from canonical snapshots, transactions, audits, assets, and migration sources.
    - Browser persistence never invents or revalidates field semantics and never publishes a session after a failed Rust action.
  </acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npx --no-install vitest run --project unit web/tests/form-session.test.ts web/tests/indexeddb-store.test.ts</automated></verify>
  <done>Accepted noncanonical form values can survive browser reload through a guarded dedicated store while canonical recovery remains unchanged.</done>
</task>
</tasks>

<artifacts>Rust/WASM form-session protocol, guarded IndexedDB form-session store, TypeScript bridge, and focused tests.</artifacts>
<verification>Run formatting, locked Rust form/core/WASM tests, TypeScript, and focused IndexedDB/bridge unit tests.</verification>
<success_criteria>Only Rust-accepted source-bound session state reaches durable browser storage; stale/conflicting/malformed records fail closed and no canonical or PDF-derived state is persisted.</success_criteria>
