---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "23"
title: "Expose caller-owned controller dependencies at the editor shell boundary"
type: execute
wave: 1
depends_on: ["05-22"]
files_modified:
  - web/src/editor/editor-app.tsx
  - web/tests/pdf-preview.browser.test.ts
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-23-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-23-PLAN.md
requirements: [FORM-07, QUAL-07]
delivery:
  ticket: T-05-23
  risk: low
  human_checkpoint: false
estimate: { tokens: 6500, raw_tokens: 6500, tasks: 1, confidence: high }
must_haves:
  truths:
    - "EditorApp and mountEditorApp can compose caller-owned controller dependencies without manual controller construction."
    - "The existing supplied-controller path remains authoritative and source-compatible."
    - "The real Chromium PDF preview still reaches the controller default request builder through the shell composition seam."
    - "No worker, font catalog, active entry, semantic state, or PDF byte authority is added."
  artifacts:
    - "Editor shell dependency-injection surface for EditorControllerDependencies."
    - "Chromium preview evidence using mountEditorApp dependencies."
  key_links:
    - "mountEditorApp dependencies -> EditorController(options, dependencies)"
    - "EditorApp controllerDependencies -> default controller construction"
    - "shell-created controller -> default createPdfExportRequest -> PDF scheduler"

objective: "Make the existing caller-owned layout/PDF scheduler seams composable through the editor shell API."

context:
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-23-RESEARCH.md
  - web/src/editor/editor-app.tsx
  - web/src/editor/editor-controller.ts
  - web/tests/pdf-preview.browser.test.ts

tasks:
  - id: 05-23-01
    type: auto
    tdd: true
    name: "05-23-01: Thread controller dependencies through the editor shell"
    read_first:
      - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-23-RESEARCH.md
      - web/src/editor/editor-app.tsx
      - web/src/editor/editor-controller.ts
      - web/tests/pdf-preview.browser.test.ts
    files:
      - web/src/editor/editor-app.tsx
      - web/tests/pdf-preview.browser.test.ts
    behavior:
      - "EditorApp constructs its controller with controllerDependencies when no controller is supplied."
      - "mountEditorApp constructs its controller with its optional dependencies argument."
      - "The preview smoke uses mountEditorApp dependencies and keeps default request identity assertions green."
    action: "Export the dependency type, add optional shell seams, update the mount helper, and migrate the preview fixture."
    acceptance_criteria:
      - "TypeScript and focused Chromium preview pass with caller-owned scheduler fixtures."
      - "Existing supplied-controller behavior remains unchanged."
      - "No production worker/font catalog or active entry wiring is introduced."
    verify:
      automated: "npm run typecheck && npm run test:browser -- web/tests/pdf-preview.browser.test.ts"
    done: "Editor shell callers can provide the existing scheduler/WASM seams without duplicating controller construction."

artifacts: "Caller-owned controller dependency composition at EditorApp and mountEditorApp."
verification: "Run the scoped TypeScript and Chromium preview gates, then the full project check."
success_criteria: "The shell-created controller reaches the default PDF request builder while all authority and stale-result fences remain unchanged."
