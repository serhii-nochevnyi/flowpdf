---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "21"
title: "Use the shared PDF request builder as the controller default"
type: execute
wave: 1
depends_on: ["05-20"]
files_modified:
  - web/src/editor/editor-controller.ts
  - web/tests/editor-controller.test.ts
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-21-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-21-PLAN.md
requirements: [FORM-07, QUAL-07]
delivery:
  ticket: T-05-21
  risk: low
  human_checkpoint: false
estimate: { tokens: 7000, raw_tokens: 7000, tasks: 1, confidence: high }
must_haves:
  truths:
    - "A caller-provided PDF scheduler can use the shared Rust-compatible request builder without hand-writing an opaque factory."
    - "An explicit custom PDF request factory remains authoritative and backward-compatible."
    - "A verified source-bound form selection reaches the default factory path before scheduler submission."
    - "No worker, font catalog, active entry, semantic state, or PDF byte authority is invented by the controller default."
  artifacts:
    - "Controller default factory backed by createPdfExportRequest."
    - "Unit evidence for default selection transport and custom-factory preservation."
  key_links:
    - "EditorControllerDependencies.pdfExportScheduler -> default createPdfExportRequest"
    - "requestPdfExport(formSelection) -> default factory -> PDF scheduler"

objective: "Remove duplicate opaque PDF request boilerplate from scheduler callers while retaining explicit integration ownership and fail-closed selection propagation."

context:
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-21-RESEARCH.md
  - web/src/editor/editor-controller.ts
  - web/src/pdf/pdf-request.ts
  - web/tests/editor-controller.test.ts

tasks:
  - id: 05-21-01
    type: auto
    tdd: true
    name: "05-21-01: Install and verify the controller default request factory"
    read_first:
      - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-21-RESEARCH.md
      - web/src/editor/editor-controller.ts
      - web/src/pdf/pdf-request.ts
      - web/tests/editor-controller.test.ts
    files:
      - web/src/editor/editor-controller.ts
      - web/tests/editor-controller.test.ts
    behavior:
      - "When a PDF scheduler is supplied without a factory, the controller creates validated requests through createPdfExportRequest with local request IDs."
      - "A custom factory still receives the accepted editor/layout/selection arguments and its request is scheduled unchanged."
      - "The default path carries the supplied selection's verified form plan and flattened IDs into serializedRequest."
    action: "Add the default factory assignment, request-ID counter, and unit fixtures/assertions while preserving the existing custom-factory test."
    acceptance_criteria:
      - "The default request passes validatePdfExportRequest and retains accepted page/layout/source identity."
      - "A stale or malformed selection still fails before scheduler invocation."
      - "No production worker/font catalog or active entry wiring is introduced."
    verify:
      automated: "npm run typecheck && npm run test:unit -- web/tests/editor-controller.test.ts"
    done: "Scheduler callers can omit only the redundant opaque factory; integration authority remains explicit."

artifacts: "Controller default Rust-compatible PDF request factory and focused selection propagation evidence."
verification: "Run the scoped typecheck and controller unit suite."
success_criteria: "The controller's default export path is source-bound, selection-aware, and backward-compatible with custom factories."
