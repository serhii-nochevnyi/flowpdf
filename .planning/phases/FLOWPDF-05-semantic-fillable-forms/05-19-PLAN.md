---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "19"
type: execute
wave: 1
depends_on: ["05-18"]
files_modified:
  - web/src/pdf/pdf-request.ts
  - web/tests/pdf-request.test.ts
  - web/tests/pdf-preview.browser.test.ts
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-19-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-19-PLAN.md
requirements: [FORM-07, QUAL-07]
estimate: { tokens: 11000, raw_tokens: 11000, tasks: 2, confidence: high }
must_haves:
  truths:
    - "The browser can build the exact bounded Rust PDF export payload from one accepted editor/layout identity without inspecting DOM text or inventing page geometry."
    - "A verified source-bound form selection is carried into the opaque payload as the plan plus normalized flattened IDs."
    - "Ordinary, partial, and all-field exports remain distinguishable while an empty selection preserves the existing editable export contract."
    - "Stale or invalid source/layout/selection inputs fail closed before a PDF worker can schedule bytes."
  artifacts:
    - "Reusable PDF request builder under the existing PDF-owned browser path."
    - "Unit evidence for payload shape, form plan/selection transport, and identity fences."
    - "PDF preview fixture using the builder rather than hand-written opaque JSON."
  key_links:
    - "EditorAcceptedSnapshot + AcceptedLayoutDto + FormProjectionSelectionDto -> createPdfExportRequest"
    - "createPdfExportRequest.serializedRequest -> Rust PdfExportWireRequest"
    - "controller callback selection -> builder -> revision-safe PDF scheduler"

objective: "Close the browser form-selection export path with one tested Rust-compatible PDF request builder while preserving caller-owned scheduling and font/viewer scope fences."

context:
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-19-RESEARCH.md
  - web/src/pdf/pdf-protocol.ts
  - web/src/pdf/pdf-worker.ts
  - web/src/editor/editor-controller.ts
  - web/src/forms/form-projection.ts
  - crates/flow-wasm/src/lib.rs

tasks:
  - id: 05-19-01
    type: auto
    tdd: true
    name: "05-19-01: Build the Rust-compatible PDF export request"
    read_first:
      - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-19-RESEARCH.md
      - web/src/pdf/pdf-protocol.ts
      - web/src/forms/form-projection.ts
      - crates/flow-wasm/src/lib.rs
    files:
      - web/src/pdf/pdf-request.ts
      - web/tests/pdf-request.test.ts
    behavior:
      - "The builder copies accepted revision/hash, layout result/settings/font/hyphenation identities, canonical JSON, and page bounds into the closed Rust wire shape."
      - "A valid selection copies its verified form plan and normalized flattened IDs; no selection emits null/empty form options."
      - "Source, layout, request ID, page, and selection identity mismatches return null without serializing partial data."
    action: "Implement the typed request options, identity fences, deterministic JSON payload builder, and unit fixtures for ordinary/partial/all/stale cases."
    acceptance_criteria:
      - "The serialized payload uses only fields accepted by PdfExportWireRequest's deny-unknown-fields contract."
      - "The returned PdfExportRequestDto passes validatePdfExportRequest for valid fixtures."
      - "No browser code parses canonical text, calculates coordinates, or rewrites the Rust-produced plan."
    verify:
      automated: "npm run typecheck && npm run test:unit -- web/tests/pdf-request.test.ts"
    done: "A caller can construct a validated export request that carries explicit form flattening without hand-writing opaque JSON."

  - id: 05-19-02
    type: auto
    tdd: true
    name: "05-19-02: Prove controller-to-preview request propagation"
    read_first:
      - web/src/pdf/pdf-request.ts
      - web/tests/pdf-preview.browser.test.ts
      - web/src/editor/editor-controller.ts
    files:
      - web/tests/pdf-preview.browser.test.ts
    behavior:
      - "The existing preview fixture builds its request through the shared helper and still publishes only the accepted layout/source identity."
      - "The fixture proves the request carries the accepted page bounds and layout result hash rather than browser-measured geometry."
      - "Existing stale export, search, zoom, virtualization, and semantic-input assertions remain unchanged."
    action: "Replace the fixture's hand-written serialized payload with createPdfExportRequest and assert the parsed request identity at export time."
    acceptance_criteria:
      - "Chromium evidence remains based on public request/result identity and accessibility attributes, not CSS geometry."
      - "No target-viewer or production font-catalog claim is added."
    verify:
      automated: "npm run test:browser -- pdf-preview.browser.test.ts"
    done: "The controller callback path is proven to use the shared Rust-compatible request builder."

artifacts: "Rust-compatible browser PDF request builder, form plan/selection transport, unit payload evidence, and preview integration evidence."
verification: "Run focused unit/typecheck/browser tests, then the full repository and Phase 4 gates."
success_criteria: "Verified form selection reaches the existing Rust export payload deterministically without introducing browser semantic/PDF authority."
