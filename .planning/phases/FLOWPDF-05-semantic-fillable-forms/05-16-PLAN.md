---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "16"
type: execute
wave: 1
depends_on: ["05-15"]
files_modified:
  - web/src/forms/form-projection.ts
  - web/src/editor/editor-controller.ts
  - web/tests/form-projection.test.ts
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-16-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-16-PLAN.md
requirements: [FORM-02, FORM-03, FORM-07, QUAL-07]
estimate: { tokens: 14000, raw_tokens: 14000, tasks: 2, confidence: high }
must_haves:
  truths:
    - "The browser can request Rust-derived form projection through a typed string-only adapter without inspecting canonical JSON or deriving geometry."
    - "Only a projection matching the requested source and accepted layout result identity can be published."
    - "Superseded, cancelled, malformed, stale, or Rust-unverified projection responses cannot replace the accepted snapshot."
  artifacts:
    - "Versioned TypeScript request/result DTOs for the Rust/WASM form projection envelope."
    - "Revision-aware single-flight scheduler with immutable accepted projection state and bounded diagnostics."
    - "Focused unit evidence for opaque serialization, Rust verification, identity fences, cancellation, and stale-result rejection."
  key_links:
    - "AcceptedLayoutDto + FormSessionStateDto -> form projection request -> project_form_widgets"
    - "serialized Rust response -> verify_form_projection_response -> typed derived projection"
    - "projection source/layout identities -> scheduler publication guard"

objective: "Expose the Rust/WASM form projection as a revision-safe browser adapter while keeping geometry, session validation, and export-plan authority in Rust."

context:
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-16-RESEARCH.md
  - web/src/layout/layout-protocol.ts
  - web/src/layout/layout-worker.ts
  - web/src/forms/form-session.ts
  - crates/flow-core/src/forms/mod.rs

tasks:
  - id: 05-16-01
    type: auto
    tdd: true
    name: "05-16-01: Add the typed Rust/WASM projection adapter and scheduler"
    read_first:
      - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-16-RESEARCH.md
      - web/src/layout/layout-protocol.ts
      - web/src/layout/layout-worker.ts
      - web/src/forms/form-session.ts
      - crates/flow-core/src/forms/mod.rs
    files:
      - web/src/forms/form-projection.ts
      - web/src/editor/editor-controller.ts
    behavior:
      - "The request carries the exact layout request, expected accepted layout result hash, and optional source-bound form session."
      - "The adapter sends only JSON strings to Rust, invokes the Rust response verifier, and maps no browser-authored coordinates or plan fields."
      - "The scheduler is single-flight, aborts superseded work, and checks request/source/layout/projection/plan identities before publication."
      - "The optional WASM exports remain optional at the shared editor boundary until a later UI integration slice wires them into the application."
    action: "Implement the DTO catalog, runtime response guards, WASM engine adapter, and scheduler using the established layout/PDF worker conventions."
    acceptance_criteria:
      - "No canonical schema, transaction, form-session mutation, or PDF writer code changes."
      - "No DOM measurement or fixed-point geometry conversion is performed in the browser adapter."
      - "All adapter failures use stable FLOW_FORM_PROJECTION_* codes and never return partial results."
    verify:
      automated: "npm run typecheck"
    done: "A later browser projection surface can consume one verified immutable scheduler snapshot without duplicating Rust authority."

  - id: 05-16-02
    type: auto
    tdd: true
    name: "05-16-02: Prove projection transport and revision fences"
    read_first:
      - web/src/forms/form-projection.ts
      - web/tests/layout-worker.test.ts
      - web/tests/pdf-worker.test.ts
    files:
      - web/tests/form-projection.test.ts
    behavior:
      - "The adapter serializes the opaque nested layout request and optional session, accepts a Rust-verified response, and preserves derived identities."
      - "Mismatched request, source, accepted layout, projection, plan, malformed, forged, and verifier-rejected responses are rejected."
      - "A newer request publishes while an older late response is cancelled or discarded and cannot replace it."
      - "Cancellation and engine failures leave no partial accepted projection and report bounded diagnostics."
    action: "Add deterministic unit fixtures that model the closed response envelope without fabricating geometry in production code."
    acceptance_criteria:
      - "Tests cover both a valid session-aware result and a review-bearing result with a null plan."
      - "Tests assert the adapter called Rust verification before publication."
      - "Existing full TypeScript and Rust/WASM gates remain green."
    verify:
      automated: "npm run test:unit -- web/tests/form-projection.test.ts"
    done: "FORM-02/03/07 browser transport evidence is present without claiming the projection UI is complete."

artifacts: "Typed form-projection browser boundary, revision-safe scheduler, and identity/cancellation unit evidence."
verification: "Run the focused Vitest unit file, TypeScript typecheck, then the full repository gate."
success_criteria: "Only a Rust-verified projection bound to the accepted source and layout result can enter browser state."
---
