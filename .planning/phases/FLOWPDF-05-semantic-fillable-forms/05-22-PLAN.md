---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "22"
title: "Prove the controller default PDF builder in Chromium preview"
type: execute
wave: 1
depends_on: ["05-21"]
files_modified:
  - web/tests/pdf-preview.browser.test.ts
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-22-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-22-PLAN.md
requirements: [FORM-07, QUAL-07]
delivery:
  ticket: T-05-22
  risk: low
  human_checkpoint: false
estimate: { tokens: 5000, raw_tokens: 5000, tasks: 1, confidence: high }
must_haves:
  truths:
    - "The real Chromium PDF preview path can use the controller's shared request builder without a custom factory."
    - "The caller-owned scheduler receives a validated source/layout-bound request produced by the controller default."
    - "Existing stale export cancellation and visual-only preview boundaries remain intact."
    - "No production worker, font catalog, active entry, semantic authority, or PDF byte authority is added."
  artifacts:
    - "Chromium preview smoke using the controller default request builder."
    - "Focused assertions for the captured source/layout-bound request."
  key_links:
    - "EditorApp -> EditorController.hasPdfExport -> default createPdfExportRequest -> PDF scheduler engine"
    - "accepted layout pages -> serializedRequest -> browser fixture engine"

objective: "Replace test-only PDF request factory duplication with real browser evidence for the controller default builder."

context:
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-22-RESEARCH.md
  - web/src/editor/editor-controller.ts
  - web/src/pdf/pdf-request.ts
  - web/src/pdf/pdf-worker.ts
  - web/tests/pdf-preview.browser.test.ts

tasks:
  - id: 05-22-01
    type: auto
    tdd: true
    name: "05-22-01: Route Chromium preview through the controller default builder"
    read_first:
      - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-22-RESEARCH.md
      - web/src/editor/editor-controller.ts
      - web/src/pdf/pdf-request.ts
      - web/src/pdf/pdf-worker.ts
      - web/tests/pdf-preview.browser.test.ts
    files:
      - web/tests/pdf-preview.browser.test.ts
    behavior:
      - "The preview test omits pdfExportRequestFactory and captures the request at the scheduler engine boundary."
      - "The captured request retains the accepted source revision/hash, layout result hash, and one or more page bounds."
      - "The existing pending, stale-revision, and no-download assertions remain green."
    action: "Remove the duplicated browser factory, capture the request in the fixture engine, and assert the default wire identity."
    acceptance_criteria:
      - "The focused Chromium preview test passes with no custom PDF request factory."
      - "The captured request passes validatePdfExportRequest and has source/layout identity matching the accepted fixture."
      - "No production worker/font catalog or active entry wiring is introduced."
    verify:
      automated: "npm run typecheck && npm run test:browser -- web/tests/pdf-preview.browser.test.ts"
    done: "Chromium preview provides direct evidence for the controller default builder while retaining caller-owned scheduling."

artifacts: "Real-browser evidence for the controller default Rust-compatible PDF request builder."
verification: "Run the scoped TypeScript and Chromium preview gates, then the full project check."
success_criteria: "The existing preview path schedules a validated default-built request and preserves all stale/export accessibility fences."
