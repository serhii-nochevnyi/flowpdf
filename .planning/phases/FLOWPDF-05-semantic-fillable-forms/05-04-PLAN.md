---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "04"
type: execute
wave: 1
depends_on: ["05-03"]
files_modified:
  - crates/flow-core/src/pdf/forms.rs
  - crates/flow-core/src/pdf/mod.rs
  - crates/flow-core/src/pdf/metadata.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/pdf_forms.rs
autonomous: true
requirements: [FORM-06, QUAL-07]
estimate: { tokens: 20000, raw_tokens: 20000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "A validated session-aware form projection can produce deterministic bounded AcroForm field/widget COS structure in the owned writer."
    - "Field names, flags, values/defaults, options, page references, and fixed-point rectangles remain source-bound and fail closed on stale/review/geometry input."
    - "Generic annotations, actions, appearances, flattening, and viewer behavior are not inferred by this slice."
  artifacts:
    - "Typed PdfFormPlan, bounded COS emission, export request attachment, and dynamic form support reporting."
    - "Focused PDF form tests for deterministic bytes, field kinds, values, page widgets, limits, and rejection paths."
  key_links:
    - "FormWidgetProjection -> PdfFormPlan -> COS AcroForm/catalog/page widget references"
---

<objective>Emit a bounded, deterministic AcroForm field/widget structure from the Rust-owned form projection.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-CONTEXT.md
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-04-RESEARCH.md
@crates/flow-core/src/forms/mod.rs
@crates/flow-core/src/pdf/mod.rs
@crates/flow-core/src/pdf/metadata.rs
</context>

<tasks>
<task id="05-04-01" type="auto" tdd="true">
  <name>05-04-01: Add bounded typed form plan and COS field/widget emission</name>
  <read_first>
    - crates/flow-core/src/pdf/mod.rs
    - crates/flow-core/src/pdf/metadata.rs
    - crates/flow-core/src/forms/mod.rs
    - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-04-RESEARCH.md
  </read_first>
  <files>crates/flow-core/src/pdf/forms.rs, crates/flow-core/src/pdf/mod.rs, crates/flow-core/src/pdf/metadata.rs, crates/flow-core/src/lib.rs</files>
  <behavior>
    - Build a source revision/hash-bound PdfFormPlan from a validated document, projection, and effective widget values; reject review entries, unsupported projection versions, mismatches, malformed values/options, and overlimits.
    - Attach a plan through PdfExportRequest without changing existing no-form exports.
    - Emit deterministic field dictionaries, widget annotations, catalog AcroForm Fields, fixed-point rectangles, field flags, option values/defaults, and page references using the owned COS graph.
    - Promote only FormFields in the support report when the plan is present; keep generic annotations/actions/external behavior unsupported.
  </behavior>
  <action>Implement the bounded PDF forms adapter and integrate it at the existing page annotation/catalog hooks. Keep appearance streams absent and explicit in the support boundary.</action>
  <acceptance_criteria>
    - Existing envelope exports remain byte-compatible when no form plan is attached.
    - Form COS emission never serializes browser state, DOM paths, raw diagnostics, or canonical page coordinates.
  </acceptance_criteria>
  <verify><automated>cargo fmt --all &amp;&amp; cargo test --locked -p flow-core --test pdf_forms -- --nocapture</automated></verify>
  <done>Owned exports can carry bounded AcroForm structure from an accepted Rust projection, without an appearance or viewer-compatibility claim.</done>
</task>

<task id="05-04-02" type="auto" tdd="true">
  <name>05-04-02: Prove form COS determinism and fail-closed export guards</name>
  <read_first>
    - crates/flow-core/tests/pdf_cos.rs
    - crates/flow-core/tests/pdf_resources.rs
    - crates/flow-core/src/pdf/forms.rs
  </read_first>
  <files>crates/flow-core/tests/pdf_forms.rs</files>
  <behavior>
    - Text, checkbox, radio, select, signature, and button fields map to bounded field dictionaries with effective/default values and deterministic option names.
    - Repeated form exports produce identical hashes/bytes; changing only the session value changes the derived export identity while preserving placement/source identity.
    - Stale source/session, review entries, invalid rectangles, page overflow, unknown options, and field-count/option limits fail before publication.
  </behavior>
  <action>Add focused integration tests over real display-list/form projections and the owned export envelope. Assert safe debug/errors and explicit absence of appearance/viewer claims.</action>
  <acceptance_criteria>
    - `cargo test --locked -p flow-core --test pdf_forms -- --nocapture` passes with no ignored branch.
    - `cargo clippy --locked -p flow-core --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <verify><automated>cargo test --locked -p flow-core --test pdf_forms -- --nocapture &amp;&amp; cargo clippy --locked -p flow-core --all-targets -- -D warnings</automated></verify>
  <done>The bounded AcroForm structure has executable Rust evidence and its non-goals remain explicit.</done>
</task>
</tasks>

<artifacts>PDF forms adapter, COS export integration, dynamic support report, focused PDF form tests.</artifacts>
<verification>Run formatting, focused PDF form tests, and locked clippy.</verification>
<success_criteria>Only accepted Rust form projections reach COS; appearance, flattening, import, and target-viewer validation remain future gates.</success_criteria>
