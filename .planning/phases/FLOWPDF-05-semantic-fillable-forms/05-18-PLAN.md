---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "18"
type: execute
wave: 1
depends_on: ["05-17"]
files_modified:
  - web/src/forms/form-projection.ts
  - web/src/forms/form-projection-viewport.tsx
  - web/src/forms/form-projection.css
  - web/src/editor/editor-controller.ts
  - web/src/editor/editor-app.tsx
  - web/tests/form-projection.test.ts
  - web/tests/editor-controller.test.ts
  - web/tests/form-projection.browser.test.ts
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-18-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-18-PLAN.md
requirements: [FORM-07, QUAL-07]
estimate: { tokens: 16000, raw_tokens: 16000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "A synchronized, review-free Rust form plan exposes explicit accessible field selection and clear/select-all actions without mutating semantic or session state."
    - "Selection state is source/projection-bound, normalized in Rust plan order, and cleared when the accepted projection identity changes."
    - "A PDF export request factory receives the source-bound selection separately from the opaque serialized request, while stale or malformed selections fail before scheduling."
    - "Review-bearing or stale projections never enable flatten selection controls, and the visual overlay remains aria-hidden."
  artifacts:
    - "Versioned FormProjectionSelectionDto and fail-closed selection constructor/validator."
    - "Accessible flatten-selection fieldset integrated with FormProjectionViewport and EditorApp."
    - "Optional controller export-factory selection seam with unit and Chromium evidence."
  key_links:
    - "verified FormProjectionResult.formPlan -> FormProjectionSelectionDto -> EditorController.requestPdfExport"
    - "projection identity change -> EditorApp selection reset"
    - "selection controls -> opaque caller-owned PDF request factory, never direct Rust/document mutation"

objective: "Add explicit accessible flatten-selection controls over the verified projection and carry the source-bound selection to PDF export without moving authority into the browser."

context:
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-18-RESEARCH.md
  - web/src/forms/form-projection.ts
  - web/src/forms/form-projection-viewport.tsx
  - web/src/editor/editor-controller.ts
  - web/src/editor/editor-app.tsx
  - crates/flow-core/src/pdf/forms.rs

tasks:
  - id: 05-18-01
    type: auto
    tdd: true
    name: "05-18-01: Add source-bound selection and export seam"
    read_first:
      - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-18-RESEARCH.md
      - web/src/forms/form-projection.ts
      - web/src/editor/editor-controller.ts
      - crates/flow-core/src/pdf/forms.rs
    files:
      - web/src/forms/form-projection.ts
      - web/src/editor/editor-controller.ts
      - web/src/editor/editor-app.tsx
    behavior:
      - "The helper creates a versioned selection only from a synchronized, review-free verified plan, validates source/display/plan identities, rejects unknown or duplicate IDs, and returns field IDs in plan order."
      - "EditorApp stores selection only as UI state, clears it on accepted projection identity changes, and passes the derived selection to PDF export."
      - "EditorController accepts an optional selection argument, checks its source identity, and passes it as a third argument to existing-compatible export factories before scheduling."
      - "The serialized PDF request remains opaque to the browser selection layer; no canonical JSON or form-session mutation is introduced."
    action: "Implement the typed selection DTO, normalization/validation helper, controller seam, and EditorApp selection lifecycle."
    acceptance_criteria:
      - "An empty selection remains valid and means no fields are flattened; selected IDs can only come from the verified plan."
      - "Source-mismatched or invalid selections return stable failure outcomes and never call the PDF scheduler."
      - "Existing two-argument PDF export factories continue to typecheck and behave unchanged."
    verify:
      automated: "npm run typecheck && npm run test:unit -- web/tests/form-projection.test.ts web/tests/editor-controller.test.ts"
    done: "The browser can hand an identity-bound flatten selection to an export caller without becoming an export or semantic authority."

  - id: 05-18-02
    type: auto
    tdd: true
    name: "05-18-02: Add accessible selection controls and browser evidence"
    read_first:
      - web/src/forms/form-projection-viewport.tsx
      - web/src/forms/form-projection.css
      - web/tests/form-projection.browser.test.ts
      - web/tests/form-controls.browser.test.ts
    files:
      - web/src/forms/form-projection-viewport.tsx
      - web/src/forms/form-projection.css
      - web/tests/form-projection.browser.test.ts
    behavior:
      - "A ready review-free plan renders a labelled fieldset with keyboard-accessible per-field checkboxes, select-all, clear, and selected-count status."
      - "Review-bearing, stale, pending, and planless states show an explicit disabled reason and never offer flatten selection."
      - "Selection changes affect only derived UI/export state; semantic inputs remain the sole editable/accessibility surface and the visual overlay remains aria-hidden."
      - "Changing source or projection identity clears old selection before a new matching plan becomes selectable."
    action: "Render the localized selection fieldset, add styles and public data attributes, and extend the Chromium fixture with plan fields, review/selection assertions, and source-reset coverage."
    acceptance_criteria:
      - "Tests use roles, labels, and public data attributes rather than DOM geometry or reconstructed text."
      - "All existing layout, form-control, accessibility, PDF-preview, and boundary tests remain green."
      - "No target-viewer, external import, or general flattening claim is added."
    verify:
      automated: "npm run test:browser -- form-projection.browser.test.ts"
    done: "FORM-07 has explicit browser selection evidence while Rust remains the only flattening authority."

artifacts: "Versioned source-bound flatten selection, accessible controls, export factory propagation, and Chromium evidence."
verification: "Run focused unit/typecheck/browser tests, then the full repository and Phase 4 gates."
success_criteria: "Users can explicitly select accepted form fields for a later PDF export, with stale/review states fenced and no semantic state mutation."
