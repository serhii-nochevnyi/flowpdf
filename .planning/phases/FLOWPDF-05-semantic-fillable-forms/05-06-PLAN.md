---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "06"
type: execute
wave: 1
depends_on: ["05-05"]
files_modified:
  - web/src/editor/editor-controller.ts
  - web/src/forms/form-session.ts
  - web/src/editor/editor-app.tsx
  - web/src/editor/semantic-document.tsx
  - web/src/editor/field-navigation.tsx
  - web/src/editor/editor-messages.ts
  - web/src/editor/editor.css
  - web/tests/form-session.test.ts
  - web/tests/form-controls.browser.test.ts
  - web/tests/editor-accessibility.browser.test.ts
autonomous: true
requirements: [FORM-05, QUAL-03, QUAL-04, QUAL-07]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Valid authored fields render as native semantic controls; the canvas is not the input authority."
    - "Every writable control routes SetValue/ClearValue through the Rust form-session boundary and publishes only accepted session state."
    - "Rust validation errors remain visible and accessible without mutating canonical revision/history or persisting rejected values."
    - "Read-only, signature, button, and review-region fields remain explicit non-writable states rather than guessed controls."
  artifacts:
    - "Session coordinator bound to the accepted canonical JSON/document identity and the Plan 05-05 WASM/IndexedDB bridge."
    - "Accessible native text, checkbox, radio, and select controls with localized labels/statuses and stable field selectors."
    - "Real Chromium evidence for all writable field kinds, clear-to-default, failed validation, and canonical revision stability."
  key_links:
    - "accepted editor snapshot -> source-bound session coordinator -> Rust FormSessionAction -> IndexedDB accepted DTO"
    - "Rust editor field projection -> native semantic controls -> accessible status/error region"
---

<objective>Make already-authored semantic fields fillable through accessible native controls backed by the Rust-owned noncanonical session.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-CONTEXT.md
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-06-RESEARCH.md
@web/src/forms/form-session.ts
@web/src/editor/editor-app.tsx
@web/src/editor/field-navigation.tsx
</context>

<tasks>
<task id="05-06-01" type="auto" tdd="true">
  <name>05-06-01: Coordinate accepted editor identity with the form-session bridge</name>
  <read_first>
    - web/src/forms/form-session.ts
    - web/src/editor/editor-controller.ts
    - web/src/editor/editor-app.tsx
    - web/persistence/indexeddb-store.ts
  </read_first>
  <files>web/src/editor/editor-controller.ts, web/src/forms/form-session.ts, web/src/editor/editor-app.tsx, web/tests/form-session.test.ts</files>
  <behavior>
    - A mounted editor starts or validates exactly one session for the accepted canonical JSON, document ID, revision, and hash.
    - A canonical source identity change discards the previous UI session reference before loading the new source-bound session.
    - Successful SetValue/ClearValue updates notify the semantic form controls; failed Rust responses retain the prior accepted session and expose only the stable error code.
    - Session initialization or persistence failure never changes the editor's canonical revision/history state.
  </behavior>
  <action>Extend the existing cached WASM boundary and form-session module with a small subscribable coordinator. Instantiate it from the editor app using the existing IndexedDB database option and accepted snapshot identity, while keeping physical persistence and Rust validation separate from the editor transaction controller.</action>
  <acceptance_criteria>
    - The coordinator can be tested with a fake WASM boundary and in-memory persistence without importing React or reimplementing field validation.
    - No rejected form action calls `saveFormSession`, and canonical editor state remains unchanged across session updates.
  </acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npx --no-install vitest run --project unit web/tests/form-session.test.ts</automated></verify>
  <done>The app has a source-bound accepted-session coordinator ready for semantic field controls.</done>
</task>

<task id="05-06-02" type="auto" tdd="true">
  <name>05-06-02: Render accessible native controls for valid authored fields</name>
  <read_first>
    - web/src/editor/field-navigation.tsx
    - web/src/editor/semantic-document.tsx
    - web/src/editor/editor-messages.ts
    - web/tests/editor-accessibility.browser.test.ts
  </read_first>
  <files>web/src/editor/semantic-document.tsx, web/src/editor/field-navigation.tsx, web/src/editor/editor-messages.ts, web/src/editor/editor.css, web/tests/form-controls.browser.test.ts, web/tests/editor-accessibility.browser.test.ts</files>
  <behavior>
    - Text fields use bounded native input/textarea controls with the descriptor hint, labels, required/read-only state, and clear action.
    - Checkbox, radio-group, and select controls expose option labels and route typed values through the coordinator; clear restores the authored default.
    - Signature and button fields show explicit non-editable status, and invalid/deleted/review fields remain in the review region without writable controls.
    - Pending, validation-error, and persistence-error status is localized, announced, and associated with the affected field without exposing canonical payloads.
  </behavior>
  <action>Replace the current valid-field metadata-only card with a semantic control projection driven by accepted session state. Keep labels/options/read-only decisions from the Rust descriptor, use native DOM controls for keyboard/AT behavior, and add focused Chromium tests for both locales, all writable kinds, clear/default behavior, error announcement, and unchanged editor revision.</action>
  <acceptance_criteria>
    - Existing field ordering, review selectors, and editor accessibility contracts remain green.
    - Browser handlers construct only FormSessionAction DTOs and never mutate canonical JSON, editor view objects, or DOM text as semantic truth.
  </acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; PLAYWRIGHT_BROWSERS_PATH=./work/playwright npx --no-install vitest run --project browser web/tests/form-controls.browser.test.ts web/tests/editor-accessibility.browser.test.ts</automated></verify>
  <done>Already-authored valid fields are fillable through accessible native controls while Rust remains the sole value authority.</done>
</task>
</tasks>

<artifacts>Source-bound form-session coordinator, accessible native field controls, localized status/error projection, and Chromium evidence.</artifacts>
<verification>Run TypeScript, focused unit, focused browser accessibility, and form-control suites.</verification>
<success_criteria>Only Rust-accepted noncanonical values reach browser persistence; semantic controls remain keyboard/AT usable and canonical editor history is unchanged.</success_criteria>
