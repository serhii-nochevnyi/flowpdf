---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "25"
title: "Verify the PDF worker message loop at its scope boundary"
type: execute
wave: 1
depends_on: ["05-24"]
files_modified:
  - web/tests/pdf-worker.test.ts
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-25-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-25-PLAN.md
requirements: [FORM-07, QUAL-07]
delivery:
  ticket: T-05-25
  risk: low
  human_checkpoint: false
estimate: { tokens: 5500, raw_tokens: 5500, tasks: 1, confidence: high }
must_haves:
  truths:
    - "A valid worker export produces one closed accepted outbound message after scheduler publication."
    - "A matching cancel message produces a closed cancelled outbound message and never publishes stale bytes."
    - "The worker loop reuses scheduler guards rather than becoming a second PDF authority."
    - "No real Worker, font catalog, active entry, or semantic state wiring is added."
  artifacts:
    - "Fake PdfWorkerScope unit evidence for accepted and cancelled message routing."
  key_links:
    - "PdfWorkerScope.onmessage -> RevisionAwarePdfExportScheduler.request/cancel"
    - "scheduler publication/outcome -> closed PdfWorkerOutboundMessage"

objective: "Close the remaining unit-evidence gap around the existing PDF worker message protocol."

context:
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-25-RESEARCH.md
  - web/src/pdf/pdf-worker.ts
  - web/src/pdf/pdf-protocol.ts
  - web/tests/pdf-worker.test.ts

tasks:
  - id: 05-25-01
    type: auto
    tdd: true
    name: "05-25-01: Assert accepted and cancelled worker messages"
    read_first:
      - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-25-RESEARCH.md
      - web/src/pdf/pdf-worker.ts
      - web/src/pdf/pdf-protocol.ts
      - web/tests/pdf-worker.test.ts
    files:
      - web/tests/pdf-worker.test.ts
    behavior:
      - "A fake scope can invoke the installed export/cancel message handler."
      - "Accepted result publication emits the request-bound accepted message."
      - "Cooperative cancellation emits cancelled and suppresses accepted publication."
    action: "Import installPdfWorker/PdfWorkerScope, add a deterministic fake scope, and cover accepted and cancelled routing."
    acceptance_criteria:
      - "Focused PDF worker unit tests pass with exact request IDs and message kinds."
      - "No stale result is posted after matching cancellation."
      - "No production worker/font catalog or active entry wiring is introduced."
    verify:
      automated: "npm run typecheck && npm run test:unit -- web/tests/pdf-worker.test.ts"
    done: "The existing PDF worker protocol has direct unit evidence at its scope boundary."

artifacts: "Closed PDF worker accepted/cancelled message-loop evidence."
verification: "Run the scoped typecheck and worker unit suite, then the full project check."
success_criteria: "Worker-scope routing remains revision-safe and delegates all PDF authority to the existing scheduler/Rust boundary."
