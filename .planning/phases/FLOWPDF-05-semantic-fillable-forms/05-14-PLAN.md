---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "14"
type: execute
wave: 1
depends_on: ["05-13"]
files_modified:
  - crates/flow-wasm/src/lib.rs
  - crates/flow-wasm/tests/pdf_exports.rs
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-14-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-14-PLAN.md
requirements: [FORM-07, QUAL-07]
estimate: { tokens: 12000, raw_tokens: 12000, tasks: 2, confidence: high }
must_haves:
  truths:
    - "The narrow WASM export envelope can carry an optional source-bound form plan and an explicit flatten selection without changing existing no-form callers."
    - "WASM flattening delegates plan, selection, geometry, and source validation to Rust-owned core contracts before publishing bytes."
    - "Deterministic partial/all flattening and exact source recovery survive the string-only WASM boundary."
    - "Malformed, unknown, plan-less, stale, or duplicate selections fail with the existing stable error envelope and no partial result."
  artifacts:
    - "Optional formPlan/flattenedFieldIds wire fields threaded into PdfExportRequest and owned PDF output."
    - "Focused WASM export tests for deterministic selection, retained/all-field output, recovery, and fail-closed rejection."
  key_links:
    - "serialized formPlan + flattenedFieldIds -> PdfExportOptions/with_form_plan -> owned core export"
    - "WASM export result -> manifest/fingerprint/private source -> exact recovery"

<objective>Expose Plan 05-13's explicit selected-field flattening through the Rust/WASM PDF export boundary while keeping the browser transport opaque and Rust authoritative.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-14-RESEARCH.md
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-13-SUMMARY.md
@crates/flow-wasm/src/lib.rs
@crates/flow-wasm/tests/pdf_exports.rs
@crates/flow-core/src/pdf/mod.rs
</context>

<tasks>
<task id="05-14-01" type="auto" tdd="true">
  <name>05-14-01: Thread the validated form plan through the WASM export envelope</name>
  <read_first>
    - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-14-RESEARCH.md
    - crates/flow-wasm/src/lib.rs
    - crates/flow-core/src/pdf/mod.rs
    - crates/flow-core/src/pdf/forms.rs
  </read_first>
  <files>crates/flow-wasm/src/lib.rs</files>
  <behavior>
    - Existing requests without formPlan or flattenedFieldIds remain accepted and deterministic.
    - Optional formPlan is attached through PdfExportRequest::with_form_plan; the adapter does not duplicate core validation.
    - Explicit selections are copied into PdfExportOptions and remain source-bound through core fingerprint, manifest, and output validation.
    - Unknown JSON remains rejected by the closed request envelope.
  </behavior>
  <action>Add optional serde fields, thread them into the core request in the existing validation order, and preserve protocol-version/error-envelope behavior.</action>
  <acceptance_criteria>
    - No canonical document, form-session, browser UI, or client-side geometry schema changes are introduced.
    - The Rust/WASM adapter publishes no bytes when plan identity or flatten selection validation fails.
    - Empty/default requests remain byte-compatible with the pre-plan wire contract.
  </acceptance_criteria>
  <verify><automated>cargo fmt --all -- --check &amp;&amp; cargo test --locked -p flow-wasm --test pdf_exports -- --nocapture</automated></verify>
  <done>WASM accepts only explicitly admitted optional form export data and delegates semantic authority to flow-core.</done>
</task>

<task id="05-14-02" type="auto" tdd="true">
  <name>05-14-02: Prove WASM partial/all flattening and recovery fences</name>
  <read_first>
    - crates/flow-wasm/tests/pdf_exports.rs
    - crates/flow-core/tests/pdf_forms.rs
    - crates/flow-core/src/pdf/forms.rs
  </read_first>
  <files>crates/flow-wasm/tests/pdf_exports.rs</files>
  <behavior>
    - A checked-in-font source-bound plan crosses JSON and produces deterministic partial and all-field exports.
    - Partial output retains AcroForm/widget evidence; all-field output omits editable form objects while retaining derived page content.
    - Selection identity appears in the manifest/fingerprint and exact private-source recovery returns the original canonical document.
    - Duplicate, unknown, plan-less, stale, and malformed selections fail closed with stable codes and null results.
  </behavior>
  <action>Add a real form-plan fixture helper through pagination/display-list/projection, exercise repeated and reordered requests, and assert response bytes, manifest, recovery, and rejection envelopes.</action>
  <acceptance_criteria>
    - Tests prove the string-only boundary without invoking an unavailable external viewer.
    - `npm run check` and `npm run check:phase4` remain green, with external/reference rows still reported honestly as unavailable.
  </acceptance_criteria>
  <verify><automated>cargo test --locked -p flow-wasm --test pdf_exports -- --nocapture &amp;&amp; npm run check &amp;&amp; npm run check:phase4</automated></verify>
  <done>FORM-07's explicit flattening contract is source-bound and deterministic through the Rust/WASM export envelope.</done>
</task>
</tasks>

<artifacts>Optional WASM form-plan/flatten selection transport, deterministic partial/all output evidence, and exact source recovery.</artifacts>
<verification>Run focused WASM export tests, then full repository and Phase 4 gates.</verification>
<success_criteria>Only an explicitly supplied validated Rust form plan and field selection can cross the WASM export boundary; no browser-side semantic authority is introduced.</success_criteria>
