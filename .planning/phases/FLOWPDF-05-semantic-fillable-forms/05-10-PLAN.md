---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "10"
type: execute
wave: 1
depends_on: ["05-09"]
files_modified:
  - crates/flow-core/src/transaction/mod.rs
  - crates/flow-core/src/capability/mod.rs
  - crates/flow-core/src/audit/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/field_authoring.rs
  - crates/flow-core/tests/command_parity.rs
  - web/src/editor/editor-controller.ts
  - web/src/editor/field-navigation.tsx
  - web/src/editor/editor-messages.ts
  - web/src/editor/editor.css
  - web/tests/form-controls.browser.test.ts
  - web/tests/editor-accessibility.browser.test.ts
  - tests/contracts/phase2-command-parity.json
  - tests/contracts/phase1-boundary.test.mjs
  - tests/contracts/phase2-gate.test.mjs
  - scripts/check-phase2.mjs
requirements: [FORM-01, FORM-02, FORM-03, FORM-05, QUAL-03, QUAL-04, QUAL-07, QUAL-08]
estimate: { tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: high }
must_haves:
  truths:
    - "A confirmed RemoveField request removes exactly one canonical descriptor through a parity-catalogued Rust transaction."
    - "The exact inverse restores the removed descriptor at its original vector index, including middle-field removal, through undo, redo, and durable recovery."
    - "Unconfirmed, missing, stale, and forged field-removal requests leave canonical revision/history and source-bound values unchanged."
    - "The accessible browser exposes removal only for valid projected fields, confirms it natively, and never mutates canonical field arrays locally."
  artifacts:
    - "Closed Rust RemoveField command/mutation/operation with confirmation, audit, capability, replay, and parity coverage."
    - "Localized field-card removal action and alert dialog for Ukrainian and English users."
    - "Rust and Chromium evidence for middle removal, exact reversal, recovery, session rebinding, and review-field protection."
  key_links:
    - "valid Rust field projection -> accessible removal button -> explicit confirmation -> RemoveField -> exact indexed inverse"
    - "RemoveField parity catalog -> audit/recovery allowlist -> Phase 2 boundary and gate contract"
    - "canonical source revision/hash change -> form-session rebinding -> undo does not leak an old override into a new source identity"
---

<objective>Add the first explicit semantic field deletion path while retaining exact source, value-session, accessibility, and recovery boundaries.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-10-RESEARCH.md
@crates/flow-core/src/transaction/mod.rs
@crates/flow-core/src/capability/mod.rs
@crates/flow-core/src/audit/mod.rs
@crates/flow-core/src/lib.rs
@web/src/editor/field-navigation.tsx
@web/src/editor/structural-controls.tsx
@web/tests/form-controls.browser.test.ts
</context>

<tasks>
<task id="05-10-01" type="auto" tdd="true">
  <name>05-10-01: Add the parity-closed reversible Rust RemoveField path</name>
  <read_first>
    - crates/flow-core/src/transaction/mod.rs
    - crates/flow-core/src/capability/mod.rs
    - crates/flow-core/src/audit/mod.rs
    - crates/flow-core/src/lib.rs
    - crates/flow-core/tests/field_authoring.rs
    - crates/flow-core/tests/command_parity.rs
  </read_first>
  <files>crates/flow-core/src/transaction/mod.rs, crates/flow-core/src/capability/mod.rs, crates/flow-core/src/audit/mod.rs, crates/flow-core/src/lib.rs, crates/flow-core/tests/field_authoring.rs, crates/flow-core/tests/command_parity.rs, tests/contracts/phase2-command-parity.json, tests/contracts/phase1-boundary.test.mjs, scripts/check-phase2.mjs</files>
  <behavior>
    - RemoveField requires explicit confirmation, locates one existing descriptor by ID, and derives exact RemoveField/InsertField operations.
    - Removing a middle field and undoing it restores the original descriptor bytes and vector order; recovery replays the committed removal without a gap.
    - Missing IDs, false confirmation, stale bases, and malformed requests reject without publication.
    - Audit command naming, capability metadata, future-voice metadata, replay allowlists, and the checked-in 35-family parity contract remain one-to-one.
  </behavior>
  <action>Extend the closed command/mutation/capability/audit/recovery mappings, add the indexed inverse insertion fence, update the checked-in parity and boundary counts, and add focused Rust tests for confirmation, middle removal, exact undo/redo, recovery, and failure atomicity.</action>
  <acceptance_criteria>
    - No field-removal path bypasses validate_document, transaction history, audit redaction, or durable recovery.
    - Existing 34-family traces remain semantically stable while the catalog expands deterministically to include removeField.
    - Forward InsertField remains append-only; only a checked inverse operation may restore an earlier index.
  </acceptance_criteria>
  <verify><automated>cargo fmt --all -- --check &amp;&amp; cargo test --locked -p flow-core --test field_authoring --test command_parity &amp;&amp; node --test tests/contracts/phase1-boundary.test.mjs</automated></verify>
  <done>RemoveField is a confirmed, Rust-owned, reversible, parity-visible command with focused transaction and recovery evidence.</done>
</task>

<task id="05-10-02" type="auto" tdd="true">
  <name>05-10-02: Expose and verify accessible field removal</name>
  <read_first>
    - web/src/editor/field-navigation.tsx
    - web/src/editor/editor-controller.ts
    - web/src/editor/editor-messages.ts
    - web/tests/form-controls.browser.test.ts
    - web/tests/editor-accessibility.browser.test.ts
  </read_first>
  <files>web/src/editor/editor-controller.ts, web/src/editor/field-navigation.tsx, web/src/editor/editor-messages.ts, web/src/editor/editor.css, web/tests/form-controls.browser.test.ts, web/tests/editor-accessibility.browser.test.ts</files>
  <behavior>
    - Ukrainian and English valid-field cards expose a keyboard-reachable remove action with a native alert dialog and localized labels.
    - Confirming removal dispatches only the typed RemoveField command; the valid card disappears, the canonical revision advances once, and the form session rebinds to the new source.
    - Cancel, Escape, review-region fields, and unconfirmed/forged paths do not change canonical state.
    - Undo and redo restore/remove the exact field and rebind source-bound session state without browser-side canonical mutation.
  </behavior>
  <action>Add the typed RemoveField DTO, localized removal labels/dialog, and a field-card action that routes confirmation through the existing structural command bus. Add real Chromium coverage for both locales, including review-field protection and undo/redo/session rebinding.</action>
  <acceptance_criteria>
    - Removal controls are native, labeled, focus-visible, and meet the existing hit-target/accessibility contract.
    - Existing field configuration, placement, value controls, and insertion behavior remain green.
    - Full type, unit, browser, Rust, repository, Phase 2, and Phase 4 gates pass.
  </acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npx --no-install vitest run --project browser web/tests/form-controls.browser.test.ts web/tests/editor-accessibility.browser.test.ts</automated></verify>
  <done>Users can explicitly remove a valid semantic field accessibly, with Rust-owned confirmation, exact undo/redo, and honest review-field boundaries.</done>
</task>
</tasks>

<artifacts>Parity-closed Rust field removal, indexed exact inverse, localized accessible confirmation flow, and transaction/browser evidence.</artifacts>
<verification>Run focused Rust/parity/boundary checks, then TypeScript, Chromium, full repository, Phase 2, and Phase 4 gates.</verification>
<success_criteria>Only confirmed Rust RemoveField results alter canonical fields; review, unconfirmed, stale, malformed, and canceled paths remain canonical no-ops.</success_criteria>
