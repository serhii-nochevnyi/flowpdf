---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "20"
title: "Prove the browser PDF request builder at the WASM export boundary"
type: execute
wave: 1
depends_on: ["05-19"]
files_modified:
  - web/tests/pdf-request.browser.test.ts
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-20-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-20-PLAN.md
requirements: [FORM-07, QUAL-07]
delivery:
  ticket: T-05-20
  risk: low
  human_checkpoint: false
estimate: { tokens: 5000, raw_tokens: 5000, tasks: 1, confidence: high }
must_haves:
  truths:
    - "The shared browser PDF request builder produces opaque JSON accepted by the generated Rust/WASM exporter."
    - "The accepted WASM response remains bound to the Rust-produced canonical source revision/hash and the supplied layout identity."
    - "The smoke test does not add browser semantic, geometry, font, or PDF authority."
  artifacts:
    - "Browser evidence for createPdfExportRequest -> generated WASM export_pdf -> verified response."
  key_links:
    - "Rust create_sample session -> createPdfExportRequest -> PdfExportWireRequest"
    - "serializedRequest -> export_pdf -> verify_pdf_export_response"

objective: "Add a real Chromium/WASM smoke for the ordinary shared PDF request path without changing production wiring scope."

context:
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-20-RESEARCH.md
  - web/src/pdf/pdf-request.ts
  - web/generated/flow_wasm.js
  - crates/flow-wasm/src/lib.rs

tasks:
  - id: 05-20-01
    type: auto
    tdd: true
    name: "05-20-01: Exercise the shared request through generated WASM"
    read_first:
      - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-20-RESEARCH.md
      - web/src/pdf/pdf-request.ts
      - web/tests/transaction-boundary.browser.test.ts
      - crates/flow-wasm/src/lib.rs
    files:
      - web/tests/pdf-request.browser.test.ts
    behavior:
      - "The test obtains a valid canonical session from Rust create_sample rather than embedding or rewriting document semantics."
      - "The builder emits one ordinary request with accepted page bounds and matching source/layout identities."
      - "Rust export_pdf accepts the serialized request and verify_pdf_export_response accepts the complete response."
    action: "Add the browser smoke fixture and assertions for successful Rust/WASM export, source identity, layout identity, and manifest provenance."
    acceptance_criteria:
      - "The test fails if the builder emits an unknown Rust wire field, stale source identity, invalid page envelope, or unverifiable response."
      - "No production entry point, worker construction, font bytes, or target-viewer claim is added."
    verify:
      automated: "npm run typecheck && npm run test:browser -- web/tests/pdf-request.browser.test.ts"
    done: "The ordinary builder path is proven against the actual generated Rust/WASM PDF boundary."

artifacts: "Generated-WASM browser evidence for the shared ordinary PDF request builder."
verification: "Run the scoped typecheck and browser test, then retain the full repository gates from Plan 05-19 as CI evidence."
success_criteria: "A browser-built opaque request is accepted and verified by Rust without moving semantic or geometry ownership into TypeScript."
