---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "02"
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - crates/flow-core/src/forms/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/forms.rs
autonomous: true
requirements: [FORM-05, QUAL-07]
estimate: { tokens: 12000, raw_tokens: 12000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "A form session keeps authored defaults separate from explicit user value overrides."
    - "Fill and clear operations are immutable, generation-bound, read-only aware, and reuse Rust field validation."
    - "A stale, mismatched, forged, or unknown-field session cannot publish a value."
  artifacts:
    - "Rust-owned revision/hash-bound FormSessionState with typed set/clear/read operations."
    - "Focused tests for defaults, overrides, reset, validation, read-only, serialization, and stale identity paths."
  key_links:
    - "FlowDocument field descriptors -> FormSessionState explicit overrides -> effective field value"
---

<objective>Separate authored field defaults from Rust-owned noncanonical fill state.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-CONTEXT.md
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-02-RESEARCH.md
@crates/flow-core/src/forms/mod.rs
@crates/flow-core/src/editor_view/mod.rs
</context>

<tasks>
<task id="05-02-01" type="auto" tdd="true">
  <name>05-02-01: Add the revision-bound Rust form session</name>
  <read_first>
    - crates/flow-core/src/forms/mod.rs
    - crates/flow-core/src/model/mod.rs
    - crates/flow-core/src/editor_view/mod.rs
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>crates/flow-core/src/forms/mod.rs, crates/flow-core/src/lib.rs</files>
  <behavior>
    - Create an empty override map bound to document ID, revision, and canonical hash while accepting valid empty required defaults.
    - Read effective values from an explicit override or the authored default without mutating the document.
    - Set and clear values through immutable generation increments, reject read-only fields, and route all proposed values through the closed validator.
    - Reject stale document identity, source-hash mismatch, unknown fields, invalid overrides, and forged read-only overrides before publication.
  </behavior>
  <action>Implement `FormSessionState`, typed `FormSessionError` codes, deterministic BTreeMap serialization, and public set/clear/effective-value methods. Keep the state noncanonical and free of page/PDF/DOM data; do not alter the FlowDocument schema or transaction record.</action>
  <acceptance_criteria>
    - The session is serializable, deterministic, privacy-safe, and explicitly revision/hash-bound.
    - Authored defaults remain byte-for-byte unchanged after every session operation.
  </acceptance_criteria>
  <verify><automated>cargo fmt --all &amp;&amp; cargo test --locked -p flow-core --test forms -- --nocapture</automated></verify>
  <done>Rust exposes a safe noncanonical form-value session independent of PDF appearance state.</done>
</task>

<task id="05-02-02" type="auto" tdd="true">
  <name>05-02-02: Prove fill/reset and identity guards</name>
  <read_first>
    - crates/flow-core/tests/forms.rs
    - crates/flow-core/src/forms/mod.rs
  </read_first>
  <files>crates/flow-core/tests/forms.rs</files>
  <behavior>
    - Setting a value returns a new session, keeps the prior session and document unchanged, and reads the override instead of the default.
    - Clearing removes only the override and restores the authored default; required empty user input, invalid option/type, read-only, stale, unknown, and forged-map cases fail with stable codes.
    - JSON round trips preserve deterministic session identity and explicit overrides.
  </behavior>
  <action>Add focused integration tests that assert effective values, generation/immutability, serialization, and every failure taxonomy without exposing authored values in diagnostics.</action>
  <acceptance_criteria>
    - `cargo test --locked -p flow-core --test forms -- --nocapture` passes with no ignored form-session branch.
    - `cargo clippy --locked -p flow-core --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <verify><automated>cargo test --locked -p flow-core --test forms -- --nocapture &amp;&amp; cargo clippy --locked -p flow-core --all-targets -- -D warnings</automated></verify>
  <done>FORM-05's Rust fill/clear/validate core has executable evidence while transport and PDF appearance work remain deferred.</done>
</task>
</tasks>

<artifacts>FormSessionState, typed errors, public re-exports, focused session tests.</artifacts>
<verification>Run formatting, focused form tests, and locked clippy.</verification>
<success_criteria>All session claims are tested; canonical defaults, document transactions, PDF appearances, and browser state remain separate.</success_criteria>
