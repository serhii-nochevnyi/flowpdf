---
phase: FLOWPDF-05-semantic-fillable-forms
plan: "01"
type: execute
wave: 1
depends_on: ["04-06"]
files_modified:
  - crates/flow-core/src/forms/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-core/tests/forms.rs
autonomous: true
requirements: [FORM-02, FORM-03, QUAL-07]
estimate: { tokens: 16000, raw_tokens: 16000, tasks: 2, confidence: medium }
must_haves:
  truths:
    - "Rust validates every current field kind/value shape and bounded text hint without browser heuristics."
    - "A valid grapheme-safe field anchor resolves to one deterministic fixed-point widget rectangle from an accepted display list."
    - "Stale identities, invalid anchors, and missing source mappings fail closed or enter an explicit review list."
  artifacts:
    - "Rust-owned forms validation and widget-projection module."
    - "Focused Rust tests for values, source mapping, reflow movement, and stale/review paths."
  key_links:
    - "FlowDocument field descriptors -> accepted PDF display-list source ranges -> derived FormWidgetProjection"
---

<objective>Establish the Rust-owned semantic form validation and anchor-to-widget projection contract.</objective>

<context>
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-CONTEXT.md
@.planning/phases/FLOWPDF-05-semantic-fillable-forms/05-RESEARCH.md
@crates/flow-core/src/editor_view/mod.rs
@crates/flow-core/src/pdf/display_list.rs
</context>

<tasks>
<task id="05-01-01" type="auto" tdd="true">
  <name>05-01-01: Add closed field-value validation and display-list widget projection</name>
  <read_first>
    - crates/flow-core/src/model/mod.rs
    - crates/flow-core/src/schema/mod.rs
    - crates/flow-core/src/pdf/display_list.rs
    - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-CONTEXT.md
  </read_first>
  <files>crates/flow-core/src/forms/mod.rs, crates/flow-core/src/lib.rs</files>
  <behavior>
    - Validate required values, text multiline/input-hint constraints, checkbox, radio, select cardinality/options, and unsupported signature/button value shapes with stable code-only errors.
    - Resolve only `GraphemeSafe` anchors whose node/source range exists in the accepted display list; preserve all other fields in explicit review output.
    - Bind the projection to document revision/hash and display-list hash, use stable field IDs, and derive deterministic tab order and fixed-point rectangles without CSS or PDF object state.
  </behavior>
  <action>Implement the forms module, including bounded validation, source-range/glyph-cluster caret placement, widget-size clamping to the page content rectangle, deterministic result hashing, stale identity checks, and explicit review reasons. Re-export only the public contract needed by tests and later WASM adapters.</action>
  <acceptance_criteria>
    - The public projection is serializable, closed, deterministic, and contains no DOM/PDF object numbers or raw diagnostics payloads.
    - The module never mutates `FlowDocument` or treats page coordinates as canonical state.
  </acceptance_criteria>
  <verify><automated>cargo fmt --all &amp;&amp; cargo test --locked -p flow-core --test forms -- --nocapture</automated></verify>
  <done>Rust exposes a revision-safe semantic form projection over the current field vocabulary.</done>
</task>

<task id="05-01-02" type="auto" tdd="true">
  <name>05-01-02: Prove form projection, validation, review, and stale guards</name>
  <read_first>
    - crates/flow-core/src/forms/mod.rs
    - crates/flow-core/tests/pdf_text.rs
    - crates/flow-core/tests/transaction_properties.rs
  </read_first>
  <files>crates/flow-core/tests/forms.rs</files>
  <behavior>
    - Every field kind has accepted and rejected value cases, including required and option/cardinality boundaries.
    - Two equal inputs produce byte-identical projection/result hashes; a changed display-list identity changes the derived identity.
    - Reflowed source ranges move the widget page/rectangle; stale revision/hash, invalid/deleted anchors, and missing mappings never publish a guessed widget.
  </behavior>
  <action>Build hand-authored display-list fixtures with source ranges and glyph clusters, plus sample descriptors and document identities. Add focused tests for deterministic projection, source movement, all validation branches, explicit review, and stale/mismatch errors. Keep authored text out of error strings.</action>
  <acceptance_criteria>
    - `cargo test --locked -p flow-core --test forms -- --nocapture` passes with no ignored form branch.
    - `cargo clippy --locked -p flow-core --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <verify><automated>cargo test --locked -p flow-core --test forms -- --nocapture &amp;&amp; cargo clippy --locked -p flow-core --all-targets -- -D warnings</automated></verify>
  <done>Plan 05-01 has executable evidence for the semantic form projection boundary.</done>
</task>
</tasks>

<artifacts>forms module, public re-exports, focused forms integration suite.</artifacts>
<verification>Run formatting, focused forms tests, and locked clippy.</verification>
<success_criteria>All first-slice form claims map to deterministic Rust tests; AcroForm and fill UI remain explicitly deferred.</success_criteria>
