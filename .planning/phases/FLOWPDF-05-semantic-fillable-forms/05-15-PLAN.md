phase: FLOWPDF-05-semantic-fillable-forms
plan: "15"
type: execute
wave: 1
depends_on: ["05-14"]
files_modified:
  - crates/flow-core/src/layout/mod.rs
  - crates/flow-core/src/forms/mod.rs
  - crates/flow-core/src/lib.rs
  - crates/flow-wasm/src/lib.rs
  - crates/flow-wasm/tests/form_projections.rs
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-15-RESEARCH.md
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-15-PLAN.md
requirements: [FORM-02, FORM-03, FORM-07, QUAL-07]
estimate: { tokens: 16000, raw_tokens: 16000, tasks: 2, confidence: high }
must_haves:
  truths:
    - "Rust/WASM can derive a source-bound form widget projection from the same explicit layout request that Rust pagination accepts."
    - "Session-effective values reach the derived widgets without changing canonical document bytes or revision."
    - "A review-bearing projection remains inspectable but cannot produce a PDF form plan; valid projections can produce a validated plan."
    - "The response and verifier reject malformed, stale, mismatched, or forged projection data without publishing partial results."
  artifacts:
    - "Reusable Rust layout execution context shared by pagination and form projection."
    - "Versioned form-projection WASM JSON request/response with projection, optional plan, source identities, and result hash."
    - "Focused WASM tests for deterministic default/session projection, plan derivation, review fencing, and verifier failures."
  key_links:
    - "opaque layoutRequestJson -> shared Rust layout execution -> display list -> session-aware form projection"
    - "form projection -> validated PdfFormPlan -> existing export envelope"
    - "projection response -> Rust result verifier -> publishable browser projection"

objective: "Expose Rust-owned fixed-point form projection through a closed WASM boundary without duplicating layout or form authority in the browser."

context:
  - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-15-RESEARCH.md
  - crates/flow-core/src/layout/mod.rs
  - crates/flow-core/src/forms/mod.rs
  - crates/flow-wasm/src/lib.rs

tasks:
  - id: 05-15-01
    type: auto
    tdd: true
    name: "05-15-01: Share validated layout execution and add the Rust projection protocol"
    read_first:
      - .planning/phases/FLOWPDF-05-semantic-fillable-forms/05-15-RESEARCH.md
      - crates/flow-core/src/layout/mod.rs
      - crates/flow-core/src/forms/mod.rs
      - crates/flow-core/src/pdf/forms.rs
    files:
      - crates/flow-core/src/layout/mod.rs
      - crates/flow-core/src/forms/mod.rs
      - crates/flow-core/src/lib.rs
      - crates/flow-wasm/src/lib.rs
    behavior:
      - "The existing layout JSON response remains byte-compatible for accepted requests and retains all current validation fences."
      - "The form projection request reuses the nested layout request validation and never trusts browser page coordinates or display-list data."
      - "Missing session uses authored defaults; a supplied session is validated against the decoded canonical document before projection."
      - "Review entries are returned as review data with no form plan; valid projections return a source-bound validated plan."
    action: "Factor the common layout execution into a bounded Rust helper, define the versioned projection DTOs/hash verifier, and expose a string-only WASM function."
    acceptance_criteria:
      - "No canonical schema, form-session mutation, or browser geometry schema changes are introduced."
      - "All failures return a closed error envelope with null result and no partial display list or plan."
      - "The endpoint has explicit request/result size and request-id limits."
    verify:
      automated: "cargo fmt --all -- --check && cargo check --locked --workspace"
    done: "Core and WASM own the complete projection derivation and share layout validation rather than duplicating it."

  - id: 05-15-02
    type: auto
    tdd: true
    name: "05-15-02: Prove deterministic session projection, plan derivation, and review fences"
    read_first:
      - crates/flow-wasm/src/lib.rs
      - crates/flow-wasm/tests/pdf_exports.rs
      - crates/flow-core/tests/forms.rs
      - crates/flow-core/src/pdf/forms.rs
    files:
      - crates/flow-wasm/tests/form_projections.rs
    behavior:
      - "Repeated valid requests return identical projection/plan bytes and pass the Rust verifier."
      - "A session override appears in the widget effective value while source revision/hash remain canonical."
      - "A valid projection returns a plan whose source/display-list identities are accepted by existing PDF plan validation."
      - "Legacy/deleted/unmapped anchors remain explicit review and do not yield a plan."
      - "Unknown JSON, stale session, nested layout identity mismatch, and forged result hashes fail closed."
    action: "Add a checked-in-font WASM fixture that builds the request through real pagination/display-list/form projection, then assert deterministic responses, session values, plan identity, review behavior, and verifier rejection."
    acceptance_criteria:
      - "Tests exercise the string-only WASM boundary rather than calling browser geometry or a mock projection."
      - "Existing layout, form, PDF, and full repository gates remain green."
      - "No target-viewer or external form-import claim is added."
    verify:
      automated: "cargo test --locked -p flow-wasm --test form_projections -- --nocapture && cargo clippy --locked --workspace --all-targets -- -D warnings"
    done: "FORM-02/03/07 have deterministic Rust/WASM projection evidence while browser UI remains a separate scope."

artifacts: "Rust layout execution reuse, form projection/plan WASM transport, and deterministic review/session/verifier evidence."
verification: "Run focused form projection tests, workspace clippy, then the full npm gate and Phase 4 honesty gate."
success_criteria: "Only Rust-derived display-list geometry and validated session state can produce a publishable form projection or PDF form plan across WASM."
